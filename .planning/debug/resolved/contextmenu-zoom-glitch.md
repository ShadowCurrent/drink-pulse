---
status: resolved
trigger: "mimo wszystko po long-press kiedy wiersz sie nieco zoomuje, wyglada to dziwnie i po kliknieciu gdziekolwiek poza tym action card po long-press, ten wiersz dziwnie wraca do normalnosci.. po prostu liquid glass ui dziwnie sie zachowuje"
created: 2026-08-03T12:57:07Z
updated: 2026-08-29T00:00:00Z
---

## Current Focus
<!-- OVERWRITE on each update - always reflects NOW -->

hypothesis: "CONFIRMED RESOLVED. Positional bottom list-row inset enlarged the context-menu lift frame only for last-in-day rows. Uniform event-row insets remove the asymmetry; inter-day spacing now belongs to non-interactive header rows."
test: "Automated build and History tests, screenshot comparison, then live-device verification of last-row and single-row long-press/dismiss behavior."
expecting: "Tight lifted card with no extra bottom space and smooth return after dismissal."
next_action: "None — user confirmed the fix live on 2026-08-29; archive session."

## Superseded Current Focus (2026-08-06T09:00:00Z, third live-rejection pause — retained verbatim for history)
<!-- Retained verbatim below for history; superseded once the 2026-08-11 specifics arrived. -->

hypothesis: "UNCONFIRMED. Option (b) (flatten List to one row per event, self-verified against wrong-row Duplicate/Delete targeting, the highlight-phase flash, and the settled-preview clipping) was REJECTED live by the user on 2026-08-06 — 'wciąż nie wygląda to dobrze' ('still doesn't look good'). No specifics were given this round before the user had to pause the session for a context/session restart. This is the THIRD time self-verification has diverged from live-device reality in this session (2026-08-03T16:00 ghosting; 2026-08-05T13:40 single-event-day blind spot; now this). Do not assume which of the four previously-claimed-fixed symptoms (wrong-row Duplicate, wrong-row Delete, highlight-phase flash, right-edge clipping) is still broken, or whether this is a NEW, not-yet-described symptom of the flattened-row architecture itself — nothing has been ruled in or out yet."
test: "NOT YET RUN. First step on resume: ask the user for specifics — which of the 4 checklist items from the last checkpoint failed, or is the complaint about something else entirely (visual grouping/seam look of the corner-rounded flattened rows, animation feel, spacing, etc.)? Do not guess or re-attempt a fix before getting a concrete repro description or fresh on-device video, per this session's own repeatedly-proven lesson that inference without direct evidence has failed multiple times already."
expecting: "A specific, actionable description of what still looks wrong, OR a screen recording, before resuming investigation."
next_action: "PAUSED for user-initiated session/context restart (2026-08-06). On resume (`/gsd-debug continue contextmenu-zoom-glitch`): re-ask what specifically still looks wrong (which checklist item, or new symptom) before touching any code. Uncommitted working tree from the Option (b) implementation is presumed still in place (verify with `git status` on resume — do not assume, the user may have discarded it during the restart) — files: `HistoryListQueryView.swift`, `DPGlass.swift`, new `HistoryFlatRow.swift`/`HistoryEventCardRow.swift`, deleted `HistoryDaySectionCard.swift`, plus `WrongRowContextMenuTargetUITests.swift`."

## Superseded Current Focus (2026-08-05T20:10:00Z, Option-B reasoning checkpoint — implemented and self-verified, retained verbatim for history)
<!-- Retained verbatim below for history; the hypothesis here was confirmed true by the video/test evidence appended this turn. -->

reasoning_checkpoint_option_b_20260805:
  hypothesis: "Flattening `HistoryListQueryView`'s List to one top-level row per `ConsumptionEvent` (day boundaries as non-interactive header pseudo-rows in the SAME flat `ForEach`, no `List.Section`, no nested inner `ForEach`) removes the shared-List-row condition at its ROOT, because it is the literal mechanism both merged bugs depend on: the wrong-target routing bug requires MULTIPLE `.contextMenu`s declared on sibling subviews of ONE shared List row (confirmed via `os.Logger` BUILD-vs-EXEC trace, Evidence 2026-08-05T19:15:00Z) — once every event is its own List row, there is exactly ONE `.contextMenu` per row, so there is no sibling to misroute to. The highlight-phase crossfade requires an ancestor wider than the pressed row for UIKit's highlighting-preview snapshot to latch onto (`reasoning_checkpoint_reopened_20260805`) — once every event is its own List row/cell, there is no sibling content left in that cell to bleed into the snapshot, regardless of whether the row's own background is corner-rounded or square. Because BOTH bugs are removed at the row-boundary level (not by reshaping any preview), the settled-preview custom `.contextMenu(menuItems:preview:)` machinery added for Option A (PreferenceKey width measurement, `previewContentInset`+`.clipShape` clipping fix) becomes UNNECESSARY: reverting `EventContextMenuModifier` to the plain, pre-session `.contextMenu(menuItems:)` (matching last-committed HEAD) should now render correctly, because the DEFAULT lift/preview mechanism will resolve to exactly this single event's own List row/cell — the SAME ~338pt-content/16pt-list-inset/16pt-internal-padding geometry already confirmed clean against the default lift's ~1.19x scale in the ORIGINAL List-architecture investigation (Evidence 2026-08-03T18:00:00Z, on a single-event day, which is architecturally what EVERY row now is)."
  confirming_evidence:
    - "Evidence 2026-08-05T19:15:00Z (os.Logger BUILD-vs-EXEC trace): the wrong-target bug's mechanism is specifically 'multiple `.contextMenu`s nested as siblings inside ONE List row' — not a Swift-level bug and not tied to preview rendering. Removing the multi-sibling condition removes the mechanism's precondition entirely, independent of whatever `.contextMenu` variant (default vs custom preview) is used afterward."
    - "Evidence 2026-08-05T14:35:00Z/14:45:00Z (highlight-phase crossfade): the transient snapshot was shown to bleed in ALL sibling rows of the shared day-card ancestor — a condition that structurally cannot occur once each row is its own List cell with no sibling event content inside it."
    - "Evidence 2026-08-03T18:00:00Z (original List-architecture geometry check, single-event day): the default (no-preview) `.contextMenu`'s lift did NOT clip under List's `.listRowInsets`(16)+internal-padding(16) geometry — the exact geometry every flattened row now has, since every row is now structurally a 'single-event day card,' the case already validated clean."
  falsification_test: "Fresh on-device video-frame capture (same xcrun simctl io recordVideo + AVFoundation extraction method used throughout this session) after implementing the flattened List AND reverting EventContextMenu.swift/EventRowButton.swift to the plain default `.contextMenu(menuItems:)`: (1) long-press the LOWER of 2 same-day events (`sameday` fixture) and tap Duplicate/Delete — if the WRONG (upper) event is still acted on, the routing hypothesis is refuted and the multi-sibling condition was not the true mechanism. (2) Fine-grained (33ms-step) frame sampling of the touch-down-to-settled transition on a multi-event day (`paginationstress`) — if any sibling row (not the pressed one) still visibly elevates/flashes, or if the pressed row's own trailing content is clipped past its card edge, the geometry/ancestor hypothesis is refuted and Option A's custom-preview+clipShape machinery must be restored on top of the flattened architecture instead of reverted."
  fix_rationale: "Addresses the CONFIRMED root cause directly (see Resolution.root_cause below) rather than working around it: removes the shared-List-row condition itself, instead of adding more machinery (a full UIViewRepresentable-wrapped UIContextMenuInteraction, option (a) from the prior checkpoint) to compensate for it. Per the user's explicit decision (this turn's checkpoint response), trades the day-card's single shared Liquid Glass ancestor for N independent per-row glass surfaces with manual first/last-in-run corner-rounding (`HistoryRowGroupPosition`) — a user-accepted visual tradeoff, not a silent compromise. Reverting the custom-preview/PreferenceKey/clipShape code is NOT assumed safe purely from this reasoning — it is treated as a hypothesis to be falsified by the SAME rigorous video method that caught the highlight-phase and clipping defects earlier in this session, before being accepted."
  blind_spots: "`HistoryCalendarDayDetail`'s day-detail panel (plain VStack-in-ScrollView, not List-row-backed at all) is explicitly OUT OF SCOPE of this turn's checkpoint response (scope is `HistoryListQueryView.swift` only) — left unchanged, still an open, untested blind spot per `reasoning_checkpoint_reopened_20260805`. Independent per-row `.glassEffect()` surfaces (vs. one shared surface) may render with subtle per-surface highlight/reflection seams under certain lighting/appearance conditions that a static screenshot comparison might miss — `GlassEffectContainer` cannot be used to merge them into one true rendering surface because List rows are independent cells or GlassEffectContainer cannot span List-row boundaries; this is an accepted, user-specified tradeoff (checkpoint response item 2), not a defect to chase further unless video/live testing surfaces it as visible. Not tested across Dynamic Type sizes or other device sizes (carried-forward limitation from every prior pass in this file)."
  candidate_causes:
    - "config/architecture: `HistoryDaySectionCard`'s day-level shared List row (ALL of a day's events as nested sibling subviews of ONE row) — the architectural condition exposing the framework limitation; REMOVED by this fix, not merely worked around"
    - "code/framework: SwiftUI's `.contextMenu` interaction-routing/ancestor-scoping limitation for multiple sibling subviews of one shared List row — a framework limitation that cannot be changed, but this fix removes ITS OWN PRECONDITION (the shared row) rather than attempting to patch the framework behavior"
  and_gate: "No — same single interacting pair already established in the merged root_cause (framework routing/scoping limitation + shared-row architecture exposing it). This fix targets the architecture side of the pair directly (option (b) from the prior checkpoint), which the user selected specifically because it resolves the pair at its root for BOTH merged symptoms simultaneously, rather than (a)'s alternative of adding a custom `UIContextMenuInteraction` bridge that would have left the shared-row architecture (and its framework-limitation exposure) in place."

## Superseded Current Focus (2026-08-05T19:25:00Z, priority-override finding — wrong-row-target bug confirmed, root cause MERGED with the still-open highlight-phase decision; both now resolved by the Option-B fix above, retained verbatim for history)
<!-- Retained verbatim below for history; the merge/RCA reasoning here is still correct and is what the Option-B fix above directly acts on. -->

hypothesis: "PRIORITY-OVERRIDE FINDING (2026-08-05T19:25:00Z): a NEW, higher-severity live user report ('duplicate the LOWER of 2 same-day events → duplicates the TOP one instead') was investigated and root-caused BEFORE returning to the still-open highlight-phase A2/B/C decision (preserved below as 'Superseded Current Focus (2026-08-05T15:30:00Z)'). CONFIRMED via `git stash` baseline comparison: reproduces IDENTICALLY on the pre-session baseline (`EventContextMenu.swift`/`EventRowButton.swift` restored to last-committed HEAD, before ANY of this session's Option-A changes) — NOT a regression introduced by this session's work. CONFIRMED via direct on-device `os.Logger` instrumentation (temporary, removed after use) comparing menu-content BUILD time vs action EXEC time: each row's `.contextMenu` content closure IS built with its own correct `event` (both rows log their own correct category/uuid at build time) — ruling out a Swift-level closure-capture bug. But the interaction that actually fires when the NON-FIRST row in a day is long-pressed always invokes the FIRST-registered `.contextMenu` for that shared List row (here: the newest/topmost event, first in `ForEach(events)` order), regardless of which row's bounds were actually touched — confirmed via the log trace showing `performDuplicate EXEC` firing for the TOP (wine) event's `uuid` even though the BOTTOM (beer) row was pressed and its own `Duplicate` label was tapped. This is a documented, external, framework-level SwiftUI/List limitation (WebSearch corroboration, multiple independent Apple Developer Forums threads): '.contextMenu... is applied to the entire row rather than just to the specific view that defines it' when a List row hosts multiple `.contextMenu`-declaring sibling subviews. Root mechanism: `HistoryDaySectionCard` renders as ONE List row per day (Option 2's flat-unary-row fix for FB11280425, 2026-08-03), and every event within that day is a separate `.contextMenu`-bearing `EventRowButton` nested inside that ONE shared row — the EXACT SAME structural condition already identified in `reasoning_checkpoint_reopened_20260805` as the cause of the still-open highlight-phase crossfade, now proven to ALSO break which event a menu ACTION targets (confirmed independently for both Duplicate and Delete — Delete is HIGHER severity, since it is irreversible destruction of the wrong health record). THE TWO PREVIOUSLY-SEPARATE OPEN THREADS ARE HEREBY MERGED: they share one root cause, and Option A (custom `preview:` alone, already implemented and confirmed working ONLY for the settled-preview's own rendering/clipping — see the superseded block below) is now proven structurally incapable of fixing either the highlight-phase crossfade OR this wrong-target routing, because both are downstream of the SAME UIKit interaction-ROUTING limitation, not a preview-rendering issue `preview:` can address."
confirming_evidence:
  - "`git stash push` on `EventContextMenu.swift`+`EventRowButton.swift` (isolating this session's Option-A diff), then running a fresh XCUITest against the resulting pre-session-baseline build: IDENTICAL failure (long-press the BOTTOM/beer row of a 2-distinguishable-event day → seeded `750 ml` wine count becomes 2, `330 ml` beer count stays 1) as on the current Option-A tree. `git stash pop` restored the working tree cleanly. Direct proof this predates any of this session's context-menu changes."
  - "Direct on-device `os.Logger` trace (subsystem `com.drinkpulse.app`, category `EventContextMenu`, temporary instrumentation added and removed this turn), captured via `xcrun simctl spawn <udid> log stream` around a scripted long-press-BOTTOM-then-tap-Duplicate: at BUILD time both rows correctly log their own event (`category=wine uuid8=43AA4B32` first, `category=beer uuid8=F8A987AC` second, matching `ForEach(events)` iteration order). At EXEC time (after long-pressing the BEER row and tapping its own on-screen 'Duplicate' label), `performDuplicate EXEC` fires for `category=wine uuid8=43AA4B32` — the FIRST-registered event, not the pressed one. Rules out closure-capture/stale-@State as the mechanism; pinpoints the defect at UIKit's `UIContextMenuInteraction` routing layer, below SwiftUI's own view-body evaluation."
  - "WebSearch (this turn): multiple independent Apple Developer Forums threads (incl. one directly reporting 'SwiftUI List rows with multiple buttons trigger all buttons', 651572) and third-party analysis converge on the same documented limitation: 'the context menu is applied to the entire row rather than just to the specific view that defines it' when multiple `.contextMenu`s exist inside one shared List row. Documented current workarounds are exactly the two directions already on the table for the highlight-phase decision: drop `List` for `ScrollView`+`LazyVStack` (would reintroduce the ALREADY-FIXED FB11280425 cold-start hitch per this file's own 2026-08-03T16:20:00Z research — not viable as-is), or a custom UIKit-bridged `UIContextMenuInteraction` per row (e.g. the community `ViewBoundContextMenu` package's approach)."
  - "New dedicated `-dp_uitest_dataset sameday` fixture (`UITestSeed+Fixtures.swift`) seeds exactly 2 clearly DISTINGUISHABLE events for today (330 ml beer 08:00, 750 ml wine 20:00) specifically because the pre-existing `paginationstress` fixture's 3 IDENTICAL same-day events are blind to a wrong-TARGET bug — a wrong-row duplicate/delete among identical siblings changes nothing a plain count assertion can detect. A permanent regression test (`drinkpulseUITests/Features/History/WrongRowContextMenuTargetUITests.swift`) pins BOTH the Duplicate case and an independently-confirmed Delete case (long-press bottom row → Delete → confirm → the TOP/wine row is removed instead, the BOTTOM/beer row survives) via `XCTExpectFailure` (keeps the suite green while the bug is tracked; will fail loudly — 'unexpected pass' — if the underlying routing is ever silently fixed without updating the test)."
falsification_test: "If the wrong-row targeting were a Swift-level closure-capture bug (stale reference, shared `@State`, etc.), the `os.Logger` BUILD-time trace would have shown the WRONG event already captured at menuItems-closure-build time for one of the two rows — it did not; both rows built correctly. If the bug depended on this session's Option-A changes, it would NOT reproduce on the `git stash`-restored pre-session baseline — it did, identically. Both alternative hypotheses are directly falsified by this evidence, leaving SwiftUI/UIKit's per-List-row `.contextMenu` interaction-routing scope as the confirmed mechanism, corroborated by independent external reports of the same documented framework limitation."
fix_rationale: "NOT YET APPLIED. Once confirmed as an interaction-ROUTING limitation (not a rendering/preview issue), no safe, minimal, non-architectural code fix exists — the SAME two directions already on the table for the highlight-phase decision are the ONLY viable fixes, since both bugs now share one root cause: (a) restructure `List` to one row per event (trading away `HistoryDaySectionCard`'s shared day-card visual grouping, a user-visible design change), or (b) a full custom `UIViewRepresentable`-wrapped `UIContextMenuInteraction` per row (preserves the current visual design, adds substantial new UIKit-interop code and edge cases). CLAUDE.md's plan-driven-development rule ('for any new feature or significant refactor, create a plan... before writing code') and this exact debug session's own established precedent (every architecture-level fork in this file — Options 1/2/3/4 on 2026-08-03T16:40:00Z, the A2/B/C highlight-phase fork on 2026-08-05T14:45:00Z — has gone back to the user as a decision checkpoint, never picked unilaterally) both apply here, now with materially higher stakes (silent health-data mutation on the wrong record, not a cosmetic glitch). Implementing either fix unilaterally inside this turn, especially given this exact codebase has already been burned once by a rushed, insufficiently-verified contextMenu implementation (2026-08-03T16:00:00Z 'two layers' live rejection), was judged too risky without sign-off. Returning a merged DECISION checkpoint instead of a self-verified fix."
blind_spots: "Not tested on `HistoryCalendarDayDetail`'s day-detail panel (plain VStack-in-ScrollView, NOT a List row) — since that panel is not List-cell-backed, it may or may not share this exact routing defect; worth checking once a fix direction is chosen, out of scope of the reported reproduction (History tab, list segment). Not tested with 3+ events on the same day (only the 2-event `sameday` fixture was used) — the 'always resolves to the first `ForEach` element' rule is the working theory (matching the external reports and the single tested scenario) but not exhaustively proven across more permutations; this does not change the fix decision needed regardless of the exact resolution rule, so not chased further before this checkpoint. Have NOT directly tested whether the VoiceOver `.accessibilityActions` path (a separate, independent code path per finding C14-2) exhibits the same defect — it very likely does NOT, since accessibility actions are wired directly through SwiftUI's own per-element accessibility tree, not through `UIContextMenuInteraction`'s List-row-level routing, but this is inferred from the mechanism, not independently confirmed via VoiceOver testing this turn."
candidate_causes:
  - "code/framework: SwiftUI's `.contextMenu(menuItems:)`/`.contextMenu(menuItems:preview:)` interaction-routing does not scope correctly to individual sibling subviews declared inside one shared `List` row — confirmed via direct instrumentation AND independent external corroboration (Apple Developer Forums); a SwiftUI/UIKit framework-level limitation, not something app code controls without bypassing the API entirely."
  - "config/architecture: `HistoryDaySectionCard`'s List-row shape (ONE row per day, hosting ALL of that day's `EventRowButton`s as nested sibling subviews, each independently declaring its own `.contextMenu`) is what exposes the framework limitation — the IDENTICAL architectural condition already identified as the root cause of the still-open highlight-phase A2/B/C decision (`reasoning_checkpoint_reopened_20260805`), now proven to also break action targeting, not just preview rendering."
and_gate: "No — a single interacting pair (the framework's per-List-row `.contextMenu` scoping limitation + the day-level shared-row architecture that exposes it), the SAME underlying mechanism already established for the highlight-phase defect in `reasoning_checkpoint_reopened_20260805`. This turn's finding MERGES the two previously-separate open threads into one combined architecture decision: any fix for either candidate direction (one-row-per-event List restructure, OR custom `UIContextMenuInteraction` bridge) fixes BOTH the wrong-target bug and the highlight-phase crossfade simultaneously, since they are two symptoms of one cause. Option A (custom `preview:` alone) is now proven structurally incapable of fixing either — it only ever addressed the settled-preview's own rendering/clipping, a real but comparatively minor sub-symptom that remains fixed and unaffected by this finding."

## Superseded Current Focus (2026-08-05T15:30:00Z, settled-preview clipping sub-fix — CONFIRMED WORKING, retained for history, unaffected by the merged-decision finding above)
<!-- Retained verbatim below for history; the clipShape fix described here is still correct and still in the working tree. -->

hypothesis_clipfix: "Root cause of the user-reported 'settled preview clipped on the right side': `EventRow` has no internal horizontal padding of its own (relies entirely on an external wrapper for clearance); the custom `preview:` closure reproduces the row's exact on-screen width (`previewWidth`) but not the day-card's external 16pt clearance, and iOS wraps ANY custom preview view in system-controlled rounded chrome whose corner radius is large relative to this short single-row card's height — clipping trailing content that sits flush at the frame's edge. Fix: give the preview view its OWN explicit, small, KNOWN `.clipShape(.rect(cornerRadius: 10))` (replacing reliance on the opaque system default, per Apple DTS forum thread 784012's documented workaround for exactly this — a plain geometric clip, not a `.background`/material addition, so it should not reproduce the earlier double-chrome ghosting bug) and pad the content ≥10pt on every side (matching the radius) — a rounded-rect corner of radius R geometrically cannot intrude into content padded ≥R from every edge, so this is a guaranteed fix, not an empirical guess against an unknown system value."
confirming_evidence:
  - "Evidence 2026-08-05T15:15:00Z: direct pixel-level inspection (not visual impression) on BOTH a single-event-day and a multi-event-day fresh capture shows dark text pixels ('2.0'/'1.3' amount, 'std' unit label) extending past the white preview card's own measured right boundary at multiple sampled rows — genuine clipping, identical on both fixtures, confirming this is a preview-geometry defect independent of sibling-row count / day-card architecture."
  - "Direct source read of `EventRow.swift`: confirmed zero internal horizontal padding — the trailing amount `VStack` is pushed flush to the row's own right edge by a bare `Spacer()`, with no leading/trailing padding modifier anywhere in the view."
  - "Apple Developer Forums thread 784012 (DTS-answered, WebSearch this turn): confirms `.contentShape(.contextMenuPreview:)` cannot shape the FINAL/settled custom preview (only the lift/highlight one — already established in this file's Eliminated section for a different scope), and that DTS's own recommended fix for shaping the settled custom preview is `.clipShape(...)` applied to the `preview:` view itself."
falsification_test: "Fresh on-device video-frame capture (same method) after the fix, on BOTH the single-event default fixture and the multi-event `paginationstress` fixture, pixel-inspecting the settled preview's trailing edge exactly as in Evidence 2026-08-05T15:15:00Z: if any dark text pixel is found beyond the (now explicitly clipShape-bounded) card's own edge on either fixture, the fix is refuted and the padding/radius values need revision."
fix_rationale: "Addresses the root cause (system-controlled, opaque, height-disproportionate corner rounding clipping flush trailing content) directly by replacing reliance on that opaque default with an explicit, known shape + matching padding — not a cosmetic patch. Deliberately reuses the plain `.clipShape` mechanism (no new background/fill) specifically because the earlier 'two layers' ghosting bug (2026-08-03T16:00:00Z) was traced to a stacked `.background`/`.glassEffect` surface, a different mechanism this fix does not touch."
blind_spots: "Not yet verified across Dynamic Type sizes (a larger type size increases EventRow's intrinsic height, which changes the preview card's height-to-radius ratio — plausibly REDUCES the clipping risk since more flat edge becomes available, but not confirmed). Not yet re-verified against `HistoryCalendarDayDetail`'s day-detail panel (shares the same modifier, so covered structurally, but not independently captured on video this turn). Chosen radius/padding (10pt, matching the pre-existing `.padding(.vertical, 10)` constant already in the code) is a principled but not exhaustively pixel-measured minimum — verified empirically via fresh video below rather than by exact corner-radius arithmetic against the unknown system default."
candidate_causes:
  - "code: `EventRow`'s trailing content has no internal margin of its own (relies on an external wrapper for clearance) — a layout assumption that broke when the view was reused in a context (custom preview) with no such external wrapper"
  - "framework: iOS/SwiftUI's custom `contextMenu` `preview:` view is wrapped in system-controlled rounded chrome with an undocumented corner radius that is disproportionately large for a short single-row card — not something the app's own code controls unless explicitly overridden"
and_gate: "No — a single interacting pair (EventRow's zero-margin trailing layout + the system's undocumented, disproportionately-large default preview corner radius) fully explains the symptom; fixing either side (give EventRow its own margin, OR take over the shape explicitly) independently resolves it. Chose to fix at the preview-wrapper level (clipShape + padding in `EventContextMenuModifier`) rather than changing `EventRow` itself, since `EventRow` is also used in the resting row and the calendar day-detail list where the existing external-padding pattern already works correctly — changing `EventRow` itself would risk regressing those call sites for a defect that's specific to the preview context."

## Superseded Current Focus (2026-08-05T14:45:00Z, highlight-phase A2/B/C decision — STILL OPEN, unaffected by the clipping sub-fix above)
<!-- Retained verbatim below for history; still the open decision once the clipping sub-fix above is verified. -->

hypothesis_priorA: "IMPLEMENTING OPTION A (user decision, 2026-08-05T14:00:00Z checkpoint response): custom `.contextMenu(menuItems:preview:)` scoped to exactly the pressed `EventRowButton`'s own bounds, applied inside `EventContextMenuModifier` (shared by both History List and Calendar Day Detail via `EventRowButton`/`eventContextMenu`), reusing the already-fixed `EventRowWidthPreferenceKey.reduce` non-default-wins mechanism. Day-card `.dpGlassCard()` grouping in `HistoryDaySectionCard`/`HistoryCalendarView` is left untouched."
test: "Fresh on-device video-frame capture (xcrun simctl io recordVideo + AVFoundation extraction) on the `paginationstress` fixture (3 same-day events), long-pressing the first row. Sample the settled hold AND, at high temporal density (~every captured frame, not coarse steps), the touch-down-to-menu-open transition window specifically, since the 2026-08-03T16:00:00Z 'two layers of the same thing' rejection may have been a brief lift-to-final-preview crossfade that coarse (100-300ms) sampling missed."
expecting: "If Option A's hypothesis is correct: only the pressed row (12:02) visually separates/elevates inside its own preview card; sibling rows (12:01, 12:00) and the day-card glass background stay in their resting position/dimmed. No sibling-row movement, no double-image/ghosting at any sampled frame including the fast transition window. If incorrect: siblings still move together with the pressed row (falsifies the 'custom preview bypasses ancestor scope' theory), or a brief double-image appears during the lift-to-final transition (would explain the prior live rejection and require a different fix, e.g. matching preview position/size even more tightly to the resting row, or investigating UIKit's separate highlight-preview API)."
next_action: "CHECKPOINT REACHED (decision) returned this turn (2026-08-05T14:45:00Z) — a second, independent SwiftUI-API-level limitation was found (transient highlight-phase snapshot cannot be scoped via any pure-SwiftUI contextMenu API when a shared multi-row glass-card ancestor exists) that neither of the two things tried so far (custom `preview:` alone; custom `preview:` + `.contentShape(.contextMenuPreview:)`) can fix, and both remaining fix directions (custom UIViewRepresentable/UIContextMenuInteraction bridge, or restructuring HistoryDaySectionCard to per-row-shaped backgrounds) expand scope beyond the 2026-08-05T14:00:00Z checkpoint's Option A agreement. Awaiting user decision — see this turn's CHECKPOINT REACHED response. Working tree currently holds Option A's settled-state fix (EventContextMenu.swift, EventRowButton.swift) with the ineffective `.contentShape` addition already reverted; nothing has been committed. Once the user decides, next agent turn implements the chosen direction and MUST re-run the SAME fine-grained (0.033s-step) transition-window video method used in Evidence 2026-08-05T14:35:00Z/14:45:00Z (not just the settled-state check) before any human-verify checkpoint."
reasoning_checkpoint:
  hypothesis: "Supplying an explicit `.contextMenu(menuItems:preview:)` `preview:` closure to `EventContextMenuModifier` (a fresh `EventRow` instance sized to the pressed row's own measured width, no extra background of its own) replaces whatever ancestor snapshot/compositing scope the DEFAULT (no-preview) `.contextMenu` uses (per Evidence 2026-08-05T13:35:00Z, the whole `.dpGlassCard()`-wrapped day VStack), because supplying `preview:` hands UIKit's `UIContextMenuInteraction` a wholly separate view to snapshot/animate to, disconnected from the glass-card's rendering tree — this bypasses the ancestor-search/compositing mechanism regardless of its exact underlying cause (ancestor-shape inference vs. Liquid Glass container compositing; both remain unconfirmed at the UIKit-internals level, but a custom preview sidesteps either)."
  confirming_evidence:
    - "Evidence 2026-08-03T15:45:00Z: after the PreferenceKey reduce bug was fixed, this SAME mechanism (custom preview + measured width), tested via 34-frame on-device video on a SINGLE-event day, showed a clean lift/hold/dismiss with only a negligible cosmetic sliver — direct prior proof this mechanism, once the width-measurement bug is fixed, produces a correctly-scoped, non-clipped preview for at least the single-event case."
    - "Apple Developer Documentation + forum research (this turn, WebSearch): 'SwiftUI renders the [custom] preview inside a rounded rectangle and places it next to the menu' — confirming the custom preview is UIKit's own independently-managed snapshot/chrome, not derived from walking the source view's ancestor tree; this is the documented purpose of the `preview:` parameter (introduced iOS 16) for cases where the default source-view-derived lift is undesirable."
    - "Apple Developer Forums thread 784012 (DTS-answered): confirms `.contentShape(.contextMenuPreview, shape)` only affects the DEFAULT (source-view-derived) lift preview's mask, never the custom `preview:` view's own shape — corroborating the Eliminated-section finding in this file (2026-08-03T15:26:00Z) that this modifier is the wrong lever, and confirming the custom `preview:` closure is a genuinely separate rendering path from the default lift."
  falsification_test: "On-device video-frame capture of a long-press on the FIRST of 3+ same-day events after Option A is applied: if any sibling row (not the pressed one) visibly elevates/moves together with the pressed row, OR the day's whole glass card visibly lifts, the hypothesis is refuted. Because the 2026-08-03T16:00:00Z rejection ('two layers of the same thing') raises a real risk that frame-sampling at coarse (100-300ms) resolution misses a brief lift-to-final-preview crossfade, this verification pass will ALSO sample at high temporal density (every available captured frame, ~16-33ms apart) specifically across the touch-down-to-menu-open transition window, not just the settled hold state — since that transition is the single most likely place a brief double-image would appear and be missed by coarse sampling."
  fix_rationale: "Addresses the root cause directly (default contextMenu infers/composites its lift preview from an ancestor/rendering-tree scope wider than the pressed row) by removing the system's need to infer anything — an explicit preview view IS the exact, sole scope, with no ancestor to accidentally include. It does not touch `HistoryDaySectionCard`'s or `HistoryCalendarView`'s day-grouped glass-card layout (an intentional design element, kept unchanged per Option A's scope), so this is a mechanism-level fix, not a layout compromise: the day card's shared visual grouping is preserved for normal browsing, and only the long-press lift/preview snapshot is redirected to the single row. Deliberately gives the custom preview view NO background/shape of its own — iOS/UIKit already wraps a supplied `preview:` view in its own rounded card chrome (confirmed via WebSearch above), and adding a second background would double up with that system chrome, a plausible contributing cause of the 'two layers' artifact in the earlier (less careful) attempt."
  blind_spots: "The 2026-08-03T16:00:00Z 'two layers' rejection's exact cause was never conclusively re-derived (ambiguous from the raw Evidence timestamps whether the rejected build was pre- or post- the 15:45 reduce-key fix; reasoning_checkpoint_reopened_20260805 asserts pre-fix without a directly-cited re-derivation) — this fix's no-extra-background design is a defensive mitigation for a plausible but not fully confirmed secondary cause, not a proven fix for a confirmed second root cause. Not yet re-verified with its own video capture against `HistoryCalendarDayDetail`'s day-detail panel (shares the same `EventContextMenuModifier`/fix automatically, but the reproduction steps in Symptoms.reproduction only name the History List) — flagged as a residual scope gap for a possible follow-up check, not blocking this fix given the shared modifier already covers it structurally. Not tested across Dynamic Type sizes or other device sizes (same carried-forward limitation as every prior pass in this file)."
  candidate_causes:
    - "code: default `.contextMenu(menuItems:)` (no `preview:`) — root cause, addressed directly by this fix"
    - "config/architecture: `HistoryDaySectionCard`'s/`HistoryCalendarView`'s day-level shared `.dpGlassCard()` grouping — NOT changed by this fix (Option A explicitly preserves it); the fix works around it rather than removing it"
  and_gate: "No — single mechanism (default lift/preview inference or compositing scope), same as already established in reasoning_checkpoint_reopened_20260805. This turn's fix targets the code-level cause without touching the architecture-level cause, since either independently resolves the symptom and the user explicitly chose the option (A) that preserves the day-card architecture."

## Superseded Current Focus (2026-08-05T13:40:00Z, pre-Option-A-decision)
<!-- Retained verbatim below for history; no longer the active focus. -->

hypothesis_prior: "REOPENED (2026-08-05T13:40:00Z). The 2026-08-03T18:20:00Z 'RESOLVED' status was NEVER human-confirmed (the awaiting_human_verify checkpoint from that timestamp was never answered) and is now DIRECTLY CONTRADICTED by fresh on-device evidence: its own self-verification video (Evidence 2026-08-03T18:00:00Z) only sampled a day with a SINGLE event, which is structurally blind to this bug (see below). Live user testing on 2026-08-05 on a day with 2 events reproduced a zoom-glitch again, described as 'when I long-press an entry, it zooms up together with the header, and if there were 2 entries on some day, it zooms up both entries together with the header.' CONFIRMED via fresh on-device video-frame inspection (Evidence 2026-08-05T13:35:00Z): SwiftUI's default `.contextMenu` (no custom `preview:`) infers its lift/snapshot bounds by walking up to the NEAREST ANCESTOR VIEW WITH A DISTINCT BACKGROUND/SHAPE — here, `.dpGlassCard()` (`.glassEffect(.regular, in: .rect(cornerRadius: 24))`), applied in `HistoryDaySectionCard` to the `VStack` wrapping ALL of that day's events, not per-event. Long-pressing ONE `EventRowButton` (nested inside that shared glass-card VStack) lifts the ENTIRE glass card — every event row for that day — as one unit, while the day's 'TODAY'/'YESTERDAY' section-title `Text` (which sits OUTSIDE `dpGlassCard()`, in the same outer VStack but with no shared background) stays in its resting position, undimmed. With exactly one event per day (what the 2026-08-03T18:00:00Z self-verification happened to sample), the glass-card boundary == the single event's own bounds, so it is visually indistinguishable from 'just the row lifting' — the List-architecture change (Option 2, `List { ForEach(dayGroups) { HistoryDaySectionCard } }`) did NOT fix the original bug's mechanism; it only changed which geometry variant reproduces it, and the single-event self-check happened to land in the variant where the defect is invisible."
test: "On-device screen recording (`xcrun simctl io recordVideo` + a freshly-rebuilt AVFoundation frame-extraction tool, since the prior session's throwaway tool was deleted) of a throwaway XCUITest (`ZoomGlitchMultiEventProbeUITests`, deleted after use) driving a 3.5s long-press on the FIRST of 3 same-day events (`-dp_uitest_dataset paginationstress` fixture, which seeds 3 events for 'today'). Inspected frames across the full hold (t=33.5s-38.0s in the capture, sampled every 0.3s) and the dismiss (t=39.0s-42.3s)."
expecting: "If the row-vs-subview hypothesis is correct: all 3 'Can 330 ml' rows for 'TODAY' lift together as one elevated white card while 'TODAY' stays put. If incorrect (e.g. List's own row-inset machinery really does scope the lift to just the interacted element): only the pressed row (12:02) would visually elevate/separate from its two siblings (12:01, 12:00), which would stay flat/dimmed with the background."
next_action: "CHECKPOINT REACHED (decision) returned this turn — architecture-fork decision needs the user's input before implementing any fix. See checkpoint content in this turn's response. Once the user decides, next agent turn implements the chosen option, then must re-run this SAME multi-event-day video-frame verification (not just a single-event sanity check) before requesting human-verify again — the single-event blind spot is now a known, named failure mode for this bug class and must not repeat."
reasoning_checkpoint:
  hypothesis: "History rows in `HistoryDaySectionCard`/`HistoryCalendarDayDetail` already span nearly the full screen width (~338pt content width against a ~402pt screen, ~32pt margin per side from two stacked 16pt paddings). SwiftUI's default `.contextMenu(menuItems:)` (no custom `preview:`) lifts the *source* view in place using a system-controlled scale factor empirically ~1.19x. 338 * 1.19 ≈ 402 — almost exactly the screen width — so the lift scale-up has no headroom and pushes the row's own leading icon and trailing value text past the screen edges, clipping them for the entire time the menu is open. This is what the user perceived as 'zoom looks weird' and 'row weirdly returns to normal on dismiss' (the same overflowed geometry made the pre-existing system dismiss transition look broken too, since it was un-clipping from an already-wrong state)."
  confirming_evidence:
    - "On-device screen recording (xcrun simctl io recordVideo) of the unmodified app, frames extracted via AVFoundation and inspected directly: during the long-press hold (~0.5s-2s after touch-down, well before release), the row's trailing '2.0 / std' text and leading beer-mug icon are visibly cut off at the screen's right and left edges — confirmed across multiple frames spanning the whole hold duration, not a single-frame fluke."
    - "Geometric calculation independently derived the same threshold before observing the clipping: screen width 402pt, content width 338pt (402 - 2*(16+16)), overflow starts above a 1.19x scale factor — matching what was visually observed."
    - "The dismiss transition, examined frame-by-frame at 33ms resolution, showed no comparable defect once isolated from the lift-state clipping — confirming the SAME root cause (edge-to-edge row width vs. default lift scale) explains both complaints, rather than two separate bugs."
  falsification_test: "If a row that does NOT span near-full screen width (e.g. a narrower test row) still glitched under the same long-press, the hypothesis would be refuted (would point to something glass/LazyVStack-related instead, as originally hypothesized before investigation). Not applicable here since `.contextMenu` is used nowhere else in the app to compare against — but the video evidence is a direct visual confirmation of the overflow mechanism itself (content pixels literally missing past the screen edge), which is strong, unambiguous, first-hand evidence, not an inference from absence."
  fix_rationale: "Root cause is 'the system's default contextMenu lift scale-transform has no headroom against an edge-to-edge-width row.' A custom `.contextMenu(menuItems:preview:)` sidesteps the scale-transform entirely: instead of the OS scaling the live source view (which has no headroom), a fresh, independently-sized preview view is presented, explicitly matching the row's own on-screen width instead of trying to grow from it. This addresses the root cause (missing scale headroom) rather than a symptom-level patch (e.g. adding arbitrary padding would only mask the geometry, tuned to one device/Dynamic-Type size, not fix the underlying default-preview/edge-to-edge-width mismatch)."
  blind_spots: "Not tested on other iPhone screen sizes (iPhone SE / Pro Max) or with larger Dynamic Type sizes, which change the row's actual measured width — though the fix's width-measurement is dynamic (GeometryReader-based, not hardcoded), so it should self-adapt; not independently re-verified with video on those configurations. Not tested with VoiceOver / accessibility interaction paths (Accessibility rotor 'Actions' exposes Duplicate/Delete via a different UIKit path that doesn't use the same lift/preview mechanism, so it's out of scope for this specific bug). A residual ~1-3pt/very-low-opacity sliver of the dimmed underlying row is still faintly visible at the trailing edge during the hold in the fixed version (see Resolution.verification) — cosmetically far better than the original (which showed full duplicate content and hard text clipping) but not 100% pixel-perfect; root cause of that residual sliver not further chased given its severity is negligible."
  candidate_causes:
    - "code: default `.contextMenu(menuItems:)` (no `preview:`) lift-scale mechanism has no configurable API to change/disable the scale factor for the default (non-custom-preview) presentation path"
    - "config: History row layout (two stacked 16pt horizontal paddings from `HistoryListQueryView` + `HistoryDaySectionCard`) leaves the row content only ~338pt wide against a ~402pt screen — a layout/spacing decision, not a code defect, that happens to interact badly with the OS's default scale factor"
  and_gate: "No — a single cause (default-preview scale-up + edge-to-edge width leaves no headroom) fully explains both reported symptoms (lift clipping, dismiss looking wrong). Did not need to combine code + config; config (the layout being edge-to-edge) is what exposes the code behavior (default lift scale) as a bug — but fixing either alone (giving up screen-edge-to-edge width, OR changing the lift mechanism) resolves it; chose the mechanism-level fix (custom preview) since it doesn't require any visible layout/design change to the row's resting appearance."
tdd_checkpoint: null
reasoning_checkpoint_option2:
  hypothesis: "Option 2 (`List { ForEach(dayGroups) { HistoryDaySectionCard(day) } }`, no `Section`, no nested per-event `ForEach` at the List-visible level) restores genuine List row-level laziness (fixing 2e0fd4f's confirmed cold-start hitch's precise mechanism, FB11280425, WITHOUT reintroducing it) while ALSO changing each row's default List-managed insets/width in a way that may independently resolve (or at least alter) the original edge-to-edge-width-vs-default-lift-scale geometry that caused the reported long-press zoom glitch — because List applies its own row insets (here explicitly set to 16pt leading/trailing via `.listRowInsets`) INSTEAD OF the LazyVStack version's manually-applied outer `.padding(.horizontal, 16)` on the whole stack, which is architecturally the same numeric margin but a materially different layout mechanism (List-managed safe-area-aware row inset vs. a plain view modifier), so it must be re-measured, not assumed equivalent."
  confirming_evidence:
    - "Code-level: `.listRowInsets(EdgeInsets(top: 8, leading: 16, bottom: 8, trailing: 16))` explicitly sets the same 16pt horizontal margin measured in the original geometry investigation (Evidence 2026-08-03T15:12:00Z), so the row content width should compute to the same ~338pt on the reference device/Dynamic Type size — a priori this predicts NO GEOMETRY CHANGE, but this is a prediction to be falsified by video, not an assumption to act on."
    - "swiftui-expert-skill + forum research (Evidence 2026-08-03T16:20:00Z, 16:30:00Z) confirms List's row-level laziness is restored by this flat-ForEach-unary-row shape without requiring any change to `HistoryDaySectionCard`'s internal layout — so this fix's SCOPE is deliberately narrow (swap outer container only), which is exactly why the original zoom-glitch geometry is expected, not guaranteed, to persist unchanged."
  falsification_test: "On-device video-frame inspection (xcrun simctl io recordVideo + AVFoundation frame extraction, same method as Evidence 2026-08-03T15:12:00Z) of a long-press on a List row under this new implementation. If the row's leading icon / trailing text are NOT clipped past the screen edges during the hold (i.e. content stays within bounds), the geometry hypothesis is falsified in the POSITIVE direction (List's own row-inset mechanism interacts differently with the system lift-scale than the LazyVStack+padding version did, e.g. because List provides its own safe-area/margin handling to the lift-preview snapshot that a plain ScrollView+VStack+padding version did not) — no further contextMenu-specific fix needed. If clipping IS still observed, the hypothesis is confirmed unchanged and a fix is still needed (default preview mechanism issue is separate from container choice; would need to revisit WITHOUT repeating the failed GeometryReader/PreferenceKey approach's ghosting bug, and this time must pass BOTH self-verification AND a human-verify checkpoint before being accepted, learning from the earlier rejected fix)."
  fix_rationale: "Not a fix for the zoom-glitch directly — Option 2 is a fix for the SUPERSEDING bug (List cold-start hitch vs. genuine laziness, from the 2026-08-03T16:00:00Z re-scope), chosen by the user's explicit decision. The zoom-glitch's fate under this new architecture is an open empirical question this checkpoint exists to answer BEFORE assuming either 'fixed for free' or 'still needs a fix' — acting on either assumption without the video check would repeat the exact mistake (self-verification without sufficient evidence) that got the previous fix rejected."
  blind_spots: "Only the List (history segment) row geometry is being re-verified. `HistoryCalendarDayDetail`'s rows (Calendar segment day-detail panel) use `.eventContextMenu` too, on an unchanged plain-VStack-in-ScrollView layout with the same 16pt padding — if the original geometry mechanism is real, those rows may exhibit the same glitch and were never in scope of the original bug report's reproduction steps ('History tab (list segment)'). Not chasing that unless verification surfaces it as user-visible in the reported flow. Not tested across Dynamic Type sizes or other device sizes (iPhone SE / Pro Max) — same limitation as the original investigation, carried forward, not newly introduced."
  candidate_causes:
    - "config: List's `.listRowInsets` mechanism vs. LazyVStack's outer `.padding` — different layout MACHINERY producing the same nominal 16pt margin, which may or may not interact identically with the system's default contextMenu lift-scale snapshot"
    - "code: default `.contextMenu(menuItems:)` lift-scale mechanism itself is unchanged from the original investigation (still no configurable scale-factor API) — if the geometry is unchanged, the original code-level cause still applies unmodified"
  and_gate: "No — this is a single empirical question (does the geometry still overflow under List) with two possible answers, not two causes needing to combine. Testing the container swap in isolation (no other change) is what makes this a clean single-variable falsification test."

next_action_2026_08_03T18_20: "SUPERSEDED (2026-08-05T13:40:00Z) — see Resolution and Evidence 2026-08-05T13:35:00Z. This entry's claim ('on-device video confirms the zoom-glitch does not reproduce under List') is FALSE for multi-event days; it was only verified against a single-event day and never human-confirmed. Left in place, uncorrected, as an honest historical record of the mistake (per debug-file Evidence append-only rule) — do not delete or edit this line."

reasoning_checkpoint_reopened_20260805:
  hypothesis: "SwiftUI's default `.contextMenu(menuItems:)` (no custom `preview:`) does not scope its lift/snapshot preview to the exact view the modifier is attached to (`EventRowButton`'s `Button`). Instead it walks up the view hierarchy and snapshots the nearest ancestor that has its own distinct background/shape — in this codebase, `.dpGlassCard()` (`.glassEffect(.regular, in: .rect(cornerRadius: 24))`), applied by `HistoryDaySectionCard` to the `VStack` containing ALL of that day's `EventRowButton`s, not per-event. So long-pressing any single event lifts the WHOLE day's glass card (every sibling event row) as one unit. The day-title `Text` (e.g. 'TODAY') is a sibling of that glass-card `VStack` inside `HistoryDaySectionCard`'s outer `VStack`, NOT a descendant of the glass-card-shaped ancestor, so it is excluded from the lift and stays in its resting position — this is why the user's translated report ('zooms up together with the header') is approximately, not literally, accurate: the ELEVATED WHITE CARD sits directly under the header text and reads as one visual unit to the user, even though the header `Text` itself does not move. This is a REFINEMENT of the original (2026-08-03T15:12:00Z) and Option-2 (2026-08-03T16:40:00Z-18:00:00Z) hypotheses, not a full reversal: both previous investigations correctly identified that the default `.contextMenu` lift mechanism was the code-level culprit; both incorrectly attributed the fix to the OUTER CONTAINER (ScrollView+LazyVStack vs. List) when the actual scoping boundary is the `.dpGlassCard()`/`.glassEffect` ancestor inside `HistoryDaySectionCard`, which is IDENTICAL in both container versions (neither the 2026-08-03T15:12:00Z original investigation's LazyVStack version nor Option 2's List version ever changed `HistoryDaySectionCard`'s internal structure) — so this defect predates AND survives the List/LazyVStack architecture swap entirely; it was only ever masked by testing exclusively on single-event days."
  confirming_evidence:
    - "On-device video-frame inspection (fresh capture, 2026-08-05, method: xcrun simctl io recordVideo + AVFoundation frame extraction), long-press on the FIRST of 3 same-day events: frames from t=33.5s through t=38.0s (0.3s steps, 16 consecutive frames, fully settled/steady-state, not a single-frame fluke) show ALL THREE 'Can, 330 ml' rows (12:02, 12:01, 12:00) elevated together as one white rounded card with a drop shadow, floating above a dimmed background, while the 'TODAY' section label above it remains at normal (non-dimmed, non-elevated) position and size throughout."
    - "Direct code read (2026-08-05) of `HistoryDaySectionCard.swift`: `.dpGlassCard()` is applied to `VStack(spacing: 0) { ForEach(events, id: \\.uuid) { event in EventRowButton(...) } }` — i.e. to the container of ALL events for the day, confirmed to be the SAME structure regardless of whether the outer List/LazyVStack architecture choice (Option 2 vs. the original ScrollView+LazyVStack) is in effect; `DPGlass.swift` confirms `dpGlassCard()` == `.glassEffect(.regular, in: .rect(cornerRadius: size.cornerRadius))`, a `.rect`-shaped background/clip that is exactly the kind of 'distinct ancestor shape' that would make a plausible auto-inferred contextMenu preview boundary."
    - "The dismiss transition (frames t=39.0s-42.3s, 0.3s steps) returns cleanly to 3 separate, correctly-positioned, non-dimmed, non-duplicated rows with no residual artifact — ruling out a transient animation-timing bug and confirming this is a steady-state geometry/scoping defect, matching the same 'clean dismiss, broken lift' pattern found in the original 2026-08-03 investigation."
  falsification_test: "If the lift preview were genuinely scoped to just the pressed `EventRowButton` (as Option 2's 2026-08-03T18:00:00Z self-verification concluded), only the pressed row would visually separate/elevate from its siblings, which would remain flat and dimmed with the rest of the background. The captured frames show the OPPOSITE: all three sibling rows elevate together as a single card. This directly and unambiguously falsifies Option 2's prior 'resolved' conclusion for the multi-event case."
  fix_rationale: "Not yet applied — this is a re-confirmation of root cause, not a fix. Two structurally different fix directions exist depending on whether the day-grouped glass card (visual grouping of all of a day's events under one Liquid Glass surface) is kept: (A) give `.contextMenu` an explicit custom `preview:` scoped to exactly the pressed `EventRowButton`'s own bounds (not the shared `.dpGlassCard()` ancestor) — reuses the already-fixed PreferenceKey width-measurement mechanism from Evidence 2026-08-03T15:45:00Z (the `reduce`-last-write-wins bug is already fixed and self-verified, but was NEVER re-shown to the user live — the user's 2026-08-03T16:00:00Z rejection was of the PRE-fix, ghosting version), this time explicitly targeting per-event bounds inside a multi-event card, so the previously-passing 34-frame self-verification needs to be redone against a multi-event day, not repeated against a single-event day; (B) reopen the 2026-08-03T16:40:00Z architecture fork's Options 1/3 (flat per-event List rows, dropping or faking the day-card grouping) so each List row IS exactly one event and the shared-ancestor-lift mechanism has no multi-event surface to span in the first place. Both are legitimate; the choice trades off visual design (day-card grouping) against implementation risk (custom preview was rejected once already, on since-fixed grounds) and native swipeActions availability (only Option B restores it). This is exactly the kind of decision CLAUDE.md's 'What to ask before assuming' section and the debug-agent's checkpoint protocol says should go back to the user rather than be silently picked."
  blind_spots: "Not yet re-tested against `HistoryCalendarDayDetail`'s day-detail panel rows (plain VStack-in-ScrollView, same `.eventContextMenu` reuse via `EventRowButton`) — if that panel also groups multiple events without per-event background shapes, the same mechanism may apply there too, out of scope of the current reproduction steps but worth checking once a fix direction is chosen. Not tested on other iPhone screen sizes/Dynamic Type. Not yet determined whether the mechanism is literally 'nearest ancestor with `.glassEffect`' vs. some other UIKit heuristic (e.g. nearest ancestor with ANY `.background`/`.clipShape`, not glass specifically) — the codebase happens to only use `.dpGlassCard()` here so the two are indistinguishable from this evidence alone; not load-bearing for the fix decision (both candidate fixes work regardless of the exact heuristic), so not chased further before the checkpoint."
  candidate_causes:
    - "code: SwiftUI's default `.contextMenu(menuItems:)` (no custom `preview:`) has no explicit way to say 'scope the lift preview to only this view, not its shape-bearing ancestor' — this is the OS/framework-level mechanism identified in the original investigation, still unmodified, still the proximate cause"
    - "config/architecture: `HistoryDaySectionCard`'s layout decision to wrap ALL of a day's events under ONE shared `.dpGlassCard()` background (a day-level, not event-level, visual grouping) is what gives the default lift-preview inference a multi-event ancestor to latch onto — a design/layout decision, not a code defect in isolation, that happens to interact badly with the OS's default preview-boundary inference (same causal shape as the ORIGINAL 2026-08-03 finding: 'edge-to-edge width is what exposes the default lift-scale defect' — here it's 'shared multi-event background is what exposes the default lift-preview-boundary defect')"
  and_gate: "No — a single interacting pair (default contextMenu preview-boundary inference + the shared multi-event glass-card ancestor) fully explains the symptom. Both branches point to the same underlying interaction, not two independently-necessary causes; fixing either one (constrain the preview via custom `preview:`, OR give each event its own background-shaped ancestor by flattening to one List row per event) independently resolves it — same both/either relationship already established for the original bug, carried forward unchanged."

## Symptoms
<!-- Written during gathering, then immutable -->

expected: Long-pressing a History row should show the standard iOS context-menu zoom-in preview animation, and tapping outside the menu to dismiss it should smoothly return the row to its normal state — matching standard system contextMenu behavior.
actual: Both the zoom-in (on long-press) and the dismiss (on tap-outside) look visually wrong/glitchy — described as the row "weirdly" snapping back to normal on dismiss, and the Liquid Glass UI "behaving strangely" through the whole interaction.
errors: "None — purely visual, no console errors or warnings."
reproduction: "Always reproducible. History tab (list segment) -> long-press any row -> observe the zoom-in looks wrong -> tap anywhere outside the action card to dismiss -> observe the row snapping back looks wrong."
started: "Noticed during manual verification of the plan-0038 List -> ScrollView+LazyVStack migration fix (2026-08-03), immediately after confirming history-scrollview-bugs' two fixes (row height + pagination). Not yet established whether this glitch predates plan-0038 (i.e. existed under `List` too) or was introduced by it — needs investigation (e.g. `git log` / testing against a pre-migration commit, or checking whether other `.contextMenu` usages elsewhere in the app that are NOT inside a Liquid Glass card show the same glitch)."

## Eliminated
<!-- APPEND only - prevents re-investigating after /clear -->

- hypothesis: "Liquid Glass (`.glassEffect`/`dpGlassCard`) ancestor container is fundamentally incompatible with `UIContextMenuInteraction`'s snapshot-based lift/dismiss rendering (by analogy with this codebase's own prior finding that `.glassEffect` renders zero pixels inside Swift Charts `.annotation`, and independent reports of `UIGlassEffect` + snapshot-testing tools misrendering in iOS 26)."
  evidence: "On-device video evidence showed the row rendering CORRECTLY (proper glass translucency, no missing-pixel/zero-pixel rendering) during the entire lift+hold+dismiss sequence — the defect was specifically edge clipping of the row's own text/icon content past the screen bounds, not a glass-rendering failure. This is a geometry/layout-headroom problem, not a Liquid Glass compositing problem. The original hypothesis's cited precedent (Swift Charts annotations) was a red herring for this specific bug."
  timestamp: 2026-08-03T15:20:00Z

- hypothesis: "Missing `.contentShape(.contextMenuPreview, shape)` — the row lacks an explicit context-menu-preview shape, so the system uses a mismatched default shape/mask for the lift."
  evidence: "Tested directly: applied `.contentShape(.contextMenuPreview, RoundedRectangle(cornerRadius: 16).inset(by: 24))` and re-recorded on-device video. Result was WORSE, not better — `.contentShape(.contextMenuPreview, shape)` acts as a hard CLIP MASK on the lifted preview (not extra scale headroom to grow into), so insetting it clipped the row's own content down to a small pill, hiding most of the row rather than fixing the overflow. Confirms this API controls the preview's mask/shape, not the scale-transform's growth headroom — wrong lever for this root cause."
  timestamp: 2026-08-03T15:26:00Z

- hypothesis: "Adding `.contentShape(.contextMenuPreview, Rectangle())` (UNCLIPPED — matching the row's own bounds exactly, no inset, unlike the 2026-08-03T15:26:00Z attempt above) to the row, in ADDITION to the working custom `preview:` closure, will mask the transient HIGHLIGHT/lift-phase snapshot (shown between touch-down and the menu fully opening) down to just the row's own bounds — per Apple Developer Forums thread 784012 (DTS-confirmed), which establishes that `.contentShape(.contextMenuPreview:)` controls the highlight-phase mask independently of a custom `preview:` view."
  evidence: "Implemented and re-tested via the SAME fine-grained (0.033s-step) on-device video method used to find the original crossfade artifact. Result: NO CHANGE — the highlight-phase snapshot still shows the WHOLE day's 3-row glass card lifting together (frames at t=29.16s-29.23s in the second capture show all 3 rows elevated as one unit, identical in kind to the pre-custom-preview default-contextMenu bug), before crossfading into the correct single-row custom preview around t=29.26s-29.30s — if anything this window (~200ms) was LONGER than the first (unmodified) attempt's ~100ms window, though sample-to-sample timing differences make that difference not meaningfully comparable. Follow-up WebSearch (Apple Developer Forums, GitHub gists on custom contextMenu previews) confirmed the actual mechanism: SwiftUI's `.contextMenu(menuItems:preview:)` only maps to `UIContextMenuConfiguration`'s content-preview provider; the SEPARATE UIKit delegate method that controls the transient highlighting-preview (`UIContextMenuInteractionDelegate.contextMenuInteraction(_:previewForHighlightingMenuWithConfiguration:)`) is not exposed by any pure-SwiftUI API — reaching it requires a custom `UIViewRepresentable`-wrapped `UIContextMenuInteraction`, bypassing SwiftUI's `.contextMenu` entirely. `.contentShape(.contextMenuPreview:)` can only mask WITHIN the interaction's own captured region — it cannot narrow what UIKit chooses to capture in the first place, which, absent an explicit highlighting-preview override, still derives from the ancestor-scoped default (the shared `.dpGlassCard()`)."
  timestamp: 2026-08-05T14:45:00Z

## Evidence
<!-- APPEND only - facts discovered during investigation -->

- timestamp: 2026-08-05T15:00:00Z
  checked: "User's live on-device test of the build containing Option A's settled-state fix (the state left at the end of the 2026-08-05T14:45:00Z checkpoint: custom `preview:` alone, `.contentShape(.contextMenuPreview:)` already tried and reverted)."
  found: "Two things reported, translated from Polish: (1) confirms the already-documented transient whole-card-lifts-then-settles behavior (matches the known, still-open highlight-phase defect). (2) NEW: once settled, the single-row preview itself 'is oddly clipped a bit on the right side — it looks bad.' This second finding was NOT caught by this session's own settled-state self-verification (Evidence 2026-08-05T14:35:00Z/14:45:00Z), which confirmed only sibling-scoping (no other rows lift) and crossfade timing, but never inspected the settled preview's own trailing-edge geometry."
  implication: "A second, independent defect exists inside what this file's own Resolution.fix previously called 'CONFIRMED WORKING for the settled/open state.' Per this turn's instructions, investigated as a SEPARATE defect from the highlight-phase crossfade, not assumed to share a root cause. Re-opened targeted investigation into the settled preview's own geometry."

- timestamp: 2026-08-05T15:15:00Z
  checked: "Fresh on-device video-frame capture (xcrun simctl io recordVideo + a freshly-compiled AVFoundation frame-extraction tool, prior session's tool already gone) of a NEW throwaway XCUITest (`ZoomGlitchClipProbeUITests`, deleted after use) on the SINGLE-event default fixture (`-dp_uitest YES`, no dataset flag — seeds one 500 ml beer 'Bottle' row for today), long-pressing the row for 4s and inspecting the settled/open-menu frames. Also re-examined the EXISTING probe2.mov capture (multi-event `paginationstress` fixture, 3 same-day events) already on disk from the 2026-08-05T14:45:00Z pass, this time specifically pixel-inspecting the trailing/right edge of the settled preview rather than the transition window it was originally captured for."
  found: "IDENTICAL defect confirmed on BOTH fixtures via direct pixel inspection (not visual impression alone): the trailing amount column ('2.0'/'1.3' + 'std' unit label) is hard-clipped by a curved edge that eats increasingly into the content toward the bottom of the short (~56pt-tall) preview card. Pixel-level measurement on the multi-event capture: the white card's own right boundary traces a curve from x=1050 (y=760, near top) out to a maximum x=1109 (y=840, vertical center) and back in to x=1046 (y=920, near bottom) — i.e. almost the ENTIRE right edge is inside a corner-radius arc, with no (or a vanishingly thin) flat vertical segment, because the corner radius is large relative to this short single-row card's height. Dark text pixels (the digit glyph and unit-label glyph) were found extending past this curved white boundary at several rows (e.g. y=800: text_rightmost=1099 vs card_rightmost=1086), i.e. genuinely intersecting/being cut by the curve, not merely sitting close to it. Visually: the '0' of '2.0'/'3' of '1.3' and the 'd' of 'std' are each visibly sliced by the curve. Single-event-day capture (independent XCUITest, independent video, independent extraction run) shows the exact same shape and the exact same glyphs ('0' of '2.0', 'd' of 'std') sliced in the same way."
  implication: "CONFIRMS this is a SEPARATE, independent defect from the already-documented highlight-phase crossfade — it lives in the SETTLED/final preview's own geometry, is present identically regardless of sibling-row count (single-event and multi-event days both affected, so this is NOT a `HistoryDaySectionCard`/shared-ancestor issue), and traces to `EventRow`'s own layout: `EventRow` has NO internal horizontal padding of its own (confirmed by direct source read) — in the RESTING row, clearance from the day-card's `.dpGlassCard()` 24pt-radius corners comes entirely from `HistoryDaySectionCard`'s external `.padding(.horizontal, 16)`, wrapping the VStack of events, not from EventRow itself. The custom `preview:` closure (`EventContextMenu.swift`) reproduces the row's exact on-screen width via `.frame(width: previewWidth)` but does NOT reproduce that external 16pt clearance (deliberately — per the 2026-08-05T14:45:00Z design decision that the preview should carry no background/shape of its own, to avoid the double-chrome ghosting that got the first attempt rejected on 2026-08-03). Per Apple's own documented behavior (already cited in this file's Option A reasoning_checkpoint), 'SwiftUI renders the [custom] preview inside a rounded rectangle' — i.e. iOS ALWAYS wraps a custom preview view in system-controlled chrome/clip, and for this codebase's short single-row preview, that system-controlled corner radius is large enough relative to the row's own height that it clips content sitting flush (zero margin) at the frame's edge — exactly the same class of edge-to-edge-vs-no-headroom mechanism already established as this session's root-cause pattern for the ORIGINAL zoom-glitch bug (edge-to-edge width interacting with a system-controlled rounding/scale mechanism), just recurring here at the settled-preview layer instead of the default-lift layer."

- timestamp: 2026-08-05T15:25:00Z
  checked: "WebSearch: SwiftUI `.contextMenu(menuItems:preview:)` custom preview corner radius / clipping behavior and whether it's controllable."
  found: "Apple Developer Forums thread 784012 ('Cannot configure contentShape for final preview of .contextMenu(menuItems:, preview:)', DTS-answered) confirms directly: applying `.contentShape(.contextMenuPreview:, shape)` to the SOURCE view only affects the LIFT/highlight preview's shape, never the final/settled custom preview's shape — matching and independently corroborating this file's own Eliminated-section finding (2026-08-05T14:45:00Z). Apple DTS's own suggested workaround for shaping the FINAL/settled custom preview specifically is different: apply `.clipShape(.rect(cornerRadius:).inset(by:))` directly to the view returned by the `preview:` closure itself (not to the source `content`) — a plain geometric clip/mask, not a `.background`/material addition."
  implication: "This is a documented, DTS-sanctioned, narrowly-scoped fix for exactly this defect class, and is mechanically different from the two things already tried and eliminated for the (separate) highlight-phase defect: it targets the `preview:` view's OWN clip shape, not `content`'s contentShape. Because `.clipShape` is a pure geometric mask (no new fill/material drawn), it should not reproduce the earlier 'two layers of the same thing' double-chrome ghosting (that bug came from stacking a second `.dpGlassCard()`/`.glassEffect` BACKGROUND under the system's own auto-wrap chrome, not from clipping). Combined with sufficient padding to keep content clear of the CHOSEN (now explicit, known) radius, this gives a mathematically guaranteed fix: a rounded-rect corner of radius R never intrudes into content that is padded ≥R from every edge — a hard geometric property, not empirical guesswork against an opaque system default."

- timestamp: 2026-08-05T15:35:00Z
  checked: "Implemented the fix in `EventContextMenu.swift`: preview closure changed from `.padding(.vertical, 10).frame(width: previewWidth > 0 ? previewWidth : nil, alignment: .leading)` to `.padding(previewContentInset).frame(width: previewWidth > 0 ? previewWidth : nil, alignment: .leading).clipShape(.rect(cornerRadius: previewContentInset))`, with `previewContentInset: CGFloat = 10` (reusing the pre-existing vertical-padding value rather than a new magic number) applied uniformly on all sides and as the matching corner radius. `xcodebuild build` — zero warnings, zero errors (BUILD SUCCEEDED, explicitly grepped for 'warning:'/'error:', only the pre-existing unrelated `appintentsmetadataprocessor` tool notice present, not a compiler warning)."
  found: "Rebuilt for testing, then fresh on-device video-frame captures (xcrun simctl io recordVideo + the same AVFoundation extraction tool) on TWO independent throwaway XCUITests (`ZoomGlitchClipProbeUITests` on the single-event default fixture, `ZoomGlitchClipProbeMultiEventUITests` on the multi-event `paginationstress` fixture — both deleted after use). Pixel-level measurement (same method as Evidence 2026-08-05T15:15:00Z: rightmost dark-text pixel vs. rightmost white-card pixel per scanline) on the settled preview's trailing edge on BOTH fixtures: every sampled row on both captures shows text_rightmost STRICTLY LESS than card_rightmost (negative 'overhang' of roughly 22-39px / ~7-13pt of clear margin) — no dark text pixel found beyond the card's own boundary at any sampled row on either fixture. Visual inspection at 5-6x zoom confirms both digits ('2.0' on single-event, '1.3' on multi-event) and the 'std' unit label are now fully rendered with visible clearance from the rounded edge, vs. the previous hard mid-glyph slicing. Also re-confirmed on the multi-event capture that sibling-row scoping is unaffected: only the pressed row (12:02) is elevated in its own card; siblings (12:01, 12:00) and other days stay flat/dimmed in their resting position — the clipping fix did not regress the settled-state sibling-scoping fix from Evidence 2026-08-05T14:35:00Z."
  implication: "FALSIFICATION TEST (from this turn's reasoning_checkpoint) did NOT find any beyond-boundary text pixel on either fixture — the fix is confirmed effective for the settled-preview trailing-edge clipping defect, independently on both the single-event and multi-event day, closing the exact gap the live user report caught that self-verification previously missed. This defect is now resolved; the SEPARATE, still-open highlight-phase transient crossfade (A2/B/C decision, Evidence 2026-08-05T14:35:00Z/14:45:00Z) is unaffected by this fix and remains open — confirmed by design (this fix only touches the settled `preview:` closure's own shape/padding, not the highlight-phase snapshot mechanism)."

- timestamp: 2026-08-05T19:10:00Z
  checked: "PRIORITY-OVERRIDE new bug report (live user, translated from Polish): 'when some day has two entries and I try to duplicate the LOWER one, it duplicates the TOP one instead.' Per this turn's explicit instruction, checked WHETHER this is a regression from this session's uncommitted Option-A changes BEFORE investigating a root cause. Added a new `-dp_uitest_dataset sameday` fixture (`UITestSeed+Fixtures.swift`/`UITestSeed.swift`: 330 ml beer at 08:00, 750 ml wine at 20:00 for 'today' — distinguishable by category+volume, unlike `paginationstress`'s 3 identical same-day events) and a throwaway XCUITest long-pressing the BOTTOM (beer) row and tapping Duplicate. Ran against the current (uncommitted Option-A) tree, then `git stash push` on exactly `EventContextMenu.swift`+`EventRowButton.swift` (isolating only this session's diff, leaving the new fixture/test files in place) and re-ran the SAME test against the resulting pre-session-baseline build, then `git stash pop` to restore."
  found: "IDENTICAL failure on BOTH trees: pressing the BOTTOM (beer, 08:00) row and tapping 'Duplicate' produces `beer=1, wine=2` — the TOP (wine, 20:00) row was duplicated instead, on the current Option-A tree AND on the git-stash-restored pre-session baseline (where `EventContextMenuModifier` has no `preview:`, no `EventRowWidthPreferenceKey`, no `unitContext` — the literal last-committed HEAD state)."
  implication: "CONFIRMS this is NOT a regression introduced by this session's Option-A context-menu work — it is a PRE-EXISTING defect, reproducible on the unmodified baseline. Scope: root-cause investigation proceeds independent of Option A/the highlight-phase decision, though (per the reasoning_checkpoint above) it turns out to share the same underlying mechanism."

- timestamp: 2026-08-05T19:15:00Z
  checked: "Direct on-device `os.Logger` instrumentation (temporary, subsystem `com.drinkpulse.app`, category `EventContextMenu`, added to `EventContextMenu.swift` for this investigation only and removed immediately after): one log line inside the `.contextMenu { }` menuItems closure (BUILD time, logs `event.category`+`event.uuid.uuidString.prefix(8)`) and one inside `performDuplicate()` (EXEC time, same fields). Captured via `xcrun simctl spawn <udid> log stream --predicate 'subsystem == \"com.drinkpulse.app\" AND category == \"EventContextMenu\"'` running concurrently with the `sameday`-fixture XCUITest (long-press BOTTOM/beer row, tap Duplicate)."
  found: "Initial render: two BUILD lines, `category=wine uuid8=43AA4B32` then `category=beer uuid8=F8A987AC` (matching `ForEach(events)` order — wine is first/newest per the `.reverse`-sorted `@Query`). After long-pressing the BEER row and tapping its own 'Duplicate' label: ONE EXEC line fires — `performDuplicate EXEC for category=wine uuid8=43AA4B32`. Post-duplicate re-render shows a NEW wine event (`uuid8=A1EE3A92`) alongside the original two, confirming the duplicate that was created is a WINE copy, not a beer copy."
  implication: "DEFINITIVE: each row's menu-content closure is built with its own CORRECT `event` (both rows log correctly at build time) — rules out a Swift-level closure-capture/stale-@State bug. But the interaction that fires when the SECOND (beer) row is pressed and its Duplicate button tapped executes the FIRST-registered row's (wine's) `performDuplicate()`, not the pressed row's own. The defect is in UIKit's `UIContextMenuInteraction` routing — WHICH interaction/delegate fires for a given touch — not in what any individual closure captures. See WebSearch corroboration below."

- timestamp: 2026-08-05T19:18:00Z
  checked: "WebSearch: 'SwiftUI contextMenu wrong item List row multiple contextMenu' / 'SwiftUI contextMenu action fires for wrong element sibling views ForEach List row'."
  found: "Multiple independent Apple Developer Forums threads (incl. 651572 'SwiftUI List rows with multiple buttons trigger all buttons', 714614, 119541) and third-party sources converge: 'When using SwiftUI's contextMenu view modifier in views inside List, the context menu is applied to the entire row rather than just to the specific view that defines it... current workaround is to remove List and replace it with a VStack (or LazyVStack) and a ScrollView' — or use a dedicated bridging package (`ViewBoundContextMenu`, wraps `UIContextMenuInteraction` via `UIViewRepresentable` to scope correctly per-view)."
  implication: "This is a DOCUMENTED, externally-corroborated SwiftUI/List framework limitation, not something specific to or introduced by this codebase's implementation. It matches the log-trace evidence exactly: multiple `.contextMenu`s declared on sibling subviews of ONE shared `List` row cannot be independently routed by touch location; SwiftUI/UIKit resolves ambiguously (empirically: to the first-registered one). Both documented workarounds match the two directions already identified in `reasoning_checkpoint_reopened_20260805` for the separate highlight-phase defect — confirming (see this turn's Current Focus reasoning_checkpoint) that the two bugs share one root cause."

- timestamp: 2026-08-05T19:19:00Z
  checked: "Whether context-menu Delete (not just Duplicate) exhibits the identical defect — higher severity, since Delete is irreversible. Wrote a second throwaway XCUITest on the same `sameday` fixture: long-press the BOTTOM (beer) row, tap Delete, confirm via `confirmContextDeleteButton`, then check which row(s) remain."
  found: "Long-pressing the BOTTOM (beer, 08:00) row, tapping Delete, and confirming resulted in the BEER row count staying at 1 (NOT deleted) rather than the expected 0 — the assertion failed at that point (test's `continueAfterFailure = false` stopped before separately re-checking the wine count, but the beer row surviving while Delete was invoked is only consistent with the WINE row having been the one actually deleted, matching the Duplicate case's exact mechanism)."
  implication: "Confirms the SAME wrong-target routing defect applies to Delete, independent of Duplicate — i.e. this is a property of the shared `.contextMenu` interaction itself (both menu items live inside one modifier), not specific to one action. This is the HIGHER-SEVERITY manifestation: a user long-pressing a non-first row on a multi-event day and choosing Delete silently, irreversibly destroys a DIFFERENT health record than the one they intended, with no warning (the confirmation dialog's own title/message do not name which event, so the wrong deletion is invisible even at confirmation time)."

- timestamp: 2026-08-05T19:22:00Z
  checked: "Removed all temporary `os.Logger` TEMP-DIAG instrumentation from `EventContextMenu.swift` (confirmed via `git diff` + `grep -c TEMP-DIAG` = 0). Replaced the two throwaway probe XCUITests with one permanent regression file, `drinkpulseUITests/Features/History/WrongRowContextMenuTargetUITests.swift` (Duplicate + Delete cases, both wrapped in `XCTExpectFailure` with a comment pointing at this debug session, pending the architecture decision). `xcodebuild build` — zero warnings, zero errors. Ran `WrongRowContextMenuTargetUITests` standalone: both tests PASS (as expected-failures — `XCTExpectFailure` intercepts the assertion failure and reports the test as passed while still recording the expected-failure detail in the log), confirming the pin behaves as intended (keeps the suite green, still exercises and records the known-bug path; will flip to an 'unexpected pass' failure if the underlying bug is ever silently fixed without updating the test)."
  found: "Build clean. `WrongRowContextMenuTargetUITests`: 2/2 passed (both as recorded expected-failures). Full History regression battery (`ConsumptionEventTests`, `HistoryInteractionUITests`, `DuplicateEditPersistenceUITests`, `ContextMenuDeleteConfirmationUITests`, `EditDeleteConfirmationUITests`, `EditVolumeIntegrityUITests`, `WrongRowContextMenuTargetUITests`) run to confirm no regression from the fixture/logging cleanup — see Resolution.verification for the tally."
  implication: "Root cause confirmed with direct, unambiguous evidence (log trace + baseline comparison + external corroboration); the bug is now tracked (not silently lost) via a permanent, currently-expected-failure regression test. No code fix applied this turn — see Resolution and this turn's reasoning_checkpoint for why a fix requires a user decision, matching this file's own established precedent for architecture-level forks."

- timestamp: 2026-08-05T20:10:00Z
  checked: "IMPLEMENTED Option (b) per the user's checkpoint decision. `HistoryListQueryView.swift`'s `List` flattened: new `HistoryFlatRow` enum (`HistoryFlatRow.swift`, new file) — `.header(id:title:)` / `.event(ConsumptionEvent, position: HistoryRowGroupPosition)` — with `Array<DaySection>.flattenedForHistoryList()` computing each event's position (`.only`/`.first`/`.middle`/`.last`) from its index within its own day. `HistoryEventCardRow` (new file) wraps `EventRowButton` in its OWN `.dpGlassCard(cornerRadii:)` (new overload added to `DPGlass.swift`, taking an explicit `RectangleCornerRadii` via `Shape.rect(cornerRadii:style:)`, verified against Apple's SwiftUI documentation before use per CLAUDE.md's no-guessing rule), rounding only the day-run's OUTER corners (24pt, matching the retired `DPGlassSize.card` radius) and leaving interior rows square — no visible seam. List row insets reconstructed the retired day-card's exact spacing rhythm (header top=8/bottom=8 default chrome; event rows top=0, bottom=8 only on the last event of a day) via a new `HistoryListQueryView.eventRowInsets(for:)` helper. Retired `HistoryDaySectionCard.swift` deleted (no longer used anywhere — confirmed via grep before deletion). `EventContextMenu.swift`+`EventRowButton.swift` reverted via `git restore --source=HEAD` to the exact pre-session baseline (no custom `preview:`, no `EventRowWidthPreferenceKey`, no `previewContentInset`/`.clipShape` — `git diff` against HEAD confirmed zero diff after restore), per the reasoning_checkpoint_option_b_20260805 hypothesis that this machinery is now unnecessary. `xcodebuild build` — zero warnings, zero errors."
  found: "Clean build. File-size check (`find drinkpulse -name '*.swift' | xargs wc -l | awk '$1 > 300'`) — no output, nothing over the ceiling (touched files: HistoryListQueryView.swift 265, HistoryFlatRow.swift 74, HistoryEventCardRow.swift 97, DPGlass.swift 106, EventContextMenu.swift 160 unchanged from HEAD, EventRowButton.swift 74 unchanged from HEAD)."
  implication: "Structural fix in place, matching the checkpoint response's scope exactly (flat one-row-per-event List, corner-rounded fake grouping, no real shared ancestor). Proceeding to falsify/confirm via the SAME rigor this session already established as necessary (regression tests as hard assertions + 33ms-frame video), not accepting the code change as sufficient on its own."

- timestamp: 2026-08-05T20:11:00Z
  checked: "`WrongRowContextMenuTargetUITests` (both cases, still `XCTExpectFailure`-wrapped at this point) rerun against the flattened-architecture build, exactly as-is, to see whether the expected failure still occurs."
  found: "BOTH tests FAILED with 'Expected failure ... but none recorded' — i.e. `XCTExpectFailure`'s own expectation (that the underlying assertions would fail) was itself violated, because the underlying Duplicate/Delete assertions now PASS. Long-pressing the BOTTOM (beer) row and tapping Duplicate correctly duplicated the beer row (`beer=2, wine=1`); long-pressing the BOTTOM row and confirming Delete correctly removed the beer row (`beer=0, wine=1`)."
  implication: "Direct, unambiguous, first-hand confirmation (not inference) that the wrong-row-target bug is fixed for both Duplicate and Delete. Proceeded to remove the `XCTExpectFailure` wrappers and rerun as hard assertions."

- timestamp: 2026-08-05T20:12:00Z
  checked: "Removed the `XCTExpectFailure` wrappers from both tests in `WrongRowContextMenuTargetUITests.swift`, updated the file's doc comment to describe the fix (not just the bug), rebuilt, reran standalone."
  found: "2/2 PASS as hard assertions (no expected-failure machinery). `test_duplicateBottomRow_duplicatesBeerNotWine`: 13.3s. `test_deleteBottomRow_deletesBeerNotWine`: 15.0s."
  implication: "This regression is now pinned as a hard, permanent test — will fail loudly (not silently pass via XCTExpectFailure) if the wrong-row-target routing bug ever returns."

- timestamp: 2026-08-05T20:13:00Z
  checked: "Fresh on-device video-frame capture (xcrun simctl io recordVideo + a freshly-compiled AVFoundation extraction tool + contact-sheet tool, same method used throughout this session; prior session tools already gone) of TWO throwaway XCUITests (`FlattenedRowVideoProbeUITests`, deleted after use): (1) long-press the FIRST of 3 same-day events (`paginationstress` fixture); (2) long-press + tap-outside-dismiss the single seeded default event. Coarse-scanned each capture (1s steps, contact sheet) to locate the press window, then fine-sampled the FULL touch-down-to-settled transition at 33ms (0.033s) resolution — matching the exact rigor of the original highlight-phase-flash-catching method (Evidence 2026-08-05T14:35:00Z) — via full-frame contact sheets, not spot-checks."
  found: "Multi-event day: every one of the ~18-19 consecutive 33ms frames spanning the ENTIRE touch-down-to-settled transition shows ONLY the pressed row (12:02) elevating — the two sibling rows (12:01, 12:00) for the SAME day, and the OTHER days' cards below, NEVER move, NEVER dim differently, at ANY sampled frame. No frame anywhere in the sequence shows more than one row lifted. Zoomed inspection of the settled state (frame_017.89) confirms the trailing '1.3 / std' text has generous clear margin from the card's rounded edge — no clipping. Single-event day: the transition (48+46 frames across two contact sheets covering the full press-to-dismiss sequence) shows a clean lift-in, held state, and fade-out dismiss with no double-image, no ghosting, no residual artifact after dismiss; zoomed inspection of the settled frame (frame_018.41) shows the leading icon and trailing '2.0 / std' text both fully clear of the card edges on the DEFAULT (non-custom-preview) lift — confirming the reasoning_checkpoint_option_b_20260805 prediction that the default lift's geometry (16pt list-row-inset + 16pt internal padding, unchanged from the retired day-card) is clean, matching the ALREADY-validated Evidence 2026-08-03T18:00:00Z geometry."
  implication: "FALSIFICATION TEST from reasoning_checkpoint_option_b_20260805 did NOT find any sibling-row lift or trailing-edge clipping at ANY sampled frame on either fixture — the highlight-phase crossfade bug (reasoning_checkpoint_reopened_20260805, previously confirmed via the identical method) is CONFIRMED GONE, and the settled-preview clipping fix (Evidence 2026-08-05T15:35:00Z) is CONFIRMED MOOT — the default lift/preview (no custom `preview:`, no `.clipShape` workaround) is clean on its own under the new per-row geometry. All three original bugs (wrong-row Duplicate/Delete targeting, highlight-phase flash, right-edge clipping) are now confirmed fixed by direct, first-hand video/test evidence — not inferred from the architecture change alone, per this turn's explicit instruction."

- timestamp: 2026-08-05T20:15:00Z
  checked: "Removed the throwaway `FlattenedRowVideoProbeUITests.swift` probe. Ran the full History unit-test suites (`HistoryViewModelTests`, `EditEventDeleteTests`, `EditEventVolumeGuardTests`, `HistoryViewTests`, `EventRowStringsTests`, `RowUnitContextTests`) and the full History UI-test battery (`HistoryInteractionUITests` incl. its `+Pagination`/`+DirectionalTransition`/`+HitTarget`/`+LoadingState`/`+Helpers` extensions, `DuplicateEditPersistenceUITests`, `ContextMenuDeleteConfirmationUITests`, `EditDeleteConfirmationUITests`, `EditVolumeIntegrityUITests`, `WrongRowContextMenuTargetUITests`, `HistoryUnitDisplayUITests`, `HistoryDynamicTypeUITests`), plus `ScreenComputePerformanceTests` (pure-function `daySections` profiling, unaffected by the List-architecture change). Then a fresh `xcodebuild clean build` for a final zero-warnings check, and the file-size ceiling check."
  found: "56 unit tests, 0 failures. 27 UI tests, 0 failures (0 unexpected). Clean build: zero warnings, zero errors (only the pre-existing, unrelated `appintentsmetadataprocessor` tool notice). File-size check: nothing over 300 lines."
  implication: "No regression from the flattened-List architecture change across the full History test surface. Combined with the video/test evidence above, this satisfies every self-verification signal available without live human interaction — the last remaining gap (per this session's own established, twice-learned lesson) is live, on-device confirmation from the user, which this turn's CHECKPOINT REACHED requests."

- timestamp: 2026-08-03T15:05:00Z
  checked: "swiftui-expert-skill (Liquid Glass reference) + Apple Developer Forums / web research on glassEffect + contextMenu/snapshot interactions."
  found: "No documented Apple guidance specifically ties contextMenu's lift transition to glassEffect ancestors. Found real, independently-reported iOS 26 UIGlassEffect + non-live-rendering-path bugs (swift-snapshot-testing GitHub issue #1019: glassEffect renders as solid dark grey/black when snapshotted with a tint), corroborating the Liquid Glass hypothesis's PLAUSIBILITY in general — but this turned out not to be the actual mechanism for THIS bug (see Eliminated)."
  implication: "Doc research alone was insufficient to confirm the mechanism; needed direct on-device reproduction (per debugging philosophy's decision-point criteria: 'observed directly, not guessing')."

- timestamp: 2026-08-03T15:12:00Z
  checked: "Built a throwaway XCUITest + `xcrun simctl io recordVideo` + a small Swift/AVFoundation frame-extraction script (no ffmpeg available) to capture and inspect the actual long-press animation frame-by-frame, since XCUITest's own `press(forDuration:)` blocks the main run loop (confirmed empirically — Task-based mid-gesture screenshot attempts never fired during the hold) and a single post-press screenshot only captured the ALREADY-SETTLED state."
  found: "Video frames from 47.7s-48.3s (during the hold, before touch-up) show the row visibly SCALED UP and CLIPPED: the trailing '2.0 / std' text and leading beer-mug icon are cut off past the screen's right/left edges. This state persists from shortly after the ~0.5s long-press threshold through touch-up and for ~1.3s after release, before self-correcting to a smaller (but still not identical to resting) size while the menu stays open."
  implication: "Root cause is real geometric overflow, not a rendering/compositing defect. Screen width (402pt) vs. row content width (338pt) implies the system's default lift scale factor is ~1.19x, exactly matching the point where 338 * scale first exceeds 402."
  
- timestamp: 2026-08-03T15:24:00Z
  checked: "First fix attempt: `.contentShape(.contextMenuPreview, RoundedRectangle(cornerRadius: 16).inset(by: 24))`, re-verified via the same video pipeline."
  found: "Made it WORSE — row content was clipped down to a small pill shape, barely showing 'Bottle' text, overlapping oddly with the menu. `.contentShape(.contextMenuPreview, shape)` is a hard clip mask on the lift preview, not scale headroom."
  implication: "Reverted. Confirms the fix must replace the default lift mechanism (custom preview), not try to reshape it."

- timestamp: 2026-08-03T15:30:00Z
  checked: "Second fix attempt: `.contextMenu(menuItems:preview:)` with a hardcoded `frame(width: 338)` preview, re-verified via video."
  found: "No more edge clipping — but a NEW, smaller artifact appeared: a thin sliver of the underlying (still partially visible) live row peeking out past the preview's edges."
  implication: "Custom preview is the right mechanism, but needs the width to be measured/matched exactly (not hardcoded) to be device/Dynamic-Type-independent and to close the sliver gap."

- timestamp: 2026-08-03T15:33:00Z
  checked: "Third attempt: dynamically measured width via `GeometryReader` + `PreferenceKey` + `@State`, threaded into `eventContextMenu(previewWidth:)`. Re-verified via video."
  found: "Ghosting got WORSE, not better — the underlying row's full content (icon + '2.0 std') visibly duplicated alongside a narrower preview card. Root-caused via on-device `os.Logger` instrumentation added temporarily to both the `GeometryReader` (`DIAG_GEO_WIDTH`) and the `preview:` closure (`DIAG_PREVIEW_WIDTH`): `DIAG_GEO_WIDTH=338.000000` (measurement itself correct) but `DIAG_PREVIEW_WIDTH=0.000000` every single time (never updates)."
  implication: "The measured value never reaches the preview closure. Needed to trace exactly where it's lost."

- timestamp: 2026-08-03T15:41:00Z
  checked: "Added a third diagnostic log directly inside `onPreferenceChange`'s callback (`DIAG_PREF_CHANGE`) to see the value AT THE MOMENT it's written into `@State`."
  found: "`DIAG_GEO_WIDTH=338.000000` fires correctly, but `DIAG_PREF_CHANGE=0.000000` — the preference value is reset to the PreferenceKey's `defaultValue` (0) somewhere between being set inside the GeometryReader and being read by `onPreferenceChange`."
  implication: "Root cause of THIS sub-bug: `contextMenu`'s own `menuItems:`/`preview:` closures are separate SwiftUI view hierarchies that ALSO implicitly participate in preference propagation (every view reports a PreferenceKey's `defaultValue` unless a descendant explicitly sets one). The custom `EventRowWidthPreferenceKey.reduce` used naive 'last write wins' (`value = nextValue()`), so the preview closure's own untouched-default (0) contribution overwrote the real 338 measurement from the row's GeometryReader once `contextMenu(preview:)` was attached to the same view. Confirmed via direct value inspection at each stage of the pipeline (measure -> reduce -> onPreferenceChange), not inferred."

- timestamp: 2026-08-03T15:45:00Z
  checked: "Fixed `EventRowWidthPreferenceKey.reduce` to only accept non-default (`> 0`) contributions (`if next > 0 { value = next }`), rebuilt, re-verified via `os.Logger` (confirmed `DIAG_PREVIEW_WIDTH` now reports the real measured value, not 0) and via the full video pipeline again."
  found: "Clean result: no edge clipping, no content duplication, proper margins maintained throughout the entire lift-hold-dismiss sequence across ~34 frames inspected at 100-150ms resolution. One residual: a barely-perceptible (few-pixel, low-contrast) sliver of the underlying dimmed row is still visible at the trailing edge during the hold in some frames — a dramatic improvement over the original bug (hard text clipping) and the failed second attempt (full content duplication), but not 100% pixel-perfect."
  implication: "Fix confirmed effective for the reported symptoms. Residual sliver noted honestly as an open cosmetic imperfection, not chased further given its severity is negligible relative to what was reported."

- timestamp: 2026-08-03T16:00:00Z
  checked: "User tested the `.contextMenu(menuItems:preview:)` fix in Simulator (human-verify checkpoint response)."
  found: "REJECTED. User: 'it's even worse now, after long-press you can see two layers of the same thing' (Polish, translated) — i.e. real content duplication/ghosting visible to the user on the actual device/Simulator interaction, contradicting this session's own frame-sampled self-verification which reported 'no content duplication' across ~34 sampled frames at 100-150ms resolution. User initially requested a full blind rollback of History List -> ScrollView+LazyVStack (all commits not yet pushed)."
  implication: "Self-verification via frame-sampling was insufficient for this class of bug (transient overlay/animation defect) — either the sampling missed a narrower time window where duplication is visible, or the actual on-device rendering differs from what `xcrun simctl io recordVideo` + AVFoundation extraction captured. Flagged for the eventual knowledge-base 'why not caught' field. A blind full rollback was flagged back to the user as reintroducing the CONFIRMED cold-start hitch 2e0fd4f fixed (List+ForEach eager body/contextMenu evaluation for the entire loaded window, per DEVLOG 2026-08-02 22:00 and diagnostic commit 98031df's Console-log measurement) — user then gave the final re-scoped direction: keep List reverted, but make it genuinely lazy."

- timestamp: 2026-08-03T16:10:00Z
  checked: "`git status` and `git diff --stat HEAD -- drinkpulse/Features/History/` to confirm current working-tree state before making any changes, per this turn's explicit instruction not to assume prior state."
  found: "Working tree has ONLY the now-rejected fix's uncommitted changes in `EventContextMenu.swift`, `HistoryCalendarDayDetail.swift`, `HistoryDaySectionCard.swift` (+82/-3 lines total across the three), plus this now-tracked debug file. No other pending changes. `git log` confirms the unpushed range `2e0fd4f..HEAD` contains, in order: 2e0fd4f (List->ScrollView+LazyVStack migration) -> 3bb5836/bbbbae3/8ba0d9e/4a62018/eee6675 (fetch-window shrink 90->7 days, KEEP) -> 3e2b48c (row-height + pagination fix, code moot after List revert but docs/tests are legitimate history, KEEP docs) -> a3a7fe3/0d4d56c (resolve+KB-update for history-scrollview-bugs debug session, KEEP)."
  implication: "Confirms the rescoped task's own git-state description is accurate and current. Uncommitted rejected-fix changes are discardable via `git restore` (files were unmodified between 98e6dc2 and HEAD except by these uncommitted edits, per `git log --all -- <path>` on each)."

- timestamp: 2026-08-03T16:20:00Z
  checked: "swiftui-expert-skill (list-patterns.md, performance-patterns.md) + Apple Developer Forums web research: does SwiftUI `List` lazily instantiate rows by default, and what specifically causes `.contextMenu` content to build for the whole loaded window rather than just visible rows?"
  found: "List IS lazily cell-reuse-backed for a flat top-level `ForEach` yielding one unary (single-top-level-view) row per element — this is standard, long-documented SwiftUI/UIKit behavior (list-patterns.md: 'List needs the identity of every row up front... When each row's body produces a single top-level view... SwiftUI can template the row id from the ForEach element's id alone, without running each row's body'). BUT: multiple independent Apple Developer Forums threads (762005 'SwiftUI List rows loaded all at once', 704778 'List on macOS prematurely loads every row', and specifically 716063 'SwiftUI List with Section and ForEach loads all rows at once') document a distinct, long-standing, Apple-acknowledged defect: when `List` contains an outer `ForEach` that dynamically generates `Section`s, each `Section` containing its OWN nested inner `ForEach` (i.e. `List { ForEach(groups) { Section { ForEach(items) {...} } } }`), List's row-level laziness is defeated entirely — ALL rows across ALL sections get their body evaluated regardless of scroll position. Filed as Apple Feedback FB11280425 (2022-08-16), still reproducing as of the forum reports found (no indication of an Apple fix)."
  implication: "This is NOT '.contextMenu is inherently eager' (no Apple documentation ties contextMenu specifically to eager evaluation) and NOT 'List in general is eager' (contradicts List's well-documented default laziness) — it is this SPECIFIC nested-ForEach-inside-dynamically-generated-Section pattern. The pre-migration `HistoryListQueryView` (commit 98e6dc2) used EXACTLY this pattern: `List { ForEach(vm.groupedByDay(events), id: \\.day) { section in Section(sectionTitle) { ForEach(section.events) { event in Button {...}.eventContextMenu(...) } } } }`. This fully explains 98031df's Console-log measurement (contextMenu-build log count == total events in the loaded window) as a real, correctly-measured consequence of this specific anti-pattern — every row's full body, including its `.eventContextMenu` modifier's closure, gets evaluated eagerly because the OUTER Section-generating ForEach breaks List's ability to lazily template row identities. 2e0fd4f's high-level empirical conclusion ('this is eager, switch away from List') was operationally correct as measured, but its causal attribution (implicitly 'List+ForEach' in general) was imprecise — the precise, fixable mechanism is the Section+nested-ForEach nesting specifically."

- timestamp: 2026-08-03T16:30:00Z
  checked: "Whether 2e0fd4f's ACTUAL replacement structure (`ScrollView { LazyVStack { ForEach(dayGroups) { HistoryDaySectionCard(...) } } }`, confirmed lazy per the same DEVLOG entry and this session's own reading of `HistoryListQueryView.swift`) avoids the Section+nested-ForEach pattern, and whether swapping ONLY the outer container (`List` in place of `ScrollView`+`LazyVStack`, keeping the exact same flat top-level `ForEach(dayGroups) { HistoryDaySectionCard(...) }` and no `List.Section`) would restore genuine List laziness without reintroducing FB11280425."
  found: "Confirmed: 2e0fd4f's structure has NO `List.Section` and NO nested top-level `ForEach` — `HistoryDaySectionCard`'s own internal `ForEach(Array(events.enumerated())...)` is a PRIVATE implementation detail of one row's body, not a sibling-generating structure the outer lazy container has to reason about. Per list-patterns.md's unary-row rule, `List { ForEach(dayGroups, id: \\.day) { HistoryDaySectionCard(...) } }` (dropping `Section` entirely, keeping `HistoryDaySectionCard` as-is) is structurally the same 'one unary view per top-level ForEach element' shape that already renders lazily today under LazyVStack — swapping the outer container should carry the same laziness guarantee into `List`."
  implication: "A genuinely-lazy List revert does NOT require literally restoring 98e6dc2's `Section`-based code (which would reintroduce FB11280425) — it CAN reuse the current `HistoryDaySectionCard` component almost unchanged, just swapping `ScrollView`+`LazyVStack` back to `List`. This resolves step 2's research question but surfaces a NEW, load-bearing constraint for step 3's swipe-to-delete decision (see next entry)."

- timestamp: 2026-08-03T16:40:00Z
  checked: "Whether native `.swipeActions` can be attached per-EVENT if the List's own top-level `ForEach` yields one row per DAY (a whole `HistoryDaySectionCard`, containing multiple events), vs. confirming `.swipeActions` DID work per-event in the literal pre-migration Section+nested-ForEach structure (98e6dc2, where the List's own ForEach yielded one row per EVENT, nested inside Section)."
  found: "Apple's `.swipeActions` modifier operates at the granularity of what `List`/`ForEach` treats as a discrete row/element in its OWN diffing — in 98e6dc2's code it was applied directly to each event `Button` inside the innermost `ForEach(section.events)`, i.e. List's own direct per-event row producer, and confirmed working (no bug reports or regressions tied to it pre-migration; it was only removed in 2e0fd4f as a direct consequence of moving to `ScrollView`, per that commit's own message: 'needs List row context or swipeActionsContainer(), iOS-27-only'). There is no Apple-documented mechanism for attaching independently-swipeable regions to sub-elements WITHIN a single opaque List row (i.e. per-event swipe inside a `HistoryDaySectionCard` row that represents a whole day) — swipeActions is a row-level affordance tied to the row List itself manages, not an arbitrary nested view."
  implication: "This produces a genuine, evidence-backed three/four-way architecture fork (see CHECKPOINT REACHED returned this turn) rather than a single obvious fix: (1) flat per-event List rows (day boundaries as plain non-swipeable header pseudo-rows in the SAME top-level ForEach) — genuinely lazy AND restores native per-event swipeActions, but drops the day-grouped 'glass card' visual from 2e0fd4f/plan-0027's design language; (2) day-card opaque List rows (current `HistoryDaySectionCard` swapped straight into `List`) — genuinely lazy AND keeps the day-card visual, but per-event swipeActions is NOT achievable (context-menu-only delete stays, same as what shipped in 2e0fd4f); (3) flattened single ForEach with manual first/last-in-group corner-rounding to fake the card grouping visually while each event is still its own List row — genuinely lazy AND native swipeActions AND keeps a card-like grouped look, at the cost of meaningfully more implementation complexity (manual per-row-position styling, no genuine `Section`); (4) literal restore of 98e6dc2's `Section`+nested-`ForEach` — native swipeActions AND day-section headers 'for free', but NOT genuinely lazy (reintroduces the exact confirmed cold-start hitch), ruled out by the user's own explicit requirement."

- timestamp: 2026-08-03T17:15:00Z
  checked: "User's CHECKPOINT REACHED (decision-point) response: chose Option 2 — `List { ForEach(dayGroups) { HistoryDaySectionCard(day) } }`, one top-level List row per day, no `Section`, no nested per-event `ForEach`. Rationale: keeps the Liquid Glass day-card visual, keeps context-menu-only delete (not a regression from 2e0fd4f's shipped state), and is genuinely lazy per the 2026-08-03T16:20:00Z-16:30:00Z research (avoids FB11280425)."
  found: "`git status`/`git diff --stat` re-confirmed (per this turn's explicit instruction not to assume prior state) that the working tree still held exactly the three rejected-fix files' uncommitted edits (EventContextMenu.swift, HistoryCalendarDayDetail.swift, HistoryDaySectionCard.swift) plus the tracked debug file — unchanged since the 2026-08-03T16:10:00Z check. `git restore` on all three cleanly reverted them to their last-committed state (3e2b48c, the row-height/pagination fix), confirmed via `git status`."
  implication: "Step 1 (revert rejected fix) complete and verified. Proceeded to implement Option 2 in HistoryListQueryView.swift only — no changes needed to HistoryDaySectionCard.swift, EventContextMenu.swift, or HistoryCalendarDayDetail.swift per the checkpoint's own architecture (HistoryDaySectionCard is reused unmodified as the List row; EventContextMenu's default `.contextMenu(menuItems:)` is unmodified, matching pre-rejected-fix state)."

- timestamp: 2026-08-03T17:30:00Z
  checked: "Implemented Option 2 in HistoryListQueryView.swift: outer container `ScrollView { LazyVStack(spacing: 16) { ... } }` replaced with `List { ForEach(vm.groupedByDay(events), id: \\.day) { HistoryDaySectionCard(...) } ... }.listStyle(.plain)`. Each row gets `.listRowInsets(EdgeInsets(top: 8, leading: 16, bottom: 8, trailing: 16))` (replicating the previous 16pt horizontal margin + 16pt inter-card spacing via 8+8 top/bottom insets), `.listRowSeparator(.hidden)`, `.listRowBackground(Color.clear)` so `HistoryDaySectionCard`'s own `.dpGlassCard()` fully controls the row's visual appearance, matching the pre-existing Liquid Glass look. `LoadMoreSentinel` switched from `.onScrollVisibilityChange(threshold:)` to `.onAppear`/`.onDisappear` (with the same one-shot-per-visibility-spell guard flag retained), per WebSearch corroboration (tanaschita.com 'How to implement pagination with SwiftUI's List view'; fatbobman.com 'List or LazyVStack' — both confirm List (iOS 15+) is cell-reuse-backed with EXACT row frames via UICollectionView, making a dedicated loading-row `.onAppear` List's own long-standing idiomatic pagination trigger, the reverse of the LazyVStack-estimate problem `onScrollVisibilityChange` was introduced to fix in history-scrollview-bugs)."
  found: "Clean build: `xcodebuild clean build` produced zero warnings, zero errors (`BUILD SUCCEEDED`, confirmed via explicit grep for 'warning:'/'error:' on the clean-build log — none found)."
  implication: "Step 2 (Option 2 implementation) complete. Proceeded to regression testing and step 3's geometry re-check."

- timestamp: 2026-08-03T17:45:00Z
  checked: "Full History regression pass: unit tests (`HistoryViewModelTests` — 32 tests, `EditEventDeleteTests`, `EditEventVolumeGuardTests`, `HistoryViewTests` — Swift Testing framework) + UI tests (`HistoryInteractionUITests` incl. `+Pagination`/`+DirectionalTransition` extensions — 12 tests, `DuplicateEditPersistenceUITests`, `EditDeleteConfirmationUITests`, `EditVolumeIntegrityUITests`, `HistoryUnitDisplayUITests`)."
  found: "First isolated run of `HistoryInteractionUITests` (12 tests) had 1 failure: `test_editCategoryChange_persists` — 'After changing to Wine the row should show the 150 ml wine default', assertion timed out (`waitForExistence(timeout: 5)`) immediately after tapping Save. A dedicated throwaway debug probe (dumping every on-screen button label 2s after Save) proved the row DID correctly update to 'Standard, 150 ml, 12.5 percent ABV, 1.5 std, logged at ...' — the underlying data/render was correct, contradicting a genuine functional regression. Re-ran the single failing test 3x consecutively: 3/3 passed. Re-ran the full `HistoryInteractionUITests` suite fresh: 12/12 passed. Re-ran the complete History regression battery (unit + UI, 9 test classes): 42 unit tests (4 Swift Testing suites) + 17 UI tests, 0 failures, 0 unexpected. Total tally after the investigation: 2 failures (both on the very first invocations after fresh builds) followed by 7 consecutive clean runs of the same test/suite."
  implication: "Classified as CI/simulator first-run timing flakiness (cold Simulator/XCTest warm-up latency on the first invocation after a fresh incremental test-target build), NOT a List-architecture regression — the debug-dump proved the correct content was present and rendering, only the assertion's polling window was occasionally too tight on a cold run. Per CLAUDE.md's UI-test bug policy (fix if small, escalate if big; this is neither a data-integrity nor a BAC/guideline/sync issue) and the flakiness having a direct, first-hand disproof of the 'wrong content' alternative hypothesis, no code change was made for this — recorded as a process note rather than escalated. Test suite is not modified from its pre-existing form (still exercises the correct end-user assertion); no regression test added since this is not a regression."

- timestamp: 2026-08-03T18:00:00Z
  checked: "Step 3: on-device video-frame re-check (same method as the original investigation — `xcrun simctl io recordVideo` + AVFoundation frame extraction via the compiled `extract_frames`/`contact_sheet` scratch tools) of whether the original zoom-glitch geometry (row content width vs. system contextMenu lift-scale headroom) still reproduces under List's own row insets/lift behavior, per this turn's explicit instruction not to assume it's fixed. A throwaway XCUITest (`ZoomGlitchVideoProbeUITests`, deleted after use) drove a long-press (1.5s hold) then an explicit tap-outside dismiss at a safe screen coordinate (avoiding the status bar, which a first attempt at [0.05, 0.05] normalized-offset accidentally targeted and which never dismissed the menu)."
  found: "LIFT/HOLD: frames at the peak-lift moment (menu fully open, e.g. t=22.25s in the first capture) show the row's leading beer-mug icon and trailing '2.0 std' text fully inside the screen bounds with generous, symmetric margins on both edges — no clipping, no scale-overflow, no ghosting/duplication of content. This holds consistently across ~34 inspected frames at 0.04-0.1s resolution spanning touch-down through the full hold. DISMISS: a second, cleaner capture (tap-outside at normalized offset [0.85, 0.75], well clear of both the status bar and the menu) shows a smooth fade-out transition — one intermediate frame (t=26.20s) shows the menu mid-fade (faint 'Duplicate' text, dimmed) with the row underneath already at its correct resting size and position; the very next frames show the row fully settled, clean, correctly sized, with no residual sliver, no duplicate content, and no clipping. This directly matches the Symptoms.expected description ('tapping outside the menu to dismiss it should smoothly return the row to its normal state')."
  implication: "FALSIFIES the geometry hypothesis in the POSITIVE direction predicted by the reasoning_checkpoint_option2 falsification_test: List's own row machinery (native UICollectionView-cell-backed row context that `.contextMenu`'s default lift/preview mechanism attaches to, combined with `.listRowInsets` rather than a plain outer `.padding` on a ScrollView+VStack) provides sufficient headroom that the original clipping mechanism (edge-to-edge ~338pt content vs ~402pt screen, ~1.19x default lift scale) does not manifest. No contextMenu-specific fix (custom preview, width measurement, etc.) is needed — the List architecture swap, chosen for the orthogonal reason of restoring genuine row-level laziness, resolves the originally-reported zoom-glitch as a side effect. This is DIRECT observation (frame-by-frame pixel inspection), not inference from absence, addressing the earlier verification-process learning (2026-08-03T16:00:00Z) that self-verification needed denser/more careful capture — this capture explicitly targeted both the lift AND the dismiss transitions (the first rejected-fix verification round only checked one)."

- timestamp: 2026-08-05T13:20:00Z
  checked: "Independent re-read of the current source tree (not assuming the orchestrator-supplied summary is accurate): `HistoryListQueryView.swift`, `HistoryDaySectionCard.swift`, `EventRowButton.swift`, `EventContextMenu.swift`, `DPGlass.swift`."
  found: "Confirmed exactly as the orchestrator's new-evidence summary described: `HistoryListQueryView.body` is `List { ForEach(sections) { section in HistoryDaySectionCard(...).historyListRowChrome() } ... }` — one List row per DAY. `HistoryDaySectionCard.body` is `VStack { Text(title); VStack(spacing: 0) { ForEach(events, id: \\.uuid) { EventRowButton(...) } }.padding(.horizontal, 16).dpGlassCard() }` — the day title `Text` and the events `VStack` are SIBLINGS in the outer VStack; `.dpGlassCard()` is applied to the events VStack only, wrapping ALL of that day's events under one shared glass background. `EventRowButton.body` wraps its `Button` with `.eventContextMenu(...)`, which resolves (`EventContextMenu.swift`) to a plain `.contextMenu { ... }` (no custom `preview:`). `DPGlass.swift` confirms `dpGlassCard()` == `.glassEffect(.regular, in: .rect(cornerRadius: 24))` for the default `.card` size used here."
  implication: "The architecture is exactly as needed to test the row-vs-subview/shared-ancestor hypothesis: the `.contextMenu` modifier is nested two levels inside the shared `.dpGlassCard()` ancestor (Button -> EventRowButton -> events-VStack.dpGlassCard()), which itself is one List row's content alongside the day title. Proceeded to fresh on-device reproduction rather than concluding from code-reading alone."

- timestamp: 2026-08-05T13:35:00Z
  checked: "Fresh on-device video-frame reproduction, deliberately on a MULTI-EVENT day (unlike the 2026-08-03T18:00:00Z self-verification, which the new evidence flagged as only having sampled a single-event day). Built a throwaway XCUITest (`ZoomGlitchMultiEventProbeUITests`, deleted after use, per this session's established throwaway-probe pattern) launching with `-dp_uitest_dataset paginationstress` (seeds 3 same-day 330ml events for 'today'), opening History, long-pressing the FIRST of the 3 rows for 3.5s, then tapping outside to dismiss. Ran with `xcrun simctl io recordVideo` capturing concurrently (booted iPhone 17 Pro simulator, UDID 4F4A0B72-E0D8-4EAF-B498-842E27732026), rebuilt a small AVFoundation frame-extraction Swift tool from scratch (the original session's tool was a throwaway, already deleted) since no `ffmpeg` is installed on this machine. Coarse-scanned the ~43s capture at 1s steps to locate the menu-open window, then fine-sampled the hold (t=33.5s-38.0s, 0.3s steps, 16 frames) and the dismiss (t=39.0s-42.3s, 0.3s steps, 12 frames)."
  found: "Every one of the 16 hold-window frames (steady state, not a single-frame fluke) shows the SAME thing: all THREE 'Can, 330 ml' rows for 'TODAY' (12:02, 12:01, 12:00 — the pressed row was 12:02, index 0) elevated together as one white rounded card with a drop shadow, floating above a dimmed/grayed background, with the context menu (Duplicate / Delete) presented below-left of it. The 'TODAY' section-title text directly above the card stays in its normal (undimmed, non-elevated) position and size throughout — it does NOT lift with the card. This exactly matches the pressed-single-event-lifts-the-whole-day-card behavior the user described. The dismiss-window frames (12 frames, t=39.0s-42.3s) show a clean return to 3 separate, correctly-positioned, non-dimmed, non-duplicated resting rows — no residual clipping, ghosting, or duplication artifact on dismiss."
  implication: "CONFIRMS the row-vs-subview/shared-ancestor hypothesis and DIRECTLY FALSIFIES the 2026-08-03T18:00:00Z 'RESOLVED' conclusion for the multi-event case: Option 2's List architecture swap did not change `HistoryDaySectionCard`'s internal structure (day title sibling to a `.dpGlassCard()`-wrapped multi-event VStack), so the defect the user is now hitting is the SAME mechanism as originally investigated, just previously untested on a multi-event day. See reasoning_checkpoint_reopened_20260805 for the full RCA and Resolution for the corrected root_cause. Raw capture and extracted frames are ephemeral (scratchpad only, not committed) — this Evidence entry is the durable record."

- timestamp: 2026-08-05T14:35:00Z
  checked: "Implemented Option A (custom `.contextMenu(menuItems:preview:)` in `EventContextMenuModifier`, scoped to the pressed row's own measured width via `EventRowWidthPreferenceKey` with the reduce-key non-default-wins fix, no extra background of its own — see files_changed). Build: `xcodebuild clean build` zero warnings/errors. Regression: 47 unit tests (4 suites) + 21 UI tests (5 classes incl. `HistoryInteractionUITests`, `DuplicateEditPersistenceUITests`, `EditDeleteConfirmationUITests`, `EditVolumeIntegrityUITests`, `HistoryUnitDisplayUITests`), 0 failures. Fresh on-device video capture on the `paginationstress` fixture (3 same-day events), long-pressing the first (12:02) for 3.5s: coarse-scanned the 46s capture at 1s steps, then fine-sampled the settled/open state and — per this turn's reasoning_checkpoint falsification_test, which specifically flagged the risk of a coarse-sampling-missed crossfade — fine-sampled the touch-down-to-menu-open TRANSITION window at 0.05s (50ms) steps (t=23.0-27.0s), inspected both individually (full-resolution) and via a generated contact sheet."
  found: "SETTLED/OPEN STATE (t=26.00s, well after the transition): CLEAN — only the pressed row (12:02) is lifted as its own white card with a drop shadow; siblings (12:01, 12:00) and every other day's card (YESTERDAY, MON 3 AUG) stay flat, dimmed, in their resting position. This directly confirms Option A's core mechanism works for the settled state, on a multi-event day (unlike the 2026-08-03T18:00:00Z single-event-day check this reopened investigation invalidated). HOWEVER: the fine-grained TRANSITION sampling found a genuine, repeatable ~100ms crossfade artifact between t=25.30s and t=25.40s (frames at 25.25s clean-resting, 25.30s transition-starting, 25.35s clearly showing overlapping double-content, 25.40s a fainter residual ghost at the preview's trailing edge, 25.45s fully clean) — during this narrow window, TWO layers are simultaneously visible: (1) the OLD default-derived highlight/lift snapshot, which still shows the WHOLE day's glass card fading/lifting (all 3 rows' text and the 'TODAY' label bleeding through, confirming this snapshot is STILL ancestor-scoped, exactly like the pre-fix defect), cross-fading into (2) the NEW custom single-row preview card growing in on top of it. Both are visible together for roughly 1-2 real video frames."
  implication: "Option A's custom `preview:` closure only replaces what is shown once the menu SETTLES (the 'final'/content preview) — it does NOT replace the separate, UIKit-internal 'highlighting preview' phase (the transitional snapshot shown between touch-down and menu-fully-open), which SwiftUI does not expose an API to customize directly and which still derives from the interaction's default ancestor-scoped snapshot. This is DIRECT, first-hand video evidence (not inference) of the exact class of defect predicted in this turn's reasoning_checkpoint.falsification_test/blind_spots BEFORE this capture was taken (the risk that the 2026-08-03T16:00:00Z 'two layers of the same thing' rejection was a coarse-sampling-missed transient, not a reduce-key-bug artifact) — CONFIRMED, and independent of the pre-fix/post-fix ambiguity flagged as a blind spot: this artifact is a structural property of `.contextMenu(menuItems:preview:)`'s two-phase (highlight → content) lift mechanism, not a previewWidth=0 measurement bug. Per the user's explicit instruction ('confirm no ghosting/duplication ... the exact defect that got the first attempt rejected'), this is NOT YET an acceptable fix — proceeding to test a targeted refinement (`.contentShape(.contextMenuPreview, Rectangle())`, unclipped/uninset, scoped to the row's own bounds) before requesting any human-verify checkpoint, per Apple Developer Forums thread 784012's DTS-confirmed finding that `.contentShape(.contextMenuPreview:)` specifically controls the HIGHLIGHT/lift-phase's mask (independently of the custom `preview:` closure), which the earlier Eliminated-section attempt (2026-08-03T15:26:00Z) never isolated because it combined it with a large `.inset(by: 24)` that clipped content down to a pill on top of using NO custom preview at all."

- timestamp: 2026-08-05T14:45:00Z
  checked: "Added `.contentShape(.contextMenuPreview, Rectangle())` (unclipped, matching `content`'s own row bounds — no inset) to `EventContextMenuModifier`, rebuilt (clean, zero warnings), re-ran the SAME fine-grained on-device video method on a fresh capture (`probe2.mov`) of the identical `paginationstress` scenario. Located the new capture's touch-down-to-menu-open window (t≈29.0-29.3s this run — capture timing shifts slightly run to run) and sampled it at 0.033s (33ms) resolution."
  found: "Highlight phase (t=29.06s-29.23s, at least 5-6 consecutive distinct frames): still shows ALL THREE 'Can' rows for TODAY elevated together as one card — IDENTICAL in kind to the original (pre-custom-preview) default-contextMenu bug and to the unmodified-Option-A crossfade found in Evidence 2026-08-05T14:35:00Z. The `.contentShape(.contextMenuPreview, Rectangle())` addition made NO observable difference. Transition into the correct single-row custom preview still happens abruptly around t=29.26s-29.30s, with the same kind of brief overlapping-content crossfade frame at t=29.26s (faint duplicate '12:01' row and ghosted 'Duplicate/Delete' text behind the settling single-row card)."
  implication: "CONFIRMS (see Eliminated 2026-08-05T14:45:00Z) that `.contentShape(.contextMenuPreview:)` cannot fix this: it only masks what UIKit already captures for the highlight-phase snapshot, and — absent an explicit override of `previewForHighlightingMenuWithConfiguration` (a UIKit delegate method with NO SwiftUI-level exposure) — that capture is still ancestor-scoped to `HistoryDaySectionCard`'s shared `.dpGlassCard()`. This means: within Option A's agreed scope (custom SwiftUI `.contextMenu(menuItems:preview:)`, `HistoryDaySectionCard`'s day-card architecture left untouched), the transient highlight-phase artifact CANNOT be fully eliminated using SwiftUI-only APIs. Two paths remain, both requiring a scope decision beyond what was agreed in the 2026-08-05T14:00:00Z checkpoint response: (a) a full custom `UIViewRepresentable`-wrapped `UIContextMenuInteraction` implementing BOTH `previewProvider` and the highlighting-preview delegate method explicitly, scoped to the row — bigger UIKit-bridging implementation, more risk/edge cases, still keeps `HistoryDaySectionCard` unchanged; or (b) restructure `HistoryDaySectionCard` so each event row carries its own background/shape ancestor instead of one shared day-level `.dpGlassCard()` (Option 3 from the 2026-08-03T16:40:00Z fork — first/last-in-group corner-rounding can preserve the current merged-card VISUAL appearance while giving each row its own shape ancestor structurally), which removes the shared-ancestor condition at its root so even the unmodified default highlight-phase snapshot only ever captures one row. Reverted the ineffective `.contentShape(.contextMenuPreview:)` addition (no benefit, unnecessary code) — current working tree state is Option A's custom-preview mechanism alone (settled state confirmed correct; highlight-phase artifact confirmed NOT YET resolved). This is a genuine architecture-scope decision point per CLAUDE.md's 'What to ask before assuming' and the debugger's checkpoint protocol — returning to the user rather than unilaterally picking (a) or (b)."

- timestamp: 2026-08-06T09:00:00Z
  checked: "User's live on-device test of the Option (b) build (flattened per-event List rows), per the 5-point checklist presented at the 2026-08-05T20:30:00Z checkpoint (wrong-row Duplicate, wrong-row Delete, no sibling-lift, overall zoom-glitch feel)."
  found: "REJECTED, no specifics given: 'wciąż nie wygląda to dobrze' ('still doesn't look good'). User then had to pause for a session/context restart before providing which checklist item failed or describing a new symptom. Working tree presumed unchanged since the last self-verification (2026-08-05T20:30:00Z) — not independently re-confirmed this turn, since no further investigation was authorized before the pause."
  implication: "Fourth instance in this session of self-verification (however rigorous — hard-assertion regression tests, 33ms-frame video, full test battery) diverging from live-device reality (after 2026-08-03T16:00 ghosting, 2026-08-05T13:40 single-event blind spot, and implicitly whatever the settled-preview clipping self-verification missed before the user caught it live on 2026-08-05T15:00). Do not resume by re-attempting a fix on inference — the very first step on resume must be to get a concrete description (which symptom, or a new one) before writing any code. This is now the established, repeatedly-reinforced protocol for this session, not a one-off caution."

- timestamp: 2026-08-11T05:55:00Z
  checked: "Resumed the session with the specific description the 2026-08-06T09:00:00Z pause was waiting for (live user report, translated from Polish): 'when I long-press a row in History, a single-entry day gets an unnecessarily increased bottom margin; if a day has several entries, the same problem always affects only the last (lowest) one in the group.' Read the full debug file first, then the current source (`HistoryListQueryView.swift`, `HistoryFlatRow.swift`, `HistoryEventCardRow.swift`) to check the Option (b) architecture already in the working tree against this new symptom."
  found: "`HistoryListQueryView.eventRowInsets(for:)` (private static func, added by the 2026-08-05T20:10:00Z Option-B fix) returned `EdgeInsets(top: 0, leading: 16, bottom: position.isLastInDay ? 8 : 0, trailing: 16)` — i.e. ONLY the last-in-day event row (which includes every `.only`-position single-event day) got a non-zero `bottom` List row inset, deliberately added to reproduce the 16pt gap before the next day's header. Every other event row got `bottom: 0`."
  implication: "This positional inset asymmetry is a plausible direct cause: the default `.contextMenu` (no custom `preview:`, confirmed still the case per the Option-B revert) lifts/highlights the row's FULL List cell frame, which SwiftUI computes from content + `listRowInsets` — not just the visible card background. A last-in-day row's cell frame is 8pt taller than an otherwise-identical row, and that extra 8pt is transparent empty space below the card, not covered by `HistoryEventCardRow`'s own background. Proceeded to fix by moving the day-gap off the event row entirely, onto the (non-interactive, no-`.contextMenu`) header row instead, per the reasoning below."

- timestamp: 2026-08-11T06:00:00Z
  checked: "Asked the user whether to resume the formal `/gsd-debug continue` loop (matching this file's own 2026-08-06 next_action) or apply a fast, self-verify-only fix given the hypothesis was already high-confidence and low-risk (a pure `EdgeInsets` value change, no new mechanism). User explicitly chose the fast path."
  found: "Implemented in `HistoryListQueryView.swift`: `eventRowInsets(for:)` replaced with a single constant `eventRowInsets = EdgeInsets(top: 0, leading: 16, bottom: 0, trailing: 16)` — now IDENTICAL for every event row regardless of `HistoryRowGroupPosition`, removing the size variance at its source. The 16pt day-to-day gap is now carried entirely by a new `headerRowInsets = EdgeInsets(top: 16, leading: 16, bottom: 8, trailing: 16)` constant applied to the day-header pseudo-row (`top: 16` = the full gap, replacing the previous 8+8 split across the previous day's last event and this header's own top) and reused for `EndOfListFooter` (so the last day's gap-to-footer stays 16pt now that the last event's own bottom inset is 0). `HistoryRowGroupPosition.isLastInDay` itself is unchanged and still used elsewhere (the divider-suppression logic in `EventRowButton`) — only its consumption for List row insets was removed. `xcodebuild build`: zero warnings, zero errors (explicitly grepped for `warning:`, only the pre-existing unrelated `appintentsmetadataprocessor` notice)."
  implication: "Every event row now has byte-identical `listRowInsets`, so the default `.contextMenu`'s row-frame-scoped lift/highlight is geometrically identical no matter which row (or day-group position) is pressed — the mechanism that produced the asymmetry no longer exists, independent of whatever UIKit's exact lift-scoping heuristic actually is."

- timestamp: 2026-08-11T06:07:00Z
  checked: "Full History UI-test battery (`HistoryInteractionUITests` incl. extensions, `DuplicateEditPersistenceUITests`, `ContextMenuDeleteConfirmationUITests`, `EditDeleteConfirmationUITests`, `EditVolumeIntegrityUITests`, `WrongRowContextMenuTargetUITests`, `HistoryUnitDisplayUITests`, `HistoryDynamicTypeUITests`) against the inset fix."
  found: "27 tests run, 3 failures on the first parallelized pass (`test_editCategoryChange_persists`, `test_segmentSwitch_withEmptyState_transitionsCorrectly`, `test_segmentSwitch_withManyEvents_endsInCorrectState` — none of the 3 failure messages reference spacing/margin/geometry: 'wine row should show the 150 ml wine default', 'Context-menu Delete should open a confirmation, not delete immediately', 'Calendar should render today's day cell'). Re-ran all 3 in isolation (not parallelized with the rest of the battery): 3/3 PASSED. `WrongRowContextMenuTargetUITests` (the wrong-row-target regression pin) passed both in the full run and isolated."
  implication: "Classified as simulator parallel-run state-bleed/timing flakiness (same class already documented and dismissed in Evidence 2026-08-03T17:45:00Z for this exact test file, under a different but analogous cause), NOT a regression from the inset change — none of the 3 failing assertions have any plausible causal link to a vertical-inset-value change, and all 3 pass cleanly in isolation. No code change made for this; not re-added as a new regression test since it is not a regression."

- timestamp: 2026-08-11T06:10:00Z
  checked: "Self-verification of the actual fix (screenshot-only, NOT the full 33ms-frame video-transition method — see Current Focus caveat on why this is a deliberately lower-rigor pass this turn). Throwaway XCUITest (`ZZThrowawayMarginProbeUITests`, deleted after use) on the `paginationstress` fixture (3 same-day events): pressed the FIRST (top) row, screenshotted the settled lift; dismissed; pressed the LAST (bottom) row, screenshotted the settled lift. Extracted both screenshots from the `.xcresult` via `xcrun xcresulttool export attachments` and inspected them directly."
  found: "Both screenshots show a visually IDENTICAL tight white lifted card — content hugs the card's top and bottom edges with no visible extra empty space in either case. The previously-expected asymmetry (last row's card floating with dead space below it, relative to the tight fit on every other row) is gone; the last row's card now matches the first row's card exactly."
  implication: "Direct, first-hand (not inferred) confirmation that the specific visual defect described by the user (extra bottom margin, last-row-only) is no longer present in a settled-state screenshot. This is NOT the full rigor this file's own established protocol calls for (no frame-by-frame transition-window sampling, no test on the single-event-day fixture, no test on `HistoryCalendarDayDetail`'s day-detail panel) — an explicit, informed tradeoff the user chose this turn over resuming `/gsd-debug continue`. Given this session's 3-for-3 track record of self-verified-clean states being live-rejected, this fix is recorded as PROBABLE pending live confirmation, not CONFIRMED — see Current Focus."

- timestamp: 2026-08-29T00:00:00Z
  checked: "Live-device verification requested at the human checkpoint: long-press the last row of a multi-event day and the only row of a single-event day, then dismiss each menu by tapping outside."
  found: "User confirmed: yes, fixed."
  implication: "Closes the final verification gap. The inset fix is confirmed on live hardware, so the session can be resolved and archived."

## Resolution
<!-- OVERWRITE as understanding evolves -->

root_cause_2026_08_11: "CONFIRMED by live-device verification on 2026-08-29. A FOURTH, previously-undiscovered symptom of the Option (b) flattened-row architecture below (not one of the three it fixed): `HistoryListQueryView.eventRowInsets(for:)` gave `bottom: 8` (vs. `0`) to ONLY the last-in-day event row's `listRowInsets`, to reproduce the 16pt inter-day gap. Since the default `.contextMenu`'s lift/highlight scopes to the row's FULL List cell frame (content + `listRowInsets`, not just the visible card), that extra 8pt showed as visible dead space below the lifted card specifically on the last row of each day (and every single-event day, since `.only` is also `isLastInDay`). AND-gate: No — single mechanism (positional inset asymmetry interacting with the default lift's row-frame scoping), same causal shape as every other finding in this file (a layout/spacing decision exposing a framework lift-scoping behavior)."
fix_2026_08_11: "IMPLEMENTED. `eventRowInsets(for:)` (position-dependent) replaced with a single constant `eventRowInsets` — `bottom: 0` for every event row, no exceptions. The 16pt day-gap moved onto the (non-interactive, `.contextMenu`-free) day-header pseudo-row's own `headerRowInsets` (`top: 16`, replacing the previous 8+8 split), also reused for `EndOfListFooter` so the last day's gap-to-footer stays 16pt. `HistoryRowGroupPosition.isLastInDay` itself is unchanged (still drives `EventRowButton`'s divider suppression) — only its use for List row insets was removed."
verification_2026_08_11: "CONFIRMED LIVE on 2026-08-29. Earlier evidence: (1) `xcodebuild build`: zero warnings/errors. (2) Full History UI-test battery: 27 tests, 3 unrelated failures that passed in isolation; `WrongRowContextMenuTargetUITests` remained 2/2 PASS. (3) Screenshot comparison showed first-row and last-row lifted cards visually identical. Final human checkpoint tested last-row and single-row long-press/dismiss behavior on a live device; user confirmed the issue is fixed."

root_cause: "FINAL, PENDING HUMAN CONFIRMATION (2026-08-05T20:30:00Z). Unchanged from the 2026-08-05T19:25:00Z merged diagnosis (superseded block below, kept verbatim): `HistoryDaySectionCard` rendered as ONE `List` row per day, and every event within that day was a separate `.contextMenu`-bearing `EventRowButton` NESTED INSIDE that single shared row. SwiftUI/UIKit's `.contextMenu` does not correctly scope its `UIContextMenuInteraction` to individual sibling subviews of one shared List row — a documented, externally-corroborated framework limitation (WebSearch, Apple Developer Forums), not something fixable purely within app code while that shared-row shape persists. This ONE architectural condition produced THREE observed symptoms across this session: (1) the transient HIGHLIGHT/lift-phase snapshot bled in all sibling rows (~100-200ms flash), (2) the interaction that FIRED for a non-first row's long-press always invoked the FIRST-registered `.contextMenu` (wrong-row Duplicate/Delete — the highest-severity symptom, silent/irreversible wrong-record mutation), and (3, a separate, already-fixed sub-defect) the custom `preview:` view added to work around (1) had no internal margin, causing settled-preview trailing-edge clipping. AND-gate: No — single interacting pair (the framework's per-List-row `.contextMenu` routing limitation + the day-level shared-row architecture that exposed it) explains all three; per the user's checkpoint decision, the fix removes the architecture side of the pair directly (flatten to one List row per event) rather than adding a UIKit bridge to compensate for it."
fix: "IMPLEMENTED (2026-08-05T20:10:00Z), Option (b) per the user's explicit checkpoint decision. `HistoryListQueryView`'s `List` flattened to one top-level row per `ConsumptionEvent`, with non-interactive day-header pseudo-rows as siblings in the SAME flat `ForEach` (`HistoryFlatRow.swift`, new file: `.header`/`.event(_, position:)` + `Array<DaySection>.flattenedForHistoryList()`). Each event row (`HistoryEventCardRow.swift`, new file) wraps `EventRowButton` in its OWN `.dpGlassCard(cornerRadii:)` (new overload on `DPGlass.swift`, using `Shape.rect(cornerRadii:style:)` — verified against Apple's SwiftUI docs before use), rounding only the OUTER corners of each day's contiguous run of rows (first/last-in-day) and leaving interior rows square, so the day's rows still read as one seamless card with no real shared ancestor underneath. List row insets (`HistoryListQueryView.eventRowInsets(for:)`) reconstruct the retired day-card's exact spacing rhythm. The retired `HistoryDaySectionCard.swift` was deleted (its shared-row shape was the root cause). Because every event now owns its own List row — exactly one `.contextMenu` per row, no sibling to misroute to, no sibling content to bleed into a highlight snapshot — the custom `.contextMenu(menuItems:preview:)`/`EventRowWidthPreferenceKey`/`previewContentInset`+`.clipShape` machinery added earlier in this session for the highlight-phase/clipping sub-fixes became unnecessary and was REVERTED (`EventContextMenu.swift`, `EventRowButton.swift` restored via `git restore --source=HEAD` to the exact pre-session baseline — confirmed zero diff against HEAD). Native per-event `.swipeActions` is now structurally reachable (each row is genuinely one event) but was deliberately NOT wired up this turn — out of the checkpoint's scope, flagged as future work. `WrongRowContextMenuTargetUITests.swift` updated to assert the CORRECT behavior as hard assertions (the `XCTExpectFailure` wrappers removed, since the bug they tracked is fixed)."
verification: "FINAL SELF-VERIFICATION (2026-08-05T20:30:00Z) — every signal available without live human interaction, addressing this session's own twice-learned lesson that self-verification alone was previously insufficient (2026-08-03T16:00:00Z live rejection; 2026-08-05T13:40:00Z single-event blind spot): (1) `WrongRowContextMenuTargetUITests` (Duplicate + Delete) rerun as HARD assertions — 2/2 PASS, no `XCTExpectFailure` involved. (2) Fresh on-device video, 33ms-frame resolution, spanning the FULL touch-down-to-settled transition on BOTH a 3-same-day-event fixture and the single-event default fixture: zero frames anywhere show more than one row lifted (highlight-phase flash gone); zoomed inspection of settled frames on both fixtures shows generous clear margin on the trailing text (clipping gone/moot). (3) Full History regression battery: 56 unit tests + 27 UI tests, 0 failures. (4) `xcodebuild clean build`: zero warnings, zero errors. (5) File-size ceiling check: nothing over 300 lines. ALL THREE original symptoms (wrong-row Duplicate targeting, wrong-row Delete targeting, whole-day highlight-phase flash) plus the already-fixed clipping sub-defect are confirmed resolved by direct evidence. STILL REQUIRED before this session can be archived: live, on-device confirmation from the user across all three symptoms plus the original reported zoom-glitch, per this session's own established checkpoint precedent — see this turn's CHECKPOINT REACHED."
files_changed:
  - drinkpulse/Features/History/HistoryListQueryView.swift (List flattened to one row per event + day-header pseudo-rows in the same ForEach; new `eventRowInsets(for:)` helper; new private `HistoryDayHeaderRow`) — [FIX, this turn]
  - drinkpulse/Features/History/Components/HistoryFlatRow.swift (NEW — `HistoryFlatRow`/`HistoryRowGroupPosition` enums + `flattenedForHistoryList()`) — [NEW, this turn]
  - drinkpulse/Features/History/Components/HistoryEventCardRow.swift (NEW — per-event List row content: `EventRowButton` + per-position corner-rounded `.dpGlassCard`) — [NEW, this turn]
  - drinkpulse/DesignSystem/DPGlass.swift (added `dpGlassCard(cornerRadii:)` overload using `Shape.rect(cornerRadii:style:)`) — [NEW overload, this turn]
  - drinkpulse/Features/History/Components/HistoryDaySectionCard.swift — DELETED (its shared-row-per-day shape was the confirmed root cause) — [REMOVED, this turn]
  - drinkpulse/Features/History/Components/EventContextMenu.swift (REVERTED to exact pre-session HEAD state — custom preview/PreferenceKey/clipShape machinery no longer needed; confirmed zero diff against HEAD) — [REVERTED, this turn]
  - drinkpulse/Features/History/Components/EventRowButton.swift (REVERTED to exact pre-session HEAD state — `unitContext:` no longer needed by `.eventContextMenu(...)`; confirmed zero diff against HEAD) — [REVERTED, this turn]
  - drinkpulseUITests/Features/History/WrongRowContextMenuTargetUITests.swift (`XCTExpectFailure` wrappers removed — now a hard regression pin; doc comment updated to describe the fix) — [UPDATED, this turn]
  - drinkpulse/UITestSeed.swift (unchanged this turn — `-dp_uitest_dataset sameday` gate added in the prior investigation turn, reused by the now-hard-assertion test) — [PRIOR, unchanged]
  - drinkpulse/UITestSeed+Fixtures.swift (unchanged this turn — `seedSameDayEvents` added in the prior investigation turn) — [PRIOR, unchanged]
  - drinkpulse/Features/History/Components/HistoryCalendarDayDetail.swift (unchanged — plain VStack-in-ScrollView, not List-row-backed, explicitly OUT OF SCOPE of this turn's checkpoint response; still an open, untested blind spot per reasoning_checkpoint_reopened_20260805) — [OUT OF SCOPE, unchanged]

### SUPERSEDED entry (2026-08-03T18:00:00Z) — kept verbatim for the record, DO NOT TREAT AS CURRENT

root_cause: "FINAL (2026-08-03T18:00:00Z), superseding the abandoned single-cause diagnosis below. Two historically distinct but causally linked findings: (1) The originally-reported zoom-glitch's mechanism, as diagnosed in the first investigation pass, WAS a real geometry defect: under `ScrollView { LazyVStack }` with a plain outer `.padding(.horizontal, 16)`, History rows spanned ~338pt against a ~402pt screen, leaving no headroom for the OS's default `.contextMenu(menuItems:)` in-place lift-scale (~1.19x), causing the row's leading icon and trailing text to clip past both screen edges during the hold, which in turn made the dismiss transition look broken (un-clipping from an already-wrong state). (2) This mechanism does NOT reproduce under `List`-hosted rows using `.listRowInsets` instead of an outer `.padding` on a plain ScrollView+VStack — confirmed via direct on-device video-frame inspection of both the lift/hold and the tap-outside dismiss (Evidence 2026-08-03T18:00:00Z): the row stays fully within screen bounds with generous margins throughout the hold, and the dismiss is a clean fade-out with no residual clipping, ghosting, or duplication. The List architecture change (Option 2, chosen by the user for the orthogonal reason of restoring List's genuine row-level laziness per the history-scrollview-bugs/cold-start-hitch re-scope) resolves the reported zoom-glitch as a side effect — no contextMenu-specific fix is needed. AND-gate: not applicable — this is a single mechanism (edge-to-edge width vs. default lift scale) whose presence/absence is fully determined by the outer container's layout machinery, not a combination of independent causes."
fix: "Two-part final fix. (1) Reverted the rejected `.contextMenu(menuItems:preview:)` custom-preview fix's uncommitted edits in `EventContextMenu.swift`, `HistoryCalendarDayDetail.swift`, `HistoryDaySectionCard.swift` via `git restore` (confirmed clean revert to last-committed state, 3e2b48c). (2) Implemented Option 2 in `HistoryListQueryView.swift`: swapped the outer `ScrollView { LazyVStack(spacing: 16) { ... } }` container back to `List { ForEach(vm.groupedByDay(events), id: \\.day) { HistoryDaySectionCard(...) } ... }.listStyle(.plain)`, keeping `HistoryDaySectionCard` completely unmodified as the one-row-per-day component (no `Section`, no nested per-event `ForEach` at the List-visible level — avoids Apple Feedback FB11280425). Each row carries `.listRowInsets(EdgeInsets(top: 8, leading: 16, bottom: 8, trailing: 16))` + `.listRowSeparator(.hidden)` + `.listRowBackground(Color.clear)` to preserve the exact Liquid Glass day-card visual and margins from the pre-revert ScrollView version. `LoadMoreSentinel`'s pagination trigger switched from `.onScrollVisibilityChange(threshold:)` (the LazyVStack-specific fix from history-scrollview-bugs, no longer applicable) to `.onAppear`/`.onDisappear` (List's own idiomatic, UICollectionView-exact-frame-backed pagination pattern), keeping the same one-shot-per-visibility-spell guard. `listPageDays = 7` fetch-window and gap-collapsing pagination logic (`HistoryViewModel.extendedWindowStart`) are untouched. Context-menu-only delete (no native per-event `.swipeActions`) is unchanged from what shipped in 2e0fd4f — not a new regression from this re-scope, an explicit, evidence-backed tradeoff of Option 2 (Evidence 2026-08-03T16:40:00Z)."
verification: "Multi-signal, addressing the earlier verification-process gap (2026-08-03T16:00:00Z) that self-verification alone was insufficient for this class of bug: (1) Build: `xcodebuild clean build` — zero warnings, zero errors. (2) Automated regression: full History battery — 42 unit tests (4 Swift Testing suites: HistoryViewModelTests incl. +Pagination, EditEventDeleteTests, EditEventVolumeGuardTests, HistoryViewTests) + 17 UI tests (HistoryInteractionUITests incl. +Pagination/+DirectionalTransition, DuplicateEditPersistenceUITests, EditDeleteConfirmationUITests, EditVolumeIntegrityUITests, HistoryUnitDisplayUITests) — 0 failures on the clean re-run (one earlier intermittent failure in test_editCategoryChange_persists investigated and attributed to cold-Simulator timing, not a regression — see Evidence 2026-08-03T17:45:00Z). (3) On-device video-frame inspection (the same rigorous method used in the original investigation, this time explicitly checking BOTH lift/hold AND dismiss, learning from the prior round's narrower check): confirmed no clipping during the hold and a clean fade-out dismiss with no residual artifacts, directly falsifying the hypothesis that the glitch would persist. (4) Reasoning checkpoint (reasoning_checkpoint_option2) completed before the fix, including the RCA branching/AND-gate check, per the mandatory Structured Reasoning Checkpoint protocol. **[2026-08-05: THIS ENTIRE VERIFICATION IS NOW KNOWN TO BE INSUFFICIENT — every video-frame check listed here, in both this pass and the 2026-08-03T15:45:00Z pass before it, sampled only single-event days. See root_cause above and reasoning_checkpoint_reopened_20260805.]**"
files_changed:
  - drinkpulse/Features/History/HistoryListQueryView.swift (outer container ScrollView+LazyVStack -> List; LoadMoreSentinel trigger onScrollVisibilityChange -> onAppear/onDisappear)
  - drinkpulse/Features/History/Components/EventContextMenu.swift (reverted rejected custom-preview fix; no net change from pre-rejected-fix state)
  - drinkpulse/Features/History/Components/HistoryDaySectionCard.swift (reverted rejected custom-preview fix; no net change — reused unmodified as the List row)
  - drinkpulse/Features/History/Components/HistoryCalendarDayDetail.swift (reverted rejected custom-preview fix; no net change — untouched by the List re-scope, not part of the List container)
