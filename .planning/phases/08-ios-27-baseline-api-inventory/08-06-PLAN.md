---
phase: 08-ios-27-baseline-api-inventory
plan: "06"
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - .planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md
  - .planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-C*.md
  - .planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY.md
autonomous: false
requirements: [MOD-05]
estimate: {tokens: 9000, raw_tokens: 9000, tasks: 2, confidence: low}
must_haves:
  truths:
    - The owner can inspect each separate substantial-candidate brief before choosing approve, retain or defer with exact scope and reason.
    - Every substantial candidate has a recorded owner outcome and only approved scope is eligible for later Phase 09/10 execution planning.
    - A substantial candidate discovered in later phases triggers the same brief and discussion gate while unrelated approved work can continue.
  artifacts:
    - .planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md
    - .planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY.md
  key_links:
    - Each register outcome links the candidate ID to its brief, exact approved/retained/deferred scope, reason, decision date and downstream phase.
    - The Phase 09/10 handoff in 08-INVENTORY.md includes approved substantial scope only; routine recommendations remain distinguishable.
  prohibitions:
    - statement: "A missing response or recommendation must not be recorded as owner approval for a substantial replacement."
      status: unverified
      flagged: true
---

<objective>
Record the owner's per-candidate modernization decisions and provide a bounded Phase 09/10 handoff.
Purpose: MOD-05 and D-09/D-10/D-11 put scope selection with the owner before any substantial rewrite reaches an execution plan.
Output: completed decision register, brief outcomes and inventory handoff.
</objective>

<execution_context>
@/Users/fempter/.codex/gsd-core/workflows/execute-plan.md
@/Users/fempter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-CONTEXT.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md
</context>

<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Obtain an owner outcome for each substantial candidate</name>
  <read_first>.planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md; every 08-DECISION-CNNN.md linked there; .planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY.md</read_first>
  <decision>For each substantial candidate in the register, choose approve, retain or defer and state the exact scope and reason; related candidates may be discussed together but outcomes are recorded separately.</decision>
  <context>Per D-09/D-10/D-11, behavior, accessibility, data, startup/lifecycle or system-integration changes need an owner scope discussion before a Phase 09 or 10 execution plan includes them. Present completed briefs and a concise decision table. If the register proves zero substantial candidates, record that fact and continue without a human checkpoint. Otherwise pause this task until the owner responds; elapsed time is not approval.</context>
  <options>
    <option id="approve"><name>Approve scoped change</name><pros>Lets the stated replacement enter its destination phase.</pros><cons>Requires the stated compatibility and regression proof.</cons></option>
    <option id="retain"><name>Retain current behavior</name><pros>Keeps proven behavior and data semantics.</pros><cons>Foregoes the proposed improvement.</cons></option>
    <option id="defer"><name>Defer decision</name><pros>Allows more evidence without blocking unrelated approved work.</pros><cons>The affected rewrite stays outside execution scope.</cons></option>
  </options>
  <resume-signal>For every candidate ID, provide approve, retain or defer, exact scope and reason. If no candidate exists, record “zero substantial candidates.”</resume-signal>
  <action>Present each finished brief with its source-backed recommendation, alternatives and risk; collect a distinct owner response for each candidate ID, including the chosen outcome, exact scope and reason. Inspect the response against the candidate ID and brief during the checkpoint; a register label alone is not an owner response. Record a verified zero-candidate state without requesting a choice when no briefs exist. Hold affected rewrite scope at the checkpoint until the owner supplies the decision; the next task records it.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; b=Path(".planning/phases/08-ios-27-baseline-api-inventory"); r=(b/"08-DECISION-REGISTER.md").read_text(); fs=list(b.glob("08-DECISION-C[0-9][0-9][0-9].md")); assert fs or "zero substantial candidates" in r.lower(); assert all(f.stem.replace("08-DECISION-","") in r for f in fs); print("Every prepared brief is present in decision register")'</automated>
    <fails_when>A substantial brief is absent or not linked in the register; owner choices are requested without a reviewable brief.</fails_when>
    <human-check>For each candidate, inspect the owner's actual checkpoint response and identify its candidate ID, approve/retain/defer outcome, exact scope and reason before resuming Task 2. An unanswered or ambiguous candidate remains at the checkpoint.</human-check>
  </verify>
  <acceptance_criteria>D-11: every substantial candidate receives an actual owner response with scope and reason before its rewrite is planned; zero-candidate case is explicit.</acceptance_criteria>
  <done>Owner outcome is available per candidate, or a verified zero-candidate finding is recorded.</done>
</task>

<task type="auto">
  <name>Persist decisions and bound Phase 09/10 execution eligibility</name>
  <read_first>Owner responses to the preceding checkpoint; 08-DECISION-REGISTER.md; 08-INVENTORY.md; all affected 08-DECISION-CNNN.md; 08-CONTEXT.md D-11</read_first>
  <files>.planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md, .planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-C*.md, .planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY.md</files>
  <action>Per D-11, transcribe each actual owner choice into the register and its separate brief with decision date, exact approved/retained/deferred scope and the owner's reason. Use a register table with columns Candidate ID, Brief, Destination, Outcome, Decision date, Exact scope, Owner reason, Owner response; record a verbatim response excerpt or explicit transcript reference in the last column and keep the original response available for checkpoint comparison. Mirror candidate ID, outcome, date, scope and reason in each brief. In 08-INVENTORY.md create an exact heading “### Substantial scope eligible for Phase 09/10” and a table with Candidate ID, Phase, Approved scope and Decision brief; use a header and separator with no rows when no substantial scope is approved. Keep routine behavior-preserving recommendations in a separate section. Encode literal pipe characters inside table cells as HTML entities so the cross-artifact check can parse them. Do not infer approval from silence or a favorable recommendation. Only approved substantial scope is eligible for a Phase 09 or Phase 10 execution plan, while retained/deferred or pending scope is explicitly excluded. Link each handoff row to the baseline diagnostic, source locations and brief. State that any later substantial discovery must receive its own source-backed brief and owner discussion before that affected rewrite, while unrelated approved tasks continue. If no substantial candidates exist, persist a zero-candidate decision outcome with evidence and no invented approval. Check high-severity threat controls before closing the plan.</action>
  <verify>
    <automated>python3 -c '
import datetime, html, re
from pathlib import Path
b=Path(".planning/phases/08-ios-27-baseline-api-inventory")
r=(b/"08-DECISION-REGISTER.md").read_text()
m=(b/"08-INVENTORY.md").read_text()
def norm(s): return re.sub(r"\s+", " ", html.unescape(s)).strip()
def table(doc, needed):
    lines=doc.splitlines()
    for i, line in enumerate(lines):
        if not line.startswith("|"): continue
        heads=[norm(x).lower() for x in line.split("|")[1:-1]]
        if not set(needed).issubset(heads): continue
        assert len(lines)>i+1 and lines[i+1].startswith("|"), "Missing table separator"
        sep=[x.strip() for x in lines[i+1].split("|")[1:-1]]
        assert len(sep)==len(heads) and all(re.fullmatch(r":?-{3,}:?", x) for x in sep), "Invalid table separator"
        rows=[]
        for row in lines[i+2:]:
            if not row.startswith("|"): break
            cells=[norm(x) for x in row.split("|")[1:-1]]
            assert len(cells)==len(heads), "Malformed decision table row"
            rows.append(dict(zip(heads,cells)))
        return rows
    raise AssertionError("Required table headings absent: "+", ".join(needed))
register=table(r, ("candidate id","brief","destination","outcome","decision date","exact scope","owner reason","owner response"))
briefs={f.stem.removeprefix("08-DECISION-"): f for f in b.glob("08-DECISION-C[0-9][0-9][0-9].md")}
ids=[x["candidate id"] for x in register]
assert len(ids)==len(set(ids)) and set(ids)==set(briefs), "Register and briefs differ"
bad={"", "-", "pending", "tbd", "n/a"}
for x in register:
    cid=x["candidate id"]
    assert re.fullmatch(r"C[0-9]{3}", cid) and x["outcome"].lower() in {"approve","retain","defer"}
    datetime.date.fromisoformat(x["decision date"])
    assert re.fullmatch(r"Phase (09|10)", x["destination"])
    assert all(x[k].lower() not in bad for k in ("exact scope","owner reason","owner response"))
    assert briefs[cid].name in x["brief"]
    body=norm(briefs[cid].read_text()).lower()
    assert all(norm(v).lower() in body for v in (cid,x["outcome"],x["decision date"],x["exact scope"],x["owner reason"])), "Brief decision differs from register"
if not register: assert not briefs and "zero substantial candidates" in r.lower()
heading=re.search(r"(?m)^### Substantial scope eligible for Phase 09/10\s*$", m)
assert heading, "Eligible handoff heading absent"
section=re.split(r"(?m)^#{1,6} ", m[heading.end():], maxsplit=1)[0]
handoff=table(section, ("candidate id","phase","approved scope","decision brief"))
approved={x["candidate id"]:x for x in register if x["outcome"].lower()=="approve"}
assert len(handoff)==len(approved) and {x["candidate id"] for x in handoff}==set(approved), "Handoff eligibility differs from approvals"
for x in handoff:
    decision=approved[x["candidate id"]]
    assert x["phase"]==decision["destination"] and x["approved scope"]==decision["exact scope"] and briefs[x["candidate id"]].name in x["decision brief"], "Handoff scope, phase or brief differs"
print("Decision fields, briefs and approved-only handoff agree; owner response requires checkpoint inspection")
'</automated>
    <fails_when>A candidate lacks a valid date, exact scope, owner reason or response record; a brief conflicts with its register row; a retained/deferred candidate appears in the eligible handoff; an approved handoff row changes scope or destination; the original owner response does not support the recorded outcome; or zero-candidate evidence is absent.</fails_when>
    <human-check>Compare each register Owner response entry and brief outcome with the actual owner response inspected at the checkpoint; confirm the recorded scope and reason preserve that response exactly. Automation checks consistency of recorded artifacts, not the authenticity of the human decision.</human-check>
  </verify>
  <acceptance_criteria>D-11/MOD-05: each substantial candidate has approve, retain or defer plus scope and reason based on the owner's actual response, and no unapproved substantial scope is passed to Phase 09/10 plans.</acceptance_criteria>
  <done>Decision artifacts and downstream handoff exactly reflect the owner's recorded choices.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Owner discussion to execution eligibility | An agent may mistake a proposal, silence or partial approval for authorized scope. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-15 | Repudiation | Owner outcome register | high | mitigate | Record each actual response, date, candidate ID, scope and reason in register and brief. |
| T-08-16 | Tampering | Phase 09/10 handoff | high | mitigate | Include only approved substantial scope with links to matching brief/decision; keep retain/defer excluded. |
</threat_model>

<verification>
Check every substantial brief against the register, inspect the owner's stated decision, and confirm only approved scope appears as eligible in the master inventory. If any high threat remains open, do not mark Phase 08 complete.
</verification>

<success_criteria>
MOD-05 owner decisions are recorded per substantial candidate before any affected Phase 09/10 execution plan is written. Later discoveries repeat this decision gate.
</success_criteria>

<output>
Create .planning/phases/08-ios-27-baseline-api-inventory/08-06-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- Completed 08-DECISION-REGISTER.md, per-candidate brief outcomes and the approved-scope handoff in 08-INVENTORY.md.
