---
phase: 08-ios-27-baseline-api-inventory
plan: "07"
type: execute
wave: 5
depends_on: ["08-06"]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
gap_closure: true
requirements: [PLAT-02]
estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - A developer on another Mac can identify a locally installed iOS 27 simulator, set its local destination identifier, and use it for the documented build and test commands.
    - README.md and CLAUDE.md identify the committed Phase 08 baseline as immutable evidence without presenting its capture-machine simulator identity as a general destination.
    - Every current general build, full-test, scoped-test, and coverage command in the two living entry points uses the documented local destination variable.
  artifacts:
    - README.md
    - CLAUDE.md
  key_links:
    - README.md and CLAUDE.md link to .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md for the captured command and result.
    - The local selection instructions define LOCAL_IOS_27_UDID before xcodebuild destinations consume it.
---

<objective>
Close the verified PLAT-02 portability defect in the two living developer entry points.
Purpose: A simulator identity captured as evidence on one Mac cannot be a general build or test destination on another Mac. D-12 and D-13 require durable, truthful living guidance linked to immutable baseline evidence.
Output: README.md and CLAUDE.md use a documented local iOS 27 simulator-selection flow and link the Phase 08 baseline evidence.
</objective>

<execution_context>
@/Users/fempter/.codex/gsd-core/workflows/execute-plan.md
@/Users/fempter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-VERIFICATION.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-DOC-AUDIT.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-02-SUMMARY.md
@README.md
@CLAUDE.md
</context>

<tasks>
<task type="tracer">
  <name>Make the README build and full-test path portable from local simulator discovery to immutable evidence</name>
  <reversibility rating="reversible">This documentation-only repair changes no build setting, source behavior, persistence, or baseline evidence; the prior prose is recoverable from Git.</reversibility>
  <read_first>README.md; .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md; .planning/phases/08-ios-27-baseline-api-inventory/08-VERIFICATION.md; .planning/phases/08-ios-27-baseline-api-inventory/08-DOC-AUDIT.md</read_first>
  <files>README.md</files>
  <action>Per D-12/D-13 and the first verified gap, revise only README.md's Development instructions. Add a self-contained local iOS 27 simulator-selection step that runs xcrun simctl list devices available, tells the developer to choose an available iOS 27 simulator on that Mac, exports LOCAL_IOS_27_UDID from the selected identifier, and uses xcrun simctl bootstatus on that variable before building. Replace the general Debug build and full-test destinations with platform=iOS Simulator,id=$LOCAL_IOS_27_UDID. Link .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md as the immutable capture record for the exact historical command, toolchain, result, and capture environment. Keep the captured simulator identity confined to that baseline artifact; README.md must describe only the developer's local value. Preserve the documented Xcode 27, Swift 6.4 compiler, Swift 6 language-mode, and no-third-party-dependency facts.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; s=Path("README.md").read_text(); assert "xcrun simctl list devices available" in s and "LOCAL_IOS_27_UDID" in s and "xcrun simctl bootstatus" in s; assert ".planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md" in s; assert s.count("id=$LOCAL_IOS_27_UDID") >= 2; assert "1D35E1B8-4141-4EFF-A493-52CB37B600A5" not in s; print("README local simulator destination is portable")'</automated>
    <fails_when>The local discovery command, destination variable, baseline link, portable build/test destinations, or removal of the capture-only identity from README.md is missing.</fails_when>
  </verify>
  <acceptance_criteria>D-12/D-13 and PLAT-02: README.md lets a developer select a local iOS 27 simulator and run its build and full-test commands without possessing the evidence-capture simulator identifier; the immutable baseline remains linked for historical reproduction.</acceptance_criteria>
  <done>The README's documented happy path reaches a local selected simulator and points to the committed evidence for the original captured result.</done>
</task>

<task type="auto">
  <name>Apply the same portable destination contract to CLAUDE coverage and verification commands</name>
  <reversibility rating="reversible">This is a narrow living-documentation correction with no code or project-setting mutation.</reversibility>
  <read_first>CLAUDE.md:426-429; CLAUDE.md:522-535; README.md; .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md; .planning/phases/08-ios-27-baseline-api-inventory/08-VERIFICATION.md</read_first>
  <files>CLAUDE.md</files>
  <action>Per D-12/D-13 and the same verified gap, update every active simulator destination in CLAUDE.md's coverage and Build &amp; verify sections. Introduce one clearly labeled local iOS 27 selection block before its commands, using the same LOCAL_IOS_27_UDID contract as README.md: list available devices, select an available iOS 27 simulator locally, export the variable, and wait with simctl bootstatus. Make the coverage, Debug build, full-suite, and scoped-test commands consume id=$LOCAL_IOS_27_UDID; do not leave a second general simulator destination form behind. Link 08-BASELINE.md at the selection block and explain that it retains the immutable evidence capture while the local variable is intentionally machine-specific. Preserve the existing scoped-test and coverage options, quality-gate language, and no-guessing documentation policy.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; s=Path("CLAUDE.md").read_text(); assert "xcrun simctl list devices available" in s and "LOCAL_IOS_27_UDID" in s and "xcrun simctl bootstatus" in s; assert "08-BASELINE.md" in s; assert s.count("id=$LOCAL_IOS_27_UDID") >= 4; assert "1D35E1B8-4141-4EFF-A493-52CB37B600A5" not in s; print("CLAUDE build, test, scoped-test, and coverage destinations are portable")'</automated>
    <fails_when>The shared local-selection block, baseline evidence link, any required portable command destination, or removal of the capture-only identity from CLAUDE.md is missing.</fails_when>
  </verify>
  <acceptance_criteria>D-12/D-13 and PLAT-02: all current CLAUDE.md coverage and build/test examples use the documented local iOS 27 destination, retain their original test scope, and link the immutable baseline rather than duplicating its machine-local identity.</acceptance_criteria>
  <done>CLAUDE.md supplies one consistent local selection mechanism that every active simulator command consumes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Living developer instructions to a developer's local Xcode environment | A stale machine-specific destination can redirect or prevent baseline verification. |
| Immutable baseline evidence to mutable entry-point documentation | Documentation can misrepresent the recorded capture if it copies environment-specific values as general defaults. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-17 | Tampering | README.md and CLAUDE.md simulator destinations | high | mitigate | Assert local discovery, local variable use, baseline link, and absence of the capture-only value in both documents. |
| T-08-18 | Repudiation | Baseline reproduction claims | medium | mitigate | Keep the immutable baseline link adjacent to local selection instructions and preserve its evidence rather than copying mutable outcomes. |
| T-08-19 | Information disclosure | Build evidence references | low | accept | Link the existing committed summary only; do not add raw logs, result bundles, signing data, or personal test data to living docs. |
</threat_model>

<verification>
Run both task-level Python assertions from the repository root. Then inspect README.md and CLAUDE.md to confirm the local destination contract precedes each command family and the fixed evidence identity remains only in 08-BASELINE.md.
</verification>

<success_criteria>
PLAT-02 is restored: developers can select an available local iOS 27 simulator and run documented build, full-test, scoped-test, and coverage commands, while immutable evidence remains in 08-BASELINE.md.
</success_criteria>

<output>
Create .planning/phases/08-ios-27-baseline-api-inventory/08-07-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- README.md and CLAUDE.md portable local-simulator instructions linked to the immutable Phase 08 baseline evidence.
