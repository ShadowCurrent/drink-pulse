---
phase: 08-ios-27-baseline-api-inventory
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - drinkpulse.xcodeproj/project.pbxproj
  - README.md
  - CLAUDE.md
  - .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md
  - .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json
autonomous: true
requirements: [PLAT-01, PLAT-02]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
must_haves:
  truths:
    - All app, unit-test, and UI-test effective Debug and Release deployment targets are iOS 27.0 and their Swift language modes are 6.0.
    - A named iOS 27 simulator and selected Xcode 27/Swift 6.4 toolchain produced a recorded Debug build, Release build, and complete unit and UI test run, whether each passed or failed.
    - Every baseline diagnostic and failure is preserved and attributed without silently changing a failed run into a pass.
  artifacts:
    - drinkpulse.xcodeproj/project.pbxproj
    - .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md
    - .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json
  key_links:
    - The baseline manifest's exact build/test commands use the same project, shared scheme, configuration, and simulator recorded in its toolchain section.
    - Every committed log path and SHA-256 in the manifest identifies a complete raw file outside the Git worktree.
  flagged_assumptions:
    - "PLAT-01 adjacency probe unresolved: verify how 27 versus 27.0 is represented in effective settings before asserting equality."
    - "PLAT-01 empty probe unresolved: a missing target/configuration or null effective setting must be reported, never counted as compliant."
    - "PLAT-01 ordering probe unresolved: matrix ordering must be stable enough to compare repeated captures; preserve target and configuration labels."
  prohibitions:
    - statement: "Raising the deployment minimum must not reset a SwiftData store or alter a shipped VersionedSchema snapshot."
      status: unverified
      flagged: true
    - statement: "The committed baseline must not contain personal health values, signing secrets, or bulky raw build and test logs."
      status: unverified
      flagged: true
---

<objective>
Establish the reproducible iOS 27 toolchain and target baseline, then capture its complete build and test evidence.
Purpose: PLAT-01 and PLAT-02 need observable settings and honest results before modernization candidates are judged. D-01, D-02, D-03, and D-08 control this work.
Output: iOS 27 project settings, runnable entry-point commands, a committed baseline summary/manifest, and external raw evidence.
</objective>

<execution_context>
@/Users/fempter/.codex/gsd-core/workflows/execute-plan.md
@/Users/fempter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-CONTEXT.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-RESEARCH.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-VALIDATION.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-PATTERNS.md
</context>

<tasks>
<task type="tracer">
  <name>Prove one documented iOS 27 Debug build from selected Xcode through target settings to external evidence</name>
  <reversibility rating="costly">Later iOS 27-only API adoption would make restoring iOS 26 support a coordinated compatibility audit across Phases 09 and 10; D-01 already selected the new minimum.</reversibility>
  <read_first>drinkpulse.xcodeproj/project.pbxproj; drinkpulse.xcodeproj/xcshareddata/xcschemes/drinkpulse.xcscheme; README.md; CLAUDE.md; 08-RESEARCH.md; Apple Xcode requirements https://developer.apple.com/xcode/system-requirements; Apple Xcode CLI reference https://developer.apple.com/documentation/xcode/xcode-command-line-tool-reference</read_first>
  <files>drinkpulse.xcodeproj/project.pbxproj, README.md, CLAUDE.md, .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md, .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json</files>
  <action>Per D-01/D-08, check xcode-select, xcodebuild -version, xcrun swift --version, iOS SDK, simulator runtime and simctl device identity at execution; use installed Xcode 27 and a named iOS 27 simulator (research observed Xcode 27.0 build 27A266a, Swift 6.4 swiftlang-6.4.0.34.1, SDK 27.0, iPhone 18 Pro UDID 1D35E1B8-4141-4EFF-A493-52CB37B600A5; reselect and record if recreated). Change the six existing IPHONEOS_DEPLOYMENT_TARGET assignments at project.pbxproj:283,346,405,480,495,512 from 26.0 to 27.0. Preserve SWIFT_VERSION = 6.0 in all target/configuration blocks at lines 287,440,469,484,500,517; record each effective setting, including the app's inherited project value. Use a new timestamped directory beneath $HOME/Library/Logs/DrinkPulse/phase-08-ios27 outside the worktree. Run an explicit Debug xcodebuild build with project, shared scheme, selected simulator UDID and unique resultBundlePath; capture full stdout, stderr, exit code, xcresult build-results, diagnostic counts and SHA-256. Create 08-BASELINE.json with stable keys artifact_root, toolchain, simulator, effective_settings, commands, runs, dependencies, diagnostics and failure_classification. Each runs entry has command, stdout_path/stdout_sha256, stderr_path/stderr_sha256, exit_code, result_bundle_path or a recorded artifact_error; build/test result summaries have result_summary_path/result_summary_sha256. Each effective_settings item has target, configuration, deployment_target and swift_version. Create 08-BASELINE.md explaining the same evidence. Update every current README.md and CLAUDE.md iOS minimum, Xcode/host requirement (Apple lists macOS Tahoe 26.6 or later for Xcode 27), simulator and Debug/full/scoped test command (notably README.md:6,118-129 and CLAUDE.md:10,36,426-427,522-534), per D-12; preserve dated historical examples. This is the production tracer: the documented command, actual project configuration, result bundle or classified artifact failure and committed manifest all refer to the same runnable path. Preserve app behavior, store files and all frozen schemas.</action>
  <verify>
    <automated>python3 -c 'import json,pathlib,hashlib; d=json.load(open(".planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json")); p=pathlib.Path(d["artifact_root"]); r=d["runs"]["debug_build"]; f=p/r["stdout_path"]; e=p/r["stderr_path"]; assert p.is_absolute() and p.exists() and f.is_file() and e.is_file() and hashlib.sha256(f.read_bytes()).hexdigest()==r["stdout_sha256"] and hashlib.sha256(e.read_bytes()).hexdigest()==r["stderr_sha256"] and isinstance(r["exit_code"],int) and ((bool(r.get("result_bundle_path")) and (p/r["result_bundle_path"]).exists()) or bool(r.get("artifact_error"))); assert len(d["effective_settings"])==6; assert {(x["target"],x["configuration"]) for x in d["effective_settings"]}=={(t,c) for t in ("drinkpulse","drinkpulseTests","drinkpulseUITests") for c in ("Debug","Release")}; assert all(x["deployment_target"]=="27.0" and x["swift_version"]=="6.0" for x in d["effective_settings"]); print("Debug tracer evidence and six effective settings present")'</automated>
    <fails_when>Manifest parsing fails; the Debug log/hash or result bundle is absent; a target/configuration is absent; an effective deployment or language mode differs.</fails_when>
  </verify>
  <acceptance_criteria>D-01/D-02/D-08: the exact Debug command ran on the named iOS 27 simulator; its true exit code, all diagnostics and raw evidence are recorded; six effective target/configuration settings match 27.0 and Swift 6 mode. A failed build remains a recorded failed baseline, not a hidden success.</acceptance_criteria>
  <done>One documented Debug build has a reproducible command, a result bundle, a hashed complete log, and a truthful status linked from the baseline.</done>
</task>

<task type="auto">
  <name>Complete Release, dependency, and full unit plus UI baseline evidence</name>
  <read_first>.planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md; .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json; drinkpulse.xcodeproj/xcshareddata/xcschemes/drinkpulse.xcscheme:39-67; Apple test-result guidance https://developer.apple.com/documentation/xcode/running-tests-and-interpreting-results</read_first>
  <files>.planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md, .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json</files>
  <action>Per D-01/D-02, run xcodebuild -resolvePackageDependencies, an explicit Release build, and one full Debug xcodebuild test invocation on the same selected simulator with separate external stdout, stderr, exit and .xcresult artifacts. Do not use -only-testing or -skip-testing for the full run; confirm the shared scheme enables drinkpulseTests and drinkpulseUITests. Capture exact command lines, Xcode/Swift/SDK/runtime/simulator build identifiers, dependency state including absence of Package.resolved or any generated resolution file, signing overrides if needed, full warnings/errors, xcresulttool build-results/test-results summary JSON, total/passed/failed/skipped counts and target-level counts. Hash every raw log and result summary; record raw bundle paths and hashes of an archived bundle or deterministic file manifest, not a fictitious directory hash. Per D-03, classify each nonzero result/diagnostic as app-owned, test-related or environment-related with the original evidence location and assign remediation to Phase 09, 10 or 11 as appropriate; never rerun and replace the original result without keeping both attempts. Record baseline runtime-state limits: no existing-device-store test in Phase 08, preserved HealthKit/notification state semantics, no CloudKit activation. Keep raw material outside Git and redact any personal or secret values from committed summaries. D-13 requires committed links and hashes.</action>
  <verify>
    <automated>python3 -c 'import json,pathlib,hashlib; d=json.load(open(".planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.json")); p=pathlib.Path(d["artifact_root"]); required=("debug_build","release_build","full_test","dependency_resolution"); assert all(k in d["runs"] for k in required); assert all((p/r[n+"_path"]).is_file() and hashlib.sha256((p/r[n+"_path"]).read_bytes()).hexdigest()==r[n+"_sha256"] for r in (d["runs"][k] for k in required) for n in ("stdout","stderr")); assert all(isinstance(d["runs"][k]["exit_code"],int) for k in required); assert "unit_tests" in d["runs"]["full_test"]["counts"] and "ui_tests" in d["runs"]["full_test"]["counts"]; assert d["failure_classification"] is not None; print("Full baseline evidence complete")'</automated>
    <fails_when>Any required run, raw stream/hash, original exit code, either test-target count, or diagnostic classification is missing or inconsistent.</fails_when>
  </verify>
  <acceptance_criteria>D-01/D-02/D-03/D-13: Debug and Release builds, dependency resolution and complete unit/UI suite all have truthful status, structured counts, diagnostics, external raw evidence and exact commands; every failure has a class and destination phase. A failing baseline is allowed only when its failure is visible and classified.</acceptance_criteria>
  <done>The baseline manifest and summary are complete, reproducible, and distinguish failures from passing runs.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Local Xcode/test process to committed planning docs | Diagnostics and result bundles may include personal test values or signing identifiers. |
| Simulator runtime to baseline claim | A stale device/runtime or missing test target can make evidence misleading. |

## STRIDE Threat Register — ASVS L1, block on high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-08-01 | Information disclosure | Baseline logs and manifest | high | mitigate | Use synthetic fixtures; retain raw streams outside Git; inspect and redact committed summaries; record path/hash only. |
| T-08-02 | Tampering | Baseline result attribution | medium | mitigate | Preserve original exit status, separate result bundles and SHA-256 for every run; classify every failure. |
| T-08-03 | Repudiation | Full-suite scope | medium | mitigate | Cite shared scheme testables and xcresult target-level counts plus skips. |
</threat_model>

<verification>
Read the committed manifest and compare all six effective target/configuration settings with a fresh xcodebuild -showBuildSettings -json query. Confirm external raw paths and hashes, both test-target counts, and D-03 failure classes. This is baseline capture, not a requirement that the first iOS 27 run pass.
</verification>

<success_criteria>
PLAT-01/PLAT-02 baseline evidence exists for all six effective settings, Debug, Release, dependencies and the complete suite; every result is reported accurately.
</success_criteria>

<output>
Create .planning/phases/08-ios-27-baseline-api-inventory/08-01-SUMMARY.md when done.
</output>

## Artifacts this phase produces

- 08-BASELINE.md and 08-BASELINE.json; complete raw logs and result bundles outside Git.
- The iOS 27 target-setting diff and current README/CLAUDE toolchain instructions.
