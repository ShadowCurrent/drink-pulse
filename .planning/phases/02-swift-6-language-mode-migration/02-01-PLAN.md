---
phase: 02-swift-6-language-mode-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drinkpulse.xcodeproj/project.pbxproj
  - drinkpulse/Features/AddDrink/DrinkTypePreset.swift
  - drinkpulse/Services/HealthKitAdapter.swift
  - drinkpulse/Services/UITestHealthStore.swift
  - drinkpulse/Services/UITestNotificationCenter.swift
  - drinkpulse/Services/NotificationScheduling.swift
autonomous: true
requirements: [SWIFT6-01, SWIFT6-02]

must_haves:
  truths:
    - "xcodebuild build for the drinkpulse app target succeeds with SWIFT_VERSION = 6.0 in both Debug and Release configurations, with zero compiler errors and zero warnings (ROADMAP Phase 2 Success Criterion #1)"
    - "Every @unchecked Sendable, @preconcurrency, and nonisolated(unsafe) suppression site under drinkpulse/ carries an inline comment stating why it is concurrency-safe — not just its product purpose (ROADMAP Phase 2 Success Criterion #2)"
    - "No deprecated or soft-deprecated SwiftUI/Foundation API usage exists anywhere the SWIFT_VERSION flip could surface it, confirmed by a clean, warning-free build log (ROADMAP Phase 2 Success Criterion #3, confirmation half — SWIFT6-02)"
    - "ConsumptionEvent.swift's baseName(in:) at lines 164 and 166 compiles unchanged, calling DrinkTypePreset.preset(for:) and VolumeOption.name(in:) across the nonisolated boundary without error"
  artifacts:
    - path: "drinkpulse.xcodeproj/project.pbxproj"
      provides: "App target now targets Swift 6.0 language mode (was 5.0) for both Debug and Release; test targets untouched (already 6.0)"
    - path: "drinkpulse/Features/AddDrink/DrinkTypePreset.swift"
      provides: "The 2 real isolation-gap fixes (nonisolated) that make the flip compile, restoring the file's existing convention"
    - path: "drinkpulse/Services/HealthKitAdapter.swift, UITestHealthStore.swift, UITestNotificationCenter.swift, NotificationScheduling.swift"
      provides: "Existing @unchecked Sendable suppressions now carry explicit concurrency-safety justification comments (doc-only change)"
  key_links:
    - from: "drinkpulse.xcodeproj/project.pbxproj (SWIFT_VERSION = 6.0, app target)"
      to: "drinkpulse/Domain/ConsumptionEvent.swift:164-166"
      via: "compiler now enforces strict concurrency across this exact call chain (baseName(in:) -> DrinkTypePreset.preset(for:) -> VolumeOption.name(in:))"
      pattern: "DrinkTypePreset\\.preset\\(for:"
    - from: "drinkpulse/Features/AddDrink/DrinkTypePreset.swift (nonisolated fixes)"
      to: "drinkpulse/Domain/ConsumptionEvent.swift (baseName(in:))"
      via: "nonisolated-to-nonisolated call compatibility restored"
      pattern: "nonisolated (static )?func"
  prohibitions:
    - statement: "MUST NOT mark ConsumptionEvent, DrinkTemplate, UserProfile, or any SchemaVN snapshot type @MainActor to route around a nonisolated-context compiler error — RESEARCH.md Pattern 2 forbids this because it would break future background-context / CloudKit use of these @Model types"
      verification: flagged-unverified
    - statement: "MUST NOT widen nonisolated / @unchecked Sendable / @preconcurrency usage beyond the 2 nonisolated sites and 4 existing @unchecked Sendable sites this plan explicitly enumerates — RESEARCH.md's Anti-Patterns section flags scope inflation as this phase's biggest risk"
      verification: flagged-unverified
---

<objective>
Flip the drinkpulse app target to real Swift 6 strict concurrency
(`SWIFT_VERSION = 6.0`, Debug + Release), fix the exact 2 compiler errors
this surfaces (missing `nonisolated` on two pure helpers in
`DrinkTypePreset.swift`), justify the 4 pre-existing `@unchecked Sendable`
suppression sites with explicit concurrency-safety comments, and confirm
the deprecated-API sweep (SWIFT6-02) stays clean.

Purpose: this is the phase's tracer — the one vertical slice that proves
the entire codebase compiles clean under the language-mode CLAUDE.md
already claims is true. RESEARCH.md ran this exact flip live and found
only 2 real errors; this plan encodes that finding directly rather than
re-deriving it.
Output: app target builds clean (Debug + Release) under `SWIFT_VERSION = 6.0`;
zero unjustified suppressions; zero deprecated-API hits.
</objective>

<execution_context>
@/Users/fempter/.claude/gsd-core/workflows/execute-plan.md
@/Users/fempter/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md
@.planning/phases/02-swift-6-language-mode-migration/02-VALIDATION.md
@CLAUDE.md
@drinkpulse/Domain/ConsumptionEvent.swift
@drinkpulse/Features/AddDrink/DrinkTypePreset.swift
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Flip SWIFT_VERSION to 6.0 and fix the 2 real isolation-gap errors</name>
  <files>drinkpulse.xcodeproj/project.pbxproj, drinkpulse/Features/AddDrink/DrinkTypePreset.swift</files>
  <precondition>The iOS Simulator "iPhone 17 Pro" is present in this machine's Xcode device list (`xcrun simctl list devices available` includes it) — RESEARCH.md's Environment Availability table confirms this on the research machine; if unavailable here, substitute another available simulator from that same table (17 Pro Max, 17e, Air, 17) for every build/test command in this plan.</precondition>
  <read_first>
    .planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md (Summary; Pitfall 1; Architecture Pattern 1 and Pattern 2; the "exact fix" Code Examples section)
    drinkpulse.xcodeproj/project.pbxproj lines 415-472 (the app target's Debug and Release XCBuildConfiguration blocks — SWIFT_VERSION sits at line 433 in Debug, line 468 in Release, alongside SWIFT_APPROACHABLE_CONCURRENCY and SWIFT_DEFAULT_ACTOR_ISOLATION which stay unchanged)
    drinkpulse/Features/AddDrink/DrinkTypePreset.swift (full file — note line 95's existing `nonisolated static func abvRange(...)` as the convention to mirror, line 20's `func name(in unitSystem:)` inside `struct VolumeOption`, and line 107's `static func preset(for category:)`)
    drinkpulse/Domain/ConsumptionEvent.swift lines 150-172 (`baseName(in:)` — line 164 calls `DrinkTypePreset.preset(for: category)`, line 166 calls `match.name(in: resolvedUnit)`; this method is itself `nonisolated` by inheriting the `@Model` class's default isolation, which is why both callees must be `nonisolated` too)
  </read_first>
  <action>
    In drinkpulse.xcodeproj/project.pbxproj, set the app target's `SWIFT_VERSION` build setting to `6.0` at line 433 (app target, Debug configuration) and at line 468 (app target, Release configuration) — both lines currently pin the pre-flip language mode per RESEARCH.md's verified baseline (see read_first). Do not touch any other `SWIFT_VERSION` line in the file — the test-target configurations (lines 274, 483, 499, 516) are already `6.0` and must stay untouched. Do not change `SWIFT_APPROACHABLE_CONCURRENCY` or `SWIFT_DEFAULT_ACTOR_ISOLATION` on either configuration.
    In drinkpulse/Features/AddDrink/DrinkTypePreset.swift, add the `nonisolated` keyword to exactly 2 declarations, mirroring the already-`nonisolated static func abvRange(from:through:step:)` at line 95 in the same file: (1) at line 20, change `func name(in unitSystem: UnitSystem) -> String` (inside `struct VolumeOption`) to `nonisolated func name(in unitSystem: UnitSystem) -> String`; (2) at line 107, change `static func preset(for category: DrinkCategory) -> DrinkTypePreset` to `nonisolated static func preset(for category: DrinkCategory) -> DrinkTypePreset`. Do not add `nonisolated` to `label(in:)` (line 27) or any other function in the file — RESEARCH.md's live compiler run confirms these are the only 2 sites the Swift 6 compiler flags. Do not mark `ConsumptionEvent` or any other `@Model` class `@MainActor` to work around the error — RESEARCH.md's Pattern 2 forbids this because it would foreclose future background-`ModelContext`/CloudKit use.
  </action>
  <acceptance_criteria>
    - `grep -c "SWIFT_VERSION = 5.0" drinkpulse.xcodeproj/project.pbxproj` returns 0 (no remaining 5.0 app-target configs)
    - `grep -n "SWIFT_VERSION = 6.0" drinkpulse.xcodeproj/project.pbxproj` includes lines 433 and 468 among its matches, and the pre-existing test-target 6.0 lines (274, 483, 499, 516) are still present and unchanged
    - `grep -n "nonisolated func name(in unitSystem" drinkpulse/Features/AddDrink/DrinkTypePreset.swift` matches
    - `grep -n "nonisolated static func preset(for category" drinkpulse/Features/AddDrink/DrinkTypePreset.swift` matches
    - `xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build 2>&1 | grep -E "error:|warning:"` produces zero lines
    - `xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Release build 2>&1 | grep -E "error:|warning:"` produces zero lines
    - `xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseTests` still passes (no regression from the 2 annotation-only changes; `ConsumptionEventTests.swift` already covers `baseName(in:)`/`displayName(in:)` behavior)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build 2>&1 | grep -E "error:|warning:"; xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Release build 2>&1 | grep -E "error:|warning:"</automated>
  </verify>
  <done>The app target's Debug and Release configurations both build cleanly under SWIFT_VERSION = 6.0 with zero errors and zero warnings; the 2 isolation-gap sites in DrinkTypePreset.swift are fixed via nonisolated, restoring the file's existing convention; ConsumptionEvent.swift's call sites at lines 164/166 compile without any change to ConsumptionEvent.swift itself.</done>
  <reversibility rating="reversible">A one-setting build-config flip plus two keyword additions, fully revertible via `git checkout` on either file; no data, schema, or public-API change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Justify the 4 existing @unchecked Sendable suppression sites</name>
  <files>drinkpulse/Services/HealthKitAdapter.swift, drinkpulse/Services/UITestHealthStore.swift, drinkpulse/Services/UITestNotificationCenter.swift, drinkpulse/Services/NotificationScheduling.swift</files>
  <read_first>
    CLAUDE.md "Quality gates" section ("Swift 6 strict-concurrency warnings are fixed at the source, never suppressed") and .planning/ROADMAP.md Phase 2 Success Criterion #2 (no suppression without an inline comment justifying it as a deliberate, reviewed exception)
    .planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md Pattern 3 and Pitfall 2 (the exact gap: existing doc comments explain product purpose, not concurrency safety; the recommended sentence shape and example)
    drinkpulse/Services/HealthKitAdapter.swift lines 1-25 (class doc comment + `final class HealthKitAdapter: HealthWriting, @unchecked Sendable` at line 15; only stored state is two `let` constants)
    drinkpulse/Services/UITestHealthStore.swift lines 1-25 (class doc comment + `@unchecked Sendable` at line 12; mutable `samplesByEvent` dictionary, gated on `-dp_uitest`)
    drinkpulse/Services/UITestNotificationCenter.swift lines 1-25 (class doc comment + `@unchecked Sendable` at line 14; mutable `pending` array, gated on `-dp_uitest`)
    drinkpulse/Services/NotificationScheduling.swift lines 1-30 (protocol + `extension UNUserNotificationCenter: @retroactive @unchecked Sendable {}` at line 20)
  </read_first>
  <action>
    Add one sentence to each of the 4 existing doc comments, stating the concurrency-safety rationale (not the class's product purpose, which is already documented) — do not restructure any type into an actor, add locking, or change any stored property. Use RESEARCH.md Pattern 3's example sentence shape as the template, and mention "concurrency" or "thread-safe"/"thread-safety" explicitly in each: (1) drinkpulse/Services/HealthKitAdapter.swift:15 — its only stored state is two `let` constants (`store`, `alcoholType`), so there is no mutable state a concurrent access could race on. (2) drinkpulse/Services/UITestHealthStore.swift:12 — its mutable `samplesByEvent` dictionary is only ever touched from `HealthService`'s `@MainActor`-confined serialized call chain, and the type is inert (never instantiated) outside a `-dp_uitest` launch. (3) drinkpulse/Services/UITestNotificationCenter.swift:14 — its mutable `pending` array is only ever touched from `ReminderService`'s `@MainActor`-confined calls, and the type is inert outside a `-dp_uitest` launch. (4) drinkpulse/Services/NotificationScheduling.swift:20 — this is a `@retroactive` conformance for an Apple SDK type (`UNUserNotificationCenter`) not yet marked `Sendable` by Apple, and every call site in this codebase routes through `@MainActor`-confined Services only. Do not touch any other line in these 4 files.
  </action>
  <acceptance_criteria>
    - `grep -rn "@unchecked\|@preconcurrency\|nonisolated(unsafe)" drinkpulse/` still returns exactly the same 4 sites as before this task (no new suppression introduced, none removed): `HealthKitAdapter.swift:15`, `UITestHealthStore.swift:12`, `UITestNotificationCenter.swift:14`, `NotificationScheduling.swift:20`
    - `grep -B6 "final class HealthKitAdapter" drinkpulse/Services/HealthKitAdapter.swift | grep -iE "concurrency|thread-safe"` matches
    - `grep -B6 "final class UITestHealthStore" drinkpulse/Services/UITestHealthStore.swift | grep -iE "concurrency|thread-safe"` matches
    - `grep -B6 "final class UITestNotificationCenter" drinkpulse/Services/UITestNotificationCenter.swift | grep -iE "concurrency|thread-safe"` matches
    - `grep -B4 "extension UNUserNotificationCenter: @retroactive" drinkpulse/Services/NotificationScheduling.swift | grep -iE "concurrency|thread-safe"` matches
    - `xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build 2>&1 | grep -E "error:|warning:"` still produces zero lines (comment-only change)
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "@unchecked\|@preconcurrency\|nonisolated(unsafe)" drinkpulse/ | wc -l</automated>
  </verify>
  <done>All 4 existing @unchecked Sendable sites carry an explicit, reviewed concurrency-safety justification sentence, satisfying ROADMAP Phase 2 Success Criterion #2 with zero behavior change.</done>
  <reversibility rating="reversible">Doc-comment-only edits across 4 files; trivially revertible via git, no code path affected.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Confirm zero deprecated APIs (SWIFT6-02); Wave 1 build gate</name>
  <files>None — verification-only task. If a genuine deprecated-API hit is found (not expected per RESEARCH.md), fix that single call site in whichever file it appears and record the deviation in this plan's SUMMARY.</files>
  <read_first>
    .planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md Summary (paragraph 2 — the full grep sweep already run and its zero-hit result) and Pitfall 3 (do not invent migration work beyond what a real hit surfaces)
  </read_first>
  <!-- planner-discipline-allow: deprecated -->
  <action>
    Re-run the deprecated-API grep sweep RESEARCH.md already ran, against the `drinkpulse/` app-target tree only (not drinkpulseTests/drinkpulseUITests): search for `onChange(of:.*perform:`, `NavigationView`, `actionSheet(`, `alert(isPresented:`, `.accentColor(`, `.tabItem`, `Section(header:`, `EnvironmentKey`, `MagnificationGesture`, `RotationGesture`, `coordinateSpace(name:`, `disableAutocorrection`, `UIPasteboard`, `presentationBackground(`. Confirm every pattern returns zero matches, matching RESEARCH.md's confirmed baseline. Then re-run the full Debug and Release builds (same commands as Task 1, now with Task 1 and Task 2's edits both in place) and grep the combined output for the case-insensitive substring `deprecated` — confirm zero lines. If any single grep or build line here surfaces a genuine new finding, fix that one call site to its modern equivalent inline (a narrowly-scoped exception to the "no code change expected" baseline) and note the deviation in the SUMMARY; do not proactively touch anything the sweep does not flag.
  </action>
  <acceptance_criteria>
    - Each of the 13 listed deprecated-API grep patterns returns 0 matches under `drinkpulse/`
    - Combined Debug+Release build output contains zero lines matching `deprecated` (case-insensitive)
    - Debug build: `xcodebuild ... -configuration Debug build 2>&1 | grep -E "error:|warning:"` still produces zero lines
    - Release build: `xcodebuild ... -configuration Release build 2>&1 | grep -E "error:|warning:"` still produces zero lines
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build 2>&1 | grep -iE "error:|warning:|deprecated"</automated>
  </verify>
  <done>SWIFT6-02 confirmed satisfied — zero deprecated/soft-deprecated API usage anywhere the flip could surface, backed by a fresh, clean, warning-free Debug+Release build log. Wave 1 (SWIFT6-01 core + suppression justification + deprecated-API confirmation) is fully verified.</done>
  <reversibility rating="reversible">Verification-only; any contingency fix would itself be a single, small, revertible edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this phase introduces no new trust boundary. It changes compiler
enforcement (a build setting) and internal actor-isolation annotations plus
doc comments only; no new data flow crosses any process, network, storage,
or user-input boundary. Confirmed against RESEARCH.md's Security Domain
section (ASVS V2-V6 not applicable to this phase).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Build configuration (`project.pbxproj` `SWIFT_VERSION`) | low | mitigate | A future edit that silently reverts `SWIFT_VERSION` to `5.0` (or removes it) would silently disable compile-time data-race enforcement with no test failure to catch it. Mitigated by this plan's explicit grep-based check (`SWIFT_VERSION = 6.0` present at lines 433/468, zero `SWIFT_VERSION = 5.0` remaining) re-run at every build gate per 02-VALIDATION.md's sampling rate. |
| T-02-02 | Repudiation | The 4 `@unchecked Sendable` suppression sites | low | mitigate | Undocumented suppressions make a future concurrency regression hard to attribute or review safely. Mitigated by Task 2 adding an explicit, reviewed justification sentence to each of the 4 sites. |

No new attack surface is introduced by this phase — no new data flows, no
new external inputs, no new persisted fields, no new network calls.
</threat_model>

<verification>
Both `xcodebuild ... -configuration Debug build` and `-configuration Release
build` for scheme `drinkpulse` are clean (zero `error:`/`warning:` lines)
with `SWIFT_VERSION = 6.0`; all 4 `@unchecked Sendable` sites carry a
concurrency-safety justification sentence; the 13-pattern deprecated-API
sweep returns zero hits; `-only-testing:drinkpulseTests` still passes.
</verification>

<success_criteria>
The app target compiles clean under real Swift 6 strict concurrency in both
build configurations, with no suppression left unjustified and no
deprecated API left unconfirmed — ROADMAP Phase 2 Success Criteria #1, #2,
and the confirmation half of #3 (SWIFT6-02) are satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-swift-6-language-mode-migration/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

No new files and no new public symbols are produced by this plan — it is
entirely edits to existing files:

- `drinkpulse.xcodeproj/project.pbxproj` — modified (`SWIFT_VERSION` build
  setting only, app target Debug + Release: `5.0` → `6.0`)
- `drinkpulse/Features/AddDrink/DrinkTypePreset.swift` — modified
  (`nonisolated` added to 2 existing declarations: `VolumeOption.name(in:)`,
  `DrinkTypePreset.preset(for:)`; no new declarations)
- `drinkpulse/Services/HealthKitAdapter.swift`,
  `UITestHealthStore.swift`, `UITestNotificationCenter.swift`,
  `NotificationScheduling.swift` — modified (doc comments only, no new
  symbols, no behavior change)
