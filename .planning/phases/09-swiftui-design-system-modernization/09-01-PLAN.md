---
phase: 09-swiftui-design-system-modernization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - drinkpulse/DesignSystem/DPArcProgress.swift
  - .planning/phases/09-swiftui-design-system-modernization/09-DASHBOARD-EVIDENCE.md
autonomous: true
requirements: [MOD-02]
estimate: {tokens: 20000, raw_tokens: 20000, tasks: 2, confidence: low}
must_haves:
  truths:
    - The Dashboard's existing arc component compiles under iOS 27 Debug and Release without changing its sweep geometry, value animation, or accessibility label.
    - A seeded Dashboard flow still renders the hero value after the compiler repair, proved by its focused XCUITest rather than an assumed full-suite result.
    - A human visual and VoiceOver inspection records the changed Dashboard surface, toolchain, simulator, and outcome.
  artifacts:
    - path: drinkpulse/DesignSystem/DPArcProgress.swift
      provides: Strict-concurrency-correct SwiftUI Shape boundary retaining the Dashboard arc contract.
    - path: .planning/phases/09-swiftui-design-system-modernization/09-DASHBOARD-EVIDENCE.md
      provides: Focused automated and human evidence for the arc repair.
  key_links:
    - from: drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift
      to: drinkpulse/DesignSystem/DPArcProgress.swift
      via: DashboardHeroCard renders DPArcProgress with todayPct and the existing risk color.
      pattern: "DPArcProgress\\(pct: vm\\.todayPct"
    - from: drinkpulseUITests/Features/Dashboard/DashboardUITests.swift
      to: drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift
      via: Seeded Dashboard UI assertions observe the hero's accessibility label and post-save value.
      pattern: "Today's Intake"
---

<objective>
Repair the app-owned DPArcProgress Shape-isolation diagnostic through one Dashboard user path, then record the visual and accessibility evidence.
Purpose: MOD-02 cannot be honestly verified while the app target fails to compile, and D-13 requires Debug and Release proof before Dashboard coverage.
Output: a compiler-correct arc component and a durable Dashboard evidence record.
</objective>

<execution_context>
@/Users/fempter/.codex/gsd-core/workflows/execute-plan.md
@/Users/fempter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-swiftui-design-system-modernization/09-CONTEXT.md
@.planning/phases/09-swiftui-design-system-modernization/09-RESEARCH.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY-UI.md
@drinkpulse/DesignSystem/DPArcProgress.swift
@drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift
@drinkpulseUITests/Features/Dashboard/DashboardUITests.swift

<interfaces>
From drinkpulse/DesignSystem/DPArcProgress.swift: `DPArcProgress` accepts `pct: Double`, `color: Color`, optional `size` and `strokeWidth`; its private `ArcShape: Shape` draws the fixed 60-degree start and 240-degree sweep.
From drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift: `arcWithLabel` constructs `DPArcProgress(pct: vm.todayPct, color: arcColor, size: 100, strokeWidth: 9)` inside the combined accessible hero card.
From drinkpulseUITests/Features/Dashboard/DashboardUITests.swift: `DashboardUITests` launches deterministic seeded UI data and already asserts the Dashboard hero label and post-save visible value.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Repair the Dashboard arc's Shape boundary through Debug, Release, and its seeded UI path</name>
  <reversibility rating="reversible">The isolation repair is local to a private drawing helper; undoing it affects no stored data, public API, or shell ownership.</reversibility>
  <read_first>drinkpulse/DesignSystem/DPArcProgress.swift; drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift; drinkpulseUITests/Features/Dashboard/DashboardUITests.swift; .planning/phases/08-ios-27-baseline-api-inventory/08-BASELINE.md; .planning/phases/09-swiftui-design-system-modernization/09-RESEARCH.md; Apple Shape documentation https://developer.apple.com/documentation/swiftui/shape</read_first>
  <files>drinkpulse/DesignSystem/DPArcProgress.swift</files>
  <action>Per D-12 and D-13, repair only the Xcode 27 strict-concurrency boundary for the private `ArcShape` conformance. Use the smallest compiler-approved nonisolated declaration or lexical separation that keeps the helper file-local and retains the current `startDeg`, `sweepDeg`, `Path.addArc` center/radius, round stroke usage, `hasSettledOnce` animation rule with `value: pct`, frame, and `arcLabel` behavior. Do not redesign the renderer or alter DashboardHeroCard. Before verification, inspect available devices with `xcrun simctl list devices available`, set `LOCAL_IOS_27_UDID` to an available iOS 27 simulator from that output, and boot it with `xcrun simctl bootstatus "$LOCAL_IOS_27_UDID" -b`. First obtain clean Debug and Release builds; only after both succeed, run the existing seeded `DashboardUITests` as the end-to-end tracer. Keep the full suite out of this per-change gate and use no package, network, telemetry, or logging addition.</action>
  <verify>
    <automated>xcrun simctl list devices available && test -n "${LOCAL_IOS_27_UDID:?Set LOCAL_IOS_27_UDID to an available iOS 27 simulator from the listing}" && xcrun simctl bootstatus "$LOCAL_IOS_27_UDID" -b && xcodebuild build -project drinkpulse.xcodeproj -scheme drinkpulse -configuration Debug -destination "platform=iOS Simulator,id=$LOCAL_IOS_27_UDID" && xcodebuild build -project drinkpulse.xcodeproj -scheme drinkpulse -configuration Release -destination "platform=iOS Simulator,id=$LOCAL_IOS_27_UDID"</automated>
    <fails_when>The selected simulator is unavailable or cannot boot, or either Debug/Release build exits nonzero or reports a warning, including the Shape isolation diagnostic.</fails_when>
    <automated>xcrun simctl list devices available && test -n "${LOCAL_IOS_27_UDID:?Set LOCAL_IOS_27_UDID to an available iOS 27 simulator from the listing}" && xcrun simctl bootstatus "$LOCAL_IOS_27_UDID" -b && xcodebuild test -project drinkpulse.xcodeproj -scheme drinkpulse -configuration Debug -destination "platform=iOS Simulator,id=$LOCAL_IOS_27_UDID" -only-testing:drinkpulseUITests/DashboardUITests</automated>
    <fails_when>The focused Dashboard test target does not start, a Dashboard assertion fails, or the command exits nonzero.</fails_when>
    <automated>test "$(wc -l < drinkpulse/DesignSystem/DPArcProgress.swift)" -le 300</automated>
    <fails_when>The changed Swift file exceeds the 300-line project limit.</fails_when>
  </verify>
  <acceptance_criteria>D-12/D-13: Debug and Release both build clean on the selected local iOS 27 destination before the focused Dashboard suite runs; the seeded hero path remains green; the arc's geometry, animation, and accessibility-label contract remain intact; no full-suite pass is claimed.</acceptance_criteria>
  <done>DPArcProgress has a compiler-correct private Shape boundary, both build configurations are clean, and DashboardUITests proves one real user-visible path.</done>
</task>

<task type="auto">
  <name>Record the Dashboard visual and accessibility confirmation for the arc repair</name>
  <read_first>drinkpulse/DesignSystem/DPArcProgress.swift; drinkpulse/Features/Dashboard/Components/DashboardHeroCard.swift; drinkpulseUITests/Features/Dashboard/DashboardUITests.swift; the successful Debug, Release, and focused Dashboard command output from Task 1</read_first>
  <files>.planning/phases/09-swiftui-design-system-modernization/09-DASHBOARD-EVIDENCE.md</files>
  <action>Per D-12 and D-13, create `09-DASHBOARD-EVIDENCE.md` with the date, Xcode/SDK/runtime and selected simulator identity, exact focused commands and truthful results from Task 1. Record the checked source paths, the protected arc geometry (60-degree start and 240-degree sweep), value-animation behavior, and accessibility-label outcome. Document the required human check separately from automation: launch seeded Home, inspect the arc before and after logging a drink for a continuous correct sweep, then use VoiceOver to confirm the Dashboard hero remains understandable and the progress label is present. Use synthetic seeded data only and record a failure as open evidence rather than changing additional code.</action>
  <verify>
    <automated>python3 -c 'from pathlib import Path; p=Path(".planning/phases/09-swiftui-design-system-modernization/09-DASHBOARD-EVIDENCE.md"); s=p.read_text(); required=("DPArcProgress.swift", "DashboardHeroCard.swift", "DashboardUITests", "Debug", "Release", "60", "240", "VoiceOver", "Human check", "LOCAL_IOS_27_UDID"); assert all(x in s for x in required), [x for x in required if x not in s]; print("Dashboard arc evidence has required provenance and human-check fields")'</automated>
    <fails_when>The evidence file lacks a checked path, local iOS 27 environment, Debug/Release result, protected arc behavior, or a distinct human accessibility outcome.</fails_when>
    <human_check>On the selected iOS 27 simulator, open seeded Home, log one drink, and visually confirm the circular arc retains its start/sweep and intended settled animation. With VoiceOver enabled, move through the Dashboard hero and confirm its progress information is announced meaningfully. Write pass, fail, or unavailable with the observation in `09-DASHBOARD-EVIDENCE.md`.</human_check>
  </verify>
  <acceptance_criteria>D-12/D-13: the affected visual and accessibility behavior has a durable, human-distinguished record tied to the actual iOS 27 build and focused Dashboard result.</acceptance_criteria>
  <done>The Dashboard evidence record distinguishes automation from human observation and preserves the arc behavior that the compiler repair was required to retain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SwiftUI drawing helper to Dashboard accessibility tree | A concurrency repair could alter the displayed risk indicator or make its accessible information unavailable. |
| Local simulator/build output to committed evidence | An unselected runtime or incomplete result can create a false iOS 27 claim. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-01 | Tampering | `DPArcProgress` rendering and label | high | mitigate | Limit the change to the Shape isolation boundary; build Debug and Release, run DashboardUITests, and retain visual/VoiceOver evidence in `09-DASHBOARD-EVIDENCE.md`. |
| T-09-02 | Repudiation | iOS 27 verification result | medium | mitigate | Record selected runtime, exact commands, source paths, automated result, and human result separately. |
| T-09-SC | Tampering | Package installation surface | low | accept | This plan authorizes no dependency installation; native SwiftUI, XCTest, and Xcode are already in the project. |
</threat_model>

<verification>
Confirm the isolated helper remains local, Debug and Release output is clean, the scoped Dashboard suite passed on a locally selected iOS 27 simulator, and the evidence record includes the distinct human check. Do not represent this focused path as a full-suite result.
</verification>

<success_criteria>
MOD-02's first blocked interface path is restored: the Dashboard compiles cleanly, preserves its visual/accessibility arc contract, and has focused iOS 27 evidence.
</success_criteria>

<output>
Create `.planning/phases/09-swiftui-design-system-modernization/09-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- `drinkpulse/DesignSystem/DPArcProgress.swift` with the narrow strict-concurrency repair.
- `09-DASHBOARD-EVIDENCE.md` with Dashboard automated and human verification provenance.
