---
context: phase
phase: 09-swiftui-design-system-modernization
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-09-18T19:46:20.747Z
---

# Critical Anti-Patterns

| Pattern | Description | Severity | Prevention mechanism |
|---|---|---|---|
| Trusting the Xcode MCP test timeout as a test outcome | `RunSomeTests` returns after a fixed 300 seconds for long UI suites without their final result. | blocking | Run long suites with persistent CLI `xcodebuild` and poll the terminal session until it exits; record only its final result. |
| Treating every `.indices` occurrence as unstable view identity | `HistoryCalendarView` uses `rotated.indices` only to construct seven `WeekdayLabel(id:)` values; its rendered loops use Identifiable values. | advisory | Audit the actual `ForEach` operand and IDs before changing protected History code. |

<current_state>

Phase 09 is executing. Plans 01 and 02 are complete. Plan 03, retain evidence,
is paused in automated task 1 before `09-RETAIN-EVIDENCE.md` exists.

The owner confirmed a repeated Dashboard flash on real iPhone 15 Pro Max/iOS 27
after Add Drink dismissal followed by any tab switch. It remains after the
`GlassEffectContainer` change and is explicitly deferred to the captured major
todo; do not expand Phase 09 to diagnose it.
</current_state>

<completed_work>

- Plan 09-01: strict-concurrency Dashboard repair and evidence — complete.
- Plan 09-02: sheet-root `DismissAction`, UI and VoiceOver regressions, owner
  confirmation — complete.
- Dashboard Glass containment exploratory correction — `553ea58`; real-device
  report says it did not resolve the flash, now deferred.
- Dashboard flash captured through `$gsd-capture` — `6f97645`.
- Plan 09-03 source audit: `@Observable @MainActor`/`@State` ownership is
  present across Dashboard, History, Insights, and Onboarding. Calendar event
  rows retain `uuid`; weekday label indices are not rendered as positional IDs.
</completed_work>

<remaining_work>

1. Run Plan 09-03's focused UI suites. Start with the slow History classes in
   a persistent CLI session, not Xcode MCP. Then run other classes in small,
   separately reported groups. Do not call a timeout pass/fail.
2. Write `09-RETAIN-EVIDENCE.md` with every UI-C-01…UI-C-10 disposition,
   automated outcomes, source audit, no-replacement rationale, and the
   explicitly disabled spec-less probe fallback.
3. Complete the Plan 09-03 human checks; failures must be recorded and
   escalated, not fixed speculatively.
4. Create `09-03-SUMMARY.md`, commit evidence, then use phase completion
   workflow only after all required manual evidence is honest and complete.
</remaining_work>

<decisions_made>

- Apple/Xcode MCP documentation is the API source of truth; third-party skills
  only supplement it. This is persisted in root `AGENTS.md` and `CLAUDE.md`
  redirects there.
- Add Drink pass confirmation is valid; the Dashboard flash is a separate
  deferred defect, not an Add Drink acceptance failure.
- Plan 09-03 retains protected History, Insights, Observation, motion, and
  Glass behavior unless fresh evidence authorizes a narrow correction.
</decisions_made>

<blockers>

- Xcode MCP has a fixed 300-second backend timeout for long UI test batches.
  It also leaves the scheme busy for a period after timing out. Use CLI for
  long suites next session.
- Human evidence for Plan 09-03 remains outstanding.
</blockers>

## Required reading

1. `AGENTS.md` — durable Apple/Xcode MCP-first policy and project rules.
2. `.planning/HANDOFF.json` — machine-readable resume state.
3. `.planning/phases/09-swiftui-design-system-modernization/09-03-PLAN.md` — remaining requirements.
4. `.planning/phases/09-swiftui-design-system-modernization/09-CONTEXT.md` — decisions and guardrails.
5. `.planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY-UI.md` — candidate meanings.
6. `.planning/todos/pending/2026-09-18-diagnose-dashboard-flash-after-sheet-dismissal.md` — deferred real-device defect.

## Infrastructure state

- Xcode MCP workspace: `workspace-i1kveRcKLS`; target simulator: iPhone 18 Pro,
  iOS 27, UDID `1D35E1B8-4141-4EFF-A493-52CB37B600A5`.
- No intentional running server or async job.
- Preserve unrelated dirty files in `.planning/config.json`, `.planning/graphs/`,
  `.gsd/`, `.planning/research/.cache/`, and `.planning/state.json`.

<next_action>

Start with `$gsd-resume-work`. After reading this handoff, run the long History
test classes with CLI `xcodebuild` in a persistent session and poll until it
finishes; then continue Plan 09-03 evidence.
</next_action>
