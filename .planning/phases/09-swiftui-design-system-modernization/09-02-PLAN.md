---
phase: 09-swiftui-design-system-modernization
plan: "02"
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - drinkpulse/Features/AddDrink/AddDrinkView.swift
  - drinkpulse/Features/AddDrink/DrinkTypeGridView.swift
  - drinkpulse/Features/AddDrink/DrinkDetailInputView.swift
  - drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift
  - drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift
  - drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift
  - .planning/phases/09-swiftui-design-system-modernization/09-ADD-DRINK-EVIDENCE.md
autonomous: true
requirements: [MOD-02]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 2, confidence: low}
must_haves:
  truths:
    - Cancel and successful save close the Add Drink sheet to the originating tab, not merely to the type grid, while the successful save still persists the event.
    - The Add Drink flow reads the standard sheet-root SwiftUI DismissAction and deliberately propagates that action through the existing navigation route.
    - The dismissal control remains reachable by its semantic Cancel label, and one durable Xcode 27 VoiceOver UI regression exercises the meaningful dismissal flow.
    - Add Drink visual and accessibility verification is documented separately from automated UI-test results.
  artifacts:
    - path: drinkpulse/Features/AddDrink/AddDrinkView.swift
      provides: Sheet-root ownership of the framework dismissal action.
    - path: drinkpulse/Features/AddDrink/DrinkTypeGridView.swift
      provides: Navigation handoff of the sheet-root dismissal action.
    - path: drinkpulse/Features/AddDrink/DrinkDetailInputView.swift
      provides: Detail cancel behavior using the propagated action.
    - path: drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift
      provides: Existing mutation sequence followed by propagated sheet dismissal.
    - path: drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift
      provides: Independent cancel and successful-save return-to-originating-tab regressions.
    - path: drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift
      provides: Xcode 27 VoiceOver dismissal-control regression retaining semantic assertions.
    - path: .planning/phases/09-swiftui-design-system-modernization/09-ADD-DRINK-EVIDENCE.md
      provides: Human visual and accessibility outcome for the changed sheet flow.
  key_links:
    - from: drinkpulse/Features/Shell/RootShellView.swift
      to: drinkpulse/Features/AddDrink/AddDrinkView.swift
      via: "RootShellView retains ownership of `.sheet(isPresented: $showAddDrink)` and installs AddDrinkView as its presented content."
      pattern: "\\.sheet\\(isPresented: \\$showAddDrink\\)"
    - from: drinkpulse/Features/AddDrink/AddDrinkView.swift
      to: drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift
      via: One sheet-root DismissAction passes through DrinkTypeGridView to the detail form's cancel and post-mutation save path.
      pattern: "DismissAction"
---

<objective>
Replace Add Drink's closure-valued environment handoff with the sheet-root SwiftUI dismissal action, then prove cancel, save, and VoiceOver return the user to the tab that opened the sheet.
Purpose: MOD-02 and D-01 through D-03 require the documented platform action without changing RootShellView ownership, navigation, data persistence, or accessibility semantics.
Output: framework dismissal wiring, focused Add Drink and VoiceOver UI regressions, and a human verification record.
</objective>

<execution_context>
@/Users/fempter/.codex/gsd-core/workflows/execute-plan.md
@/Users/fempter/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-swiftui-design-system-modernization/09-CONTEXT.md
@.planning/phases/09-swiftui-design-system-modernization/09-RESEARCH.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-DECISION-REGISTER.md
@.planning/phases/08-ios-27-baseline-api-inventory/08-INVENTORY-UI.md
@drinkpulse/Features/Shell/RootShellView.swift
@drinkpulse/Features/AddDrink/AddDrinkView.swift
@drinkpulse/Features/AddDrink/DrinkTypeGridView.swift
@drinkpulse/Features/AddDrink/DrinkDetailInputView.swift
@drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift
@drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift
@drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift

<interfaces>
From drinkpulse/Features/Shell/RootShellView.swift: `selectedTab` and `showAddDrink` remain private state; `RootShellView` presents `AddDrinkView()` via `.sheet(isPresented: $showAddDrink)` and is not modified by this plan.
From drinkpulse/Features/AddDrink/DrinkTypeGridView.swift: the value-based `navigationDestination(item: $selection)` constructs `DrinkDetailInputView(preset:)`; the propagated sheet action must traverse this existing seam.
From drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift: `save()` constructs `ConsumptionEvent`, inserts it, calls `RecordDeduplicator.ensureUniqueIdentity`, performs `HealthWriteHooks.write`, then dismisses. That order is invariant.
From drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift and `ShellNavigationUITests.swift`: tests are `@MainActor XCTestCase` suites with seeded launch arguments, semantic label queries, and five-second waits.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Propagate the sheet-root dismissal action through Add Drink and prove cancel plus save from originating tabs</name>
  <reversibility rating="costly">The typed handoff spans the presented root, grid, detail form, logic extension, and UI regression contract, but it has no persisted or public interface consequence and can be reverted locally.</reversibility>
  <read_first>drinkpulse/Features/Shell/RootShellView.swift; drinkpulse/Features/AddDrink/AddDrinkView.swift; drinkpulse/Features/AddDrink/DrinkTypeGridView.swift; drinkpulse/Features/AddDrink/DrinkDetailInputView.swift; drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift; drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift; Apple DismissAction documentation https://developer.apple.com/documentation/swiftui/environmentvalues/dismiss</read_first>
  <files>drinkpulse/Features/AddDrink/AddDrinkView.swift, drinkpulse/Features/AddDrink/DrinkTypeGridView.swift, drinkpulse/Features/AddDrink/DrinkDetailInputView.swift, drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift, drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift</files>
  <action>Per D-01, obtain the standard `DismissAction` at the presented `AddDrinkView` root and pass that same typed action as `dismissSheet` through `DrinkTypeGridView` and its existing `navigationDestination` into `DrinkDetailInputView`; remove the closure-valued custom environment path. Use that propagated `dismissSheet` action for the grid cancellation, the detail cancellation, and only the final dismissal after the current `save()` mutation sequence. Preserve `RootShellView` ownership, the `NavigationStack`, the detail-local `ModelContext`, `@Query`, Health service, every form field, localized control label, SwiftData construction/insertion, identity de-duplication, and Health write order. Per D-02 and D-03, extend the existing XCTest suite with separately named deterministic cases: one opens Add Drink from a non-Home tab and cancels; one opens from a non-Home tab, saves a uniquely named drink, confirms the sheet disappears, confirms the originating tab remains visible, then confirms persistence in History. Both cases assert the existing semantic Cancel or Save control exists before interaction. Do not replace the existing saved-event assertion with a dismissal-only assertion.</action>
  <verify>
    <automated>xcrun simctl list devices available && test -n "${LOCAL_IOS_27_UDID:?Set LOCAL_IOS_27_UDID to an available iOS 27 simulator from the listing}" && xcrun simctl bootstatus "$LOCAL_IOS_27_UDID" -b && xcodebuild test -project drinkpulse.xcodeproj -scheme drinkpulse -configuration Debug -destination "platform=iOS Simulator,id=$LOCAL_IOS_27_UDID" -only-testing:drinkpulseUITests/AddDrinkFlowUITests</automated>
    <fails_when>The selected simulator is unavailable or cannot boot, the Add Drink test target cannot compile, or either independent cancel/save behavior assertion fails.</fails_when>
    <automated>python3 -c 'from pathlib import Path; root=Path("drinkpulse/Features/AddDrink/AddDrinkView.swift").read_text(); grid=Path("drinkpulse/Features/AddDrink/DrinkTypeGridView.swift").read_text(); detail=Path("drinkpulse/Features/AddDrink/DrinkDetailInputView.swift").read_text(); logic=Path("drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift").read_text(); tests=Path("drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift").read_text(); assert "DismissAction" in root and "DismissAction" in grid and "DismissAction" in detail; assert "@Entry" not in root and ".environment(" not in root; assert logic.index("modelContext.insert(event)") < logic.index("RecordDeduplicator.ensureUniqueIdentity") < logic.index("HealthWriteHooks.write") < logic.rindex("dismissSheet()"); assert tests.count("originating") >= 2; print("Sheet-root dismissal wiring and independent originating-tab tests are present")'</automated>
    <fails_when>The custom closure path remains at the presented root, the typed action is not carried through the navigation route, save ordering changes, or separately named originating-tab coverage is missing.</fails_when>
    <automated>test "$(wc -l < drinkpulse/Features/AddDrink/AddDrinkView.swift)" -le 300 && test "$(wc -l < drinkpulse/Features/AddDrink/DrinkTypeGridView.swift)" -le 300 && test "$(wc -l < drinkpulse/Features/AddDrink/DrinkDetailInputView.swift)" -le 300 && test "$(wc -l < drinkpulse/Features/AddDrink/DrinkDetailInputView+Logic.swift)" -le 300 && test "$(wc -l < drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift)" -le 300</automated>
    <fails_when>Any changed Swift or XCTest file exceeds the 300-line project limit.</fails_when>
  </verify>
  <acceptance_criteria>D-01/D-02/D-03/D-12: cancel and successful save independently dismiss the entire sheet back to their originating tabs, save still persists the event after the established mutation sequence, and semantic dismissal-control assertions remain in the existing XCTest style.</acceptance_criteria>
  <done>The custom closure environment handoff is replaced by a compiler-proven sheet-root DismissAction route, and focused Add Drink UI tests prove cancel, save, persistence, and originating-tab return.</done>
</task>

<task type="auto">
  <name>Add the durable Xcode 27 VoiceOver regression and record Add Drink human evidence</name>
  <read_first>drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift; drinkpulseUITests/Features/AddDrink/AddDrinkFlowUITests.swift; drinkpulse/Features/Shell/RootShellView.swift; drinkpulse/Features/AddDrink/AddDrinkView.swift; Apple XCUIVoiceOverService documentation https://developer.apple.com/documentation/xcuiautomation/xcuivoiceoverservice; Apple DismissAction documentation https://developer.apple.com/documentation/swiftui/environmentvalues/dismiss</read_first>
  <files>drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift, .planning/phases/09-swiftui-design-system-modernization/09-ADD-DRINK-EVIDENCE.md</files>
  <action>Per D-03 and D-14, add one durable XCTest in `ShellNavigationUITests` for the meaningful Add Drink dismissal flow using Xcode 27's `XCUIDevice.shared.voiceOverService`. Use the compiler-confirmed service methods to enable VoiceOver, navigate to the sheet dismissal control, and assert its focused or spoken output identifies the existing localized Cancel control before exercising dismissal; reliably disable the service in cleanup. Retain the existing ordinary semantic element assertions as independent checks, and assert that the originating tab remains after the sheet closes. Create `09-ADD-DRINK-EVIDENCE.md` with the selected local runtime, commands/results, exact changed paths, and a distinct human visual/accessibility result. The human check must cover opening from a non-Home tab, grid cancel, detail cancel, successful save, return to the same tab, control labels, and VoiceOver focus; record failures without expanding navigation or data scope.</action>
  <verify>
    <automated>xcrun simctl list devices available && test -n "${LOCAL_IOS_27_UDID:?Set LOCAL_IOS_27_UDID to an available iOS 27 simulator from the listing}" && xcrun simctl bootstatus "$LOCAL_IOS_27_UDID" -b && xcodebuild test -project drinkpulse.xcodeproj -scheme drinkpulse -configuration Debug -destination "platform=iOS Simulator,id=$LOCAL_IOS_27_UDID" -only-testing:drinkpulseUITests/ShellNavigationUITests</automated>
    <fails_when>The selected simulator is unavailable or cannot boot, the shell test target does not compile against Xcode 27 VoiceOver APIs, the VoiceOver test fails, semantic cancel discovery fails, or the originating tab is not restored.</fails_when>
    <automated>python3 -c 'from pathlib import Path; test=Path("drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift").read_text(); evidence=Path(".planning/phases/09-swiftui-design-system-modernization/09-ADD-DRINK-EVIDENCE.md").read_text(); required_test=("voiceOverService", "Cancel", "History"); required_evidence=("AddDrinkView.swift", "DrinkTypeGridView.swift", "DrinkDetailInputView.swift", "AddDrinkFlowUITests", "ShellNavigationUITests", "VoiceOver", "Human check", "LOCAL_IOS_27_UDID"); assert all(x in test for x in required_test), [x for x in required_test if x not in test]; assert all(x in evidence for x in required_evidence), [x for x in required_evidence if x not in evidence]; print("VoiceOver regression and Add Drink evidence are durable")'</automated>
    <fails_when>The test omits the Xcode 27 VoiceOver service or semantic control label, or the evidence record lacks changed-path provenance, an automated outcome, or a human outcome.</fails_when>
    <automated>test "$(wc -l < drinkpulseUITests/Features/Shell/ShellNavigationUITests.swift)" -le 300</automated>
    <fails_when>The changed Shell UI-test file exceeds the 300-line project limit.</fails_when>
    <human_check>On the selected iOS 27 simulator with VoiceOver enabled, start from History or Settings, open Add Drink, verify the visible and announced Cancel control, cancel from the grid, cancel from a detail screen, then save a seeded entry. Confirm every dismissal returns to the same originating tab and save still creates the event. Record pass, fail, or unavailable with observations in `09-ADD-DRINK-EVIDENCE.md`.</human_check>
  </verify>
  <acceptance_criteria>D-03/D-12/D-14: a single durable Xcode 27 VoiceOver test adds assistive-technology coverage without removing semantic UI assertions, and the changed Add Drink flow has a documented visual/accessibility check.</acceptance_criteria>
  <done>The Sheet-root migration has focused semantic, VoiceOver, and human evidence that cancel/save preserve navigation and persistence behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Sheet-root presentation environment to pushed detail view | A wrong dismissal action can pop navigation instead of closing the sheet or permit an unintended save outcome. |
| UI automation and VoiceOver service to user-visible controls | A control can remain visible while inaccessible or have an incorrect semantic label. |
| Test fixture output to planning evidence | Test diagnostics could expose user-health content if non-synthetic data is captured. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-09-03 | Tampering | Add Drink save/cancel dismissal route | high | mitigate | Capture the sheet-root action, preserve mutation-before-dismissal order, and run independent cancel/save originating-tab XCUITests with a persistence assertion. |
| T-09-04 | Denial of service | Dismissal-control accessibility | high | mitigate | Preserve semantic label assertions and add the D-14 Xcode 27 VoiceOver service regression plus human VoiceOver inspection. |
| T-09-05 | Information disclosure | UI-test and evidence output | low | accept | Use existing deterministic synthetic fixtures and record results/paths, not drink notes, health values, personal data, or raw logs. |
| T-09-SC | Tampering | Package installation surface | low | accept | No package installation is authorized; the action uses the installed SwiftUI and XCTest/XCUITest stack only. |
</threat_model>

<verification>
Confirm RootShellView still owns sheet state, typed dismissal originates only at the presented root, the saved-event mutation sequence precedes sheet dismissal, both focused suites pass on a locally selected iOS 27 simulator, and the human outcome is recorded distinctly.
</verification>

<success_criteria>
MOD-02's approved Add Drink replacement is behavior-preserving: users dismiss from grid or detail to the tab that opened the sheet, saving still persists data, and semantic plus VoiceOver coverage remains durable.
</success_criteria>

<output>
Create `.planning/phases/09-swiftui-design-system-modernization/09-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

- The sheet-root `DismissAction` propagation across the existing Add Drink navigation path.
- Focused Add Drink and Shell XCUITest coverage, including one Xcode 27 VoiceOver regression.
- `09-ADD-DRINK-EVIDENCE.md` with the required human verification record.
