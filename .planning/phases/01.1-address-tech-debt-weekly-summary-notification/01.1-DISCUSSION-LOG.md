# Phase 01.1: Address tech debt: weekly summary notification - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-21
**Phase:** 01.1-Address tech debt: weekly summary notification
**Areas discussed:** Warm-launch tap test strategy, Onboarding toggle-off cancel() gap, Onboarding scheduling UX (ENGG-02)

---

## Warm-launch tap test strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Hidden test-only trigger button | Add an accessibility-identifier-tagged, -dp_uitest-gated hidden button that posts NotificationActionHandler.didTapWeeklySummary in-process; UI test taps it while running, asserts landing on Insights. | |
| Leave as documented gap | Audit already flagged this as a WARNING, not a BLOCKER — cold-launch path (ENGG-07's actual requirement) is already covered by WeeklySummaryTapUITests. Skip adding warm-path coverage this phase. | ✓ |
| You decide | Claude's discretion during planning. | |

**User's choice:** Leave as documented gap
**Notes:** No fix this phase; captured as D-01 in CONTEXT.md.

---

## Onboarding toggle-off cancel() gap

| Option | Description | Selected |
|--------|-------------|----------|
| Add the cancel() call | One-line fix in HealthStep.weeklySummaryToggleBinding's off-branch — call WeeklySummaryService().cancel(), matching WeeklySummarySection. | ✓ |
| Leave as documented gap | No observable behavior change since onboarding never schedules. Skip the fix. | |

**User's choice:** Add the cancel() call
**Notes:** Captured as D-02.

| Option | Description | Selected |
|--------|-------------|----------|
| No new test needed | Existing onboarding UI tests already exercise the toggle-off path and will keep passing. | |
| Add a service-call assertion | Wire a test double / spy for WeeklySummaryService so a test can assert cancel() was invoked on toggle-off. | ✓ |

**User's choice:** Add a service-call assertion
**Notes:** Captured as D-03. Open implementation question (environment injection vs. UI-test-level verification) left to researcher/planner.

---

## Onboarding scheduling UX (ENGG-02)

| Option | Description | Selected |
|--------|-------------|----------|
| Leave as-is, no change | Accepted, intentional D-01 convention from Phase 1 — a first-week onboarding user resolves to .skip anyway, so no visible discrepancy in practice. | ✓ |
| Add a copy hint | Add inline text near the toggle clarifying it activates next time the app opens. | |

**User's choice:** Leave as-is, no change
**Notes:** Captured as D-04. Confirmed not a bug — matches Phase 1's existing D-01 decision.

---

## Claude's Discretion

- Whether `HealthStep` needs `WeeklySummaryService` injected via `@Environment` (mirroring `healthService`) to make the D-03 service-call assertion testable, or whether the existing `UITestNotificationCenter` mock-center pattern is sufficient without changing `HealthStep`'s injection style.

## Deferred Ideas

None — discussion stayed within the 3 audited tech-debt items.
