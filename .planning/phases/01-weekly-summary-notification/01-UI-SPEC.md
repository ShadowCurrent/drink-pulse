---
phase: 1
slug: weekly-summary-notification
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-20
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Scope Note

DrinkPulse is a native SwiftUI iOS 26 app (CLAUDE.md: SwiftUI only, no UIKit,
Dynamic Type up to AX5 mandatory). There is no web design-system tooling
(shadcn / Tailwind / component registries) — the **shadcn gate does not
apply** to this project. This contract instead documents the project's
existing native `DesignSystem/` tokens and the exact `Features/Settings` +
`Features/Onboarding` component patterns this phase must mirror, per
CONTEXT.md D-05/D-07 ("mirror the existing pattern, don't invent a new
one") and RESEARCH.md's confirmation that every visual pattern this phase
needs already ships in the codebase.

This phase touches exactly **three** real UI surfaces — no new screen is
created:

1. **Settings → new `WeeklySummarySection` card** (mirrors `ReminderSection`)
2. **Onboarding → `HealthStep` gets a second, independent toggle row** (mirrors the existing Health toggle block, D-05)
3. **Notification banner copy** (title + 5 body variants — the only genuinely new content in this phase; no notification "screen" exists to design, per Apple's fixed banner chrome)

The **tap destination** (Insights tab) requires zero visual changes — D-04
locks "open Insights at its default state," so no deep-link UI, no new
component. It is listed here only as an interaction contract, not a visual
one.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — native SwiftUI project; shadcn/web tooling not applicable |
| Preset | not applicable |
| Component library | Project-internal `DesignSystem/` + `Features/Settings/Components/` (SwiftUI, no external UI package) |
| Icon library | SF Symbols (system-provided, no custom icon set) |
| Font | San Francisco (system font) via SwiftUI semantic text styles — Dynamic Type compliant, per CLAUDE.md AX5 requirement. **No literal pixel sizes** — this phase must use `Font` text-style roles (`.body`, `.footnote`, `.headline`, `.largeTitle`), never `.system(size:)` |

**Existing tokens this phase reuses (do not invent new ones):**

| Token | Source | Value | Use in this phase |
|-------|--------|-------|--------------------|
| `Color.dpAccent` | `DesignSystem/DPBrand.swift` | Alias for `Color.accentColor` (app's "Ember" AccentColor asset) | Implicit Toggle tint (system default — no explicit `.tint()` override needed, matches `ReminderSection`/`HealthStep`) |
| `Color.red` (system) | inline, matches `ReminderSection.swift:44`, `HealthStep.swift:49` | System red | Permission-denied hint text only |
| `.secondary` (system) | inline | System secondary label color | Default (non-denied) hint text, row values |
| `dpGlassCard(.card)` | `DesignSystem/DPGlass.swift` | 24pt corner radius, `.glassEffect(.regular, ...)` (Liquid Glass) | `WeeklySummarySection`'s outer card surface (via `SettingsSection`) |
| `.thinMaterial` panel | inline, matches `HealthStep.swift:53` | `RoundedRectangle(cornerRadius: 16)` + `.thinMaterial` | The toggle+hint panel inside `HealthStep` (the new weekly-summary toggle row lives inside this **same existing panel**, not a new one) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | `SettingsSection` title horizontal inset |
| sm | 8px | `SettingsSection` title-to-card vertical gap |
| md | 16px | `SettingsSection` card horizontal padding; `HealthStep` toggle-panel padding |
| lg | 24px | `HealthStep` screen horizontal padding |
| xl | 32px | not used by this phase's surfaces |
| 2xl | 48px | not used by this phase's surfaces |
| 3xl | 64px | not used by this phase's surfaces |

**Exceptions (existing codebase precedent — required for pixel-parity with
`ReminderSection`/`HealthStep`, per D-05/D-07's "mirror, don't reinvent"
mandate):**

| Value | Where used | Source of precedent |
|-------|-----------|----------------------|
| 6px | Onboarding title+body `VStack` spacing | `HealthStep.swift:24` |
| 10px | `SettingsRow` vertical padding; hint-text vertical padding | `SettingsRow.swift:31`, `ReminderSection.swift:46` |
| 12px | `HealthStep` toggle+hint `VStack` spacing; `SettingsActionRow` vertical padding | `HealthStep.swift:38`, `SettingsActionRow.swift:22` |
| 14px | Onboarding primary button vertical padding | `HealthStep.swift:62` (shared "Done" button — unchanged by this phase) |
| 40px | Onboarding bottom safe-area padding | `HealthStep.swift:67` (shared — unchanged by this phase) |

None of these are introduced by this phase — they are pre-existing values
in code this phase edits in place. Do not round them to the 8-point scale;
doing so would create a visible inconsistency between the new toggle row
and the Health toggle row directly above/below it in the same panel.

---

## Typography

Native SwiftUI Dynamic Type text styles (no fixed px — scales with the
user's system text size, up to AX5 per CLAUDE.md):

| Role | Text Style | Weight | Line Height | Usage in this phase |
|------|-----------|--------|-------------|----------------------|
| Section label | `.footnote` | regular, `.textCase(.uppercase)` | system default | `SettingsSection` title ("Weekly Summary") |
| Row label / body | `.body` (implicit — no explicit `.font()`) | regular | system default | Toggle row label, in both `SettingsRow` and `HealthStep`'s panel |
| Hint / footnote | `.footnote` | regular | system default | Enabled-hint and denied-hint text (both surfaces) |
| Onboarding heading | `.largeTitle` `.bold()` | bold | system default | **Not new** — `HealthStep`'s existing title, unchanged by this phase |
| Onboarding body | `.body` | regular | system default | **Not new** — `HealthStep`'s existing intro paragraph, unchanged by this phase |
| Button label | `.headline` | semibold (implicit via `.headline`) | system default | **Not new** — shared "Done" button, unchanged by this phase |

**Rule for this phase's new content:** the new toggle row and its hint text
use exactly the same two text styles already present in the panel they're
added to (`.body` for the label, `.footnote` for the hint) — no new size or
weight is introduced anywhere in this phase.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | System background (`Color(.systemGroupedBackground)` / default screen background) | Settings screen background, Onboarding step background |
| Secondary (30%) | Liquid Glass surfaces — `dpGlassCard(.card)` (Settings) and `.thinMaterial` panel (Onboarding) | `WeeklySummarySection` card; the shared toggle panel in `HealthStep` |
| Accent (10%) | `Color.dpAccent` (system `AccentColor`, "Ember") | **Reserved for:** the Toggle's on-state tint only (implicit — SwiftUI applies the app's `AccentColor` automatically; do not add an explicit `.tint()` call, matching `ReminderSection`/`HealthStep`'s existing toggles) |
| Destructive/semantic | `Color.red` (system) | **Reserved for:** permission-denied hint text only, when `weeklySummaryPermissionDenied == true`. This phase has **no destructive action** (no delete/irreversible operation) — red is used purely as the existing "denied" semantic, matching `ReminderSection.swift:44` and `HealthStep.swift:49` exactly |

Accent reserved for: the Toggle control's on-state tint — nothing else in
this phase's new UI. Do not tint the section title, hint text, or any icon
with accent color; those stay `.secondary`/`.primary`/`.red` per the
existing pattern.

**Note on `TrendBadge` colors (dpGreen/dpRed):** `Features/Insights/Components/InsightsHeroCard.swift`'s
`TrendBadge` uses `dpGreen`/`dpRed` for its up/down arrow badge. Per
RESEARCH.md's explicit Anti-Pattern, **this phase does not reuse that
component or its color logic** — the notification banner is plain OS
chrome (no custom color rendering is possible in a `UNNotificationContent`
body string), and no new in-app badge is introduced by this phase. Do not
add `dpGreen`/`dpRed` styling anywhere in this phase's Settings/Onboarding
surfaces.

---

## Copywriting Contract

### Settings — `WeeklySummarySection` (new `SettingsSection`, mirrors `ReminderSection`)

| Element | Copy | Key (proposed) |
|---------|------|-----------------|
| Section title | "Weekly Summary" | `settings.section.weeklySummary` |
| Toggle row label | "Weekly check-in" | `settings.weeklySummary.toggle` |
| Hint (enabled, not denied) | "A weekly note on how this week compares to last." | `settings.weeklySummary.hint` |
| Denied hint | "Notifications are off. Turn them on in Settings to get your weekly check-in." | `settings.weeklySummary.denied` |
| Denied action row | "Open Settings" | reuse existing `settings.reminder.openSettings` (identical generic copy — do not duplicate the string; `ReminderSection.swift:52` already defines this exact label for the identical action) |

### Onboarding — `HealthStep` second toggle (new row inside existing panel)

| Element | Copy | Key (proposed) |
|---------|------|-----------------|
| Toggle row label | "Weekly check-in" | `onboarding.health.weeklySummary.toggle` (same label text as Settings, per D-06 — same feature, same name, independent state) |
| Hint (enabled, not denied) | "Get a weekly note on how your week compares to the last." | `onboarding.health.weeklySummary.hint` |
| Denied hint | "You can turn this on later in Settings." | `onboarding.health.weeklySummary.denied` (mirrors the tone of `onboarding.health.denied`, not the Settings-card denied copy — no "Open Settings" action row exists inside onboarding, matching the existing Health toggle's denied state, which also has no action row) |

Primary CTA for this phase is not a new button — the existing shared "Done"
button (`onboarding.health.done`) already finishes onboarding regardless of
toggle state (D-05); this phase adds no new onboarding button.

### Notification banner (the one genuinely new content surface)

| Element | Copy | Key (proposed) |
|---------|------|-----------------|
| Title | "DrinkPulse" | `weeklySummary.notification.title` (reuse the exact pattern of `reminder.notification.title` — app name as title, no dynamic content in the title) |
| Body — percentage up (ENGG-04) | "You drank {X}% more than last week." | `weeklySummary.notification.body.up` |
| Body — percentage down (ENGG-04) | "You drank {X}% less than last week." | `weeklySummary.notification.body.down` |
| Body — about the same, within ±5% (ENGG-04) | "About the same as last week." | `weeklySummary.notification.body.same` |
| Body — direction-only, prior week zero, this week has drinks (ENGG-05) | "You logged a drink this week — none last week." | `weeklySummary.notification.body.directionOnlyUp` |
| Body — direction-only, both weeks zero (ENGG-05) | "No drinks logged again this week." | `weeklySummary.notification.body.directionOnlySame` |

**Copy rules (non-negotiable, carried from CLAUDE.md):**
- Never call any consumption level "safe" — these are neutral/factual
  comparisons only (per the project's existing risk-language rule; no
  "Low/Moderate/High Risk" language belongs in this notification either,
  since it's a trend comparison, not a guideline-threshold judgment).
- Percentage values are computed by `WeeklySummaryCalculator` (rounded to
  whole percent, matching `TrendBadge`'s `Int((abs(fraction) * 100).rounded())`
  formatting idiom — reuse the *rounding*, not the ±1% threshold, per
  RESEARCH.md's explicit anti-pattern note).
- Body text stays qualitative (percentage + direction), never raw gram
  totals — this is both the ENGG-05 spec and a privacy-conscious default
  per RESEARCH.md's Security Domain note (notification banners are
  visible on the Lock Screen).
- `{X}` is a placeholder for the calculated whole-percent value; exact
  `String(format:)`/interpolation mechanics are an implementation detail
  for planning, not a UI-SPEC concern.

### No destructive action in this phase

There is no delete/irreversible/destructive action anywhere in this
phase's scope — no destructive-confirmation copy is required. (Toggling
the feature off is fully reversible and non-destructive, same as
`ReminderSection`'s off-toggle.)

---

## UI Considerations

Applicable state considerations resolved: 5 covered, 1 backstop, 0 unresolved

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | Settings toggle row (`interactive-control`) | ✅ covered | No "empty" state applies to a toggle — it always renders on/off; not a list-collection, so this category doesn't raise (relevance filter). |
| populated | Notification banner body (`static-content`) | ✅ covered | The 5 body-copy variants above ARE the "populated" states for every branch `WeeklySummaryCalculator` can produce (percentage-up/down/same, direction-only-up/same) — see Copywriting Contract. |
| zero-one-many | N/A | ✅ covered | Not applicable — this phase has no list-collection element (no feed of past notifications, no history list). |
| long-text | Settings hint text, Onboarding hint text (`static-content`) | ✅ covered | Both hint strings are short, single-sentence, fixed-length app copy (not user-generated or dynamic-length) — `.footnote` with default `Text` wrapping (no explicit line-limit) matches existing `ReminderSection`/`HealthStep` hint rendering; no truncation risk since content is authored, not user input. |
| overflow | Percentage value in notification body (`static-content`) | 🧪 backstop | The `{X}%` interpolated value is bounded by realistic consumption deltas but is not hard-capped (e.g. a user could theoretically log a week with a 500%+ increase from a near-zero prior week). No max-width/truncation styling applies since this renders in OS notification chrome (fixed system line-wrap, not app-controlled) — flagged as backstop: the executor should verify a very large percentage (3-digit+) still reads correctly in the OS banner, not add custom truncation logic. |
| error | Settings toggle (permission denied) | ✅ covered | Denied-state hint + "Open Settings" action row — see Copywriting Contract, mirrors `ReminderSection` exactly. |
| error | Onboarding toggle (permission denied) | ✅ covered | Denied-state hint (no action row, matching the existing Health toggle's denied state) — see Copywriting Contract. |

---

## Registry Safety

Not applicable — native SwiftUI project, no component registry / shadcn
tooling exists or is being introduced.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
