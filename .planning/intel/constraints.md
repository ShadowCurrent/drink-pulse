# Constraints (from SPECs)

No documents classified as `SPEC` were found in this ingest
(`.planning/intel/classifications/`). This file is intentionally empty of
entries — absent rather than fabricated. If API contracts, schemas, or
protocol/NFR specs exist elsewhere in the repo, they were not part of this
classification set; re-run classification with those paths included via
`--manifest` if they should be ingested.
