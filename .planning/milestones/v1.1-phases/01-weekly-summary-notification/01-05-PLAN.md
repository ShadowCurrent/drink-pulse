---
phase: 01-weekly-summary-notification
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - drinkpulseUITests/Features/Settings/WeeklySummarySettingsUITests.swift
  - drinkpulseUITests/Features/Onboarding/OnboardingWeeklySummaryUITests.swift
  - drinkpulseUITests/Features/Shell/WeeklySummaryTapUITests.swift
  - drinkpulse/UITestSeed.swift
  - drinkpulse/drinkpulseApp.swift
autonomous: true
requirements: [ENGG-01, ENGG-02, ENGG-07]

must_haves:
  truths:
    - "The Weekly Summary Settings toggle starts off (Switch value \"0\") on a fresh UI-test launch, mirroring ReminderSettingsUITests' determinism guarantee, via a new UITestSeed.resetTransientDefaults() line removing AppStorageKeys.weeklySummaryEnabled"
    - "The Onboarding Weekly Summary toggle can be switched on independently while the Health toggle remains off, proving D-06 end-to-end through the real UI (not just unit-level AppStorage isolation)"
    - "The Onboarding Weekly Summary toggle's on-state is visible in the Settings card immediately after tapping Done, with no separate action (ENGG-02)"
    - "A cold-launch with AppStorageKeys.pendingOpenInsights pre-set to true (via a new, launch-argument-gated UITestSeed hook) results in the Insights tab's navigation bar being visible without any tap — this is the only feasible way to UI-test ENGG-07's tap-routing effect, because UNNotificationResponse has no public initializer XCTest can construct (there is no existing analog for this in the codebase — NotificationActionHandler.didReceive itself has zero existing tests)"
  artifacts:
    - path: "drinkpulseUITests/Features/Settings/WeeklySummarySettingsUITests.swift"
      provides: "UI coverage for ENGG-01 (Settings toggle starts off, hint copy visible)"
    - path: "drinkpulseUITests/Features/Onboarding/OnboardingWeeklySummaryUITests.swift"
      provides: "UI coverage for ENGG-02 (onboarding opt-in, D-06 independence, immediate Settings reflection)"
    - path: "drinkpulseUITests/Features/Shell/WeeklySummaryTapUITests.swift"
      provides: "UI coverage for ENGG-07 (tap-routing effect, simulated via a test-only pending-flag seed)"
    - path: "drinkpulse/UITestSeed.swift"
      provides: "weeklySummaryEnabled added to resetTransientDefaults(); new seedPendingOpenInsights launch-argument-gated flag"
  key_links:
    - from: "drinkpulse/drinkpulseApp.swift"
      to: "drinkpulse/UITestSeed.swift"
      via: "init() calls UITestSeed.seedPendingOpenInsights after resetTransientDefaults(), setting AppStorageKeys.pendingOpenInsights before RootShellView ever appears"
      pattern: "seedPendingOpenInsights"
  prohibitions:
    - statement: "MUST NOT key any assertion off system-process UI (permission alerts, share sheets) or localized system labels — the simulator's system locale is Polish; only the app's own English String(localized:) content or stable accessibilityIdentifiers may be asserted on, per CLAUDE.md's UI-test locale-independence rule"
      verification: flagged-unverified
---

<objective>
Add the three Wave-0 UI test files RESEARCH.md's Phase Requirements -> Test Map calls for
(ENGG-01, ENGG-02, ENGG-07), each mirroring an existing, already-shipped analog test almost
line-for-line. Since ENGG-07's tap-routing has no existing analog anywhere in this codebase
(`NotificationActionHandler.didReceive` has zero tests today, and `UNNotificationResponse` has
no public initializer XCTest can construct), this plan also adds a small, launch-argument-gated
test-only seeding hook to `UITestSeed`/`drinkpulseApp` that sets the already-wired
`pendingOpenInsights` flag directly — exercising the exact same cold-launch consumption code
path a real tap would drive, without needing to simulate the OS notification-tap gesture itself.

Purpose: close the Wave-0 test gaps RESEARCH.md identified before declaring the phase complete.
Output: three new UI test files, two small test-infrastructure edits.
</objective>

<execution_context>
@/Users/fempter/Developer/drinkpulse/.claude/gsd-core/workflows/execute-plan.md
@/Users/fempter/Developer/drinkpulse/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-weekly-summary-notification/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WeeklySummarySettingsUITests.swift + UITestSeed reset</name>
  <files>drinkpulseUITests/Features/Settings/WeeklySummarySettingsUITests.swift, drinkpulse/UITestSeed.swift</files>
  <read_first>
    drinkpulseUITests/Features/Settings/ReminderSettingsUITests.swift (full file, 86 lines — the exact launch-args, openSettings() helper, and toggle-assertion pattern to mirror)
    drinkpulse/UITestSeed.swift (current full file, 158 lines — resetTransientDefaults(), lines 50-57)
    drinkpulse/DesignSystem/AppStorageKeys.swift (weeklySummaryEnabled)
  </read_first>
  <action>
    In drinkpulse/UITestSeed.swift, add one line to `resetTransientDefaults()`, alongside the
    existing `reminderEnabled`/`healthWriteEnabled` removals:
    `UserDefaults.standard.removeObject(forKey: AppStorageKeys.weeklySummaryEnabled)` — so a prior
    UI-test run that left the toggle on never pollutes the next run's "starts off" assumption.
    Create drinkpulseUITests/Features/Settings/WeeklySummarySettingsUITests.swift as an `XCTestCase`
    (`@MainActor final class WeeklySummarySettingsUITests`), launching with
    `app.launchArguments += ["-dp_onboarding_done", "YES", "-dp_uitest", "YES"]` (identical to
    ReminderSettingsUITests), with a private `openSettings()` helper identical to
    ReminderSettingsUITests' (tap the "Settings" tab bar button, wait for the "Settings" nav bar,
    `app.swipeUp()`). Add `test_weeklySummaryToggle_startsOff_thenTogglesOn()`: find
    `app.switches["Weekly check-in"]` (swipeUp again if not yet visible), assert it exists and
    `toggle.value as? String == "0"`, tap it, assert `toggle.value as? String == "1"` within a
    5-second wait (the `-dp_uitest` stub auto-grants, matching the Health-step UI test's pattern).
    Add `test_weeklySummarySection_showsHintCopy()`: assert
    `app.staticTexts["A weekly note on how this week compares to last."]` becomes visible
    (swipeUp if needed).
  </action>
  <acceptance_criteria>
    - `UITestSeed.resetTransientDefaults()` contains a `removeObject(forKey: AppStorageKeys.weeklySummaryEnabled)` line
    - Both named test methods exist in the new file and assert only on the app's own English `String(localized:)` content, never system-process UI
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseUITests/WeeklySummarySettingsUITests</automated>
  </verify>
  <done>Both WeeklySummarySettingsUITests methods pass; a prior run's toggle state can no longer leak into the next run.</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingWeeklySummaryUITests.swift</name>
  <files>drinkpulseUITests/Features/Onboarding/OnboardingWeeklySummaryUITests.swift</files>
  <read_first>
    drinkpulseUITests/Features/Onboarding/OnboardingHealthStepUITests.swift (full file, 81 lines — the exact Welcome->Profile->Guideline->HealthStep walk, the trailing-edge coordinate-tap workaround for a full-width Toggle, and the post-Done Home-tab assertion)
  </read_first>
  <action>
    Create drinkpulseUITests/Features/Onboarding/OnboardingWeeklySummaryUITests.swift as an
    `@MainActor final class OnboardingWeeklySummaryUITests: XCTestCase` with one test,
    `test_weeklySummaryToggle_independentOfHealthToggle_andReflectedInSettingsAfterDone()`,
    launching with `["-dp_uitest", "YES", "-dp_force_onboarding", "YES"]`. Walk the same four
    steps as OnboardingHealthStepUITests (tap "Get Started", then "Continue" twice) to reach the
    4th step. Assert both `app.switches["Write to Apple Health"]` and
    `app.switches["Weekly check-in"]` exist and both read value `"0"`. Tap only the Weekly
    Summary switch on, using the same trailing-edge coordinate tap workaround
    (`.coordinate(withNormalizedOffset: CGVector(dx: 0.92, dy: 0.5)).tap()`) and the same
    `XCTNSPredicateExpectation`-based wait-for-"1" pattern OnboardingHealthStepUITests uses for
    the Health switch. Assert the Health switch is STILL `"0"` after the Weekly Summary switch
    reads `"1"` (proves D-06 independence through the real UI, not just AppStorage isolation).
    Tap "Done", assert `app.tabBars.buttons["Home"]` appears (onboarding finished). Then tap the
    "Settings" tab bar button, swipe up if needed, and assert
    `app.switches["Weekly check-in"]` (the Settings copy) now reads value `"1"` (ENGG-02:
    "reflected in Settings immediately, no separate action needed").
  </action>
  <acceptance_criteria>
    - The test asserts the Health switch's value is unchanged (still "0") immediately after the Weekly Summary switch becomes "1"
    - The test asserts the Settings-screen Weekly Summary switch reads "1" after Done, without any additional toggle tap in Settings
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseUITests/OnboardingWeeklySummaryUITests</automated>
  </verify>
  <done>Onboarding's Weekly Summary toggle is proven independent of the Health toggle and its on-state carries into Settings with no extra action.</done>
</task>

<task type="auto">
  <name>Task 3: Cold-launch tap-simulation hook + WeeklySummaryTapUITests.swift</name>
  <files>drinkpulse/UITestSeed.swift, drinkpulse/drinkpulseApp.swift, drinkpulseUITests/Features/Shell/WeeklySummaryTapUITests.swift</files>
  <read_first>
    drinkpulse/UITestSeed.swift (current full file, including the just-edited resetTransientDefaults() from Task 1 — read the file fresh, not from memory — and the `forceShowOnboarding` static let, lines 35-41, as the exact launch-arg-parsing pattern to copy)
    drinkpulse/drinkpulseApp.swift (init(), lines 20-25 — the exact call site to extend)
    drinkpulse/Features/Shell/RootShellView.swift (openInsightsIfPending(), added in plan 01-03 — this is the code path this test exercises)
    drinkpulse/Features/Insights/InsightsView.swift (line 26 — confirms `.navigationTitle(String(localized: "tab.insights"))`, English value "Insights", the stable assertion target)
  </read_first>
  <action>
    In drinkpulse/UITestSeed.swift, add a new static let, copying the exact parsing shape of
    `forceShowOnboarding` (lines 35-41): `static let seedPendingOpenInsights: Bool = { let args = ProcessInfo.processInfo.arguments; guard let idx = args.firstIndex(of: "-dp_uitest_pending_open_insights"), args.indices.contains(idx + 1) else { return false }; return args[idx + 1].uppercased() == "YES" }()`.
    In drinkpulse/drinkpulseApp.swift's `init()`, immediately after the existing
    `UITestSeed.resetTransientDefaults()` call, add:
    `if UITestSeed.seedPendingOpenInsights { UserDefaults.standard.set(true, forKey: AppStorageKeys.pendingOpenInsights) }`
    (gated purely on the new launch argument; inert whenever that argument is absent, i.e. always
    inert in production). Create
    drinkpulseUITests/Features/Shell/WeeklySummaryTapUITests.swift as an
    `@MainActor final class WeeklySummaryTapUITests: XCTestCase` with one test,
    `test_pendingOpenInsights_opensInsightsTab_onColdLaunch()`: launch with
    `["-dp_onboarding_done", "YES", "-dp_uitest", "YES", "-dp_uitest_pending_open_insights", "YES"]`,
    then assert `app.navigationBars["Insights"].waitForExistence(timeout: 10)` is true — proving
    the app landed directly on the Insights tab at cold launch with no tap performed by the test
    (the pre-set flag stands in for a real notification tap having already happened before this
    launch). Document in a header comment (mirroring OnboardingHealthStepUITests' documentation
    style) that this is a stand-in for a real OS notification tap, since `UNNotificationResponse`
    has no public initializer XCTest can construct, and that the code path exercised
    (`RootShellView.openInsightsIfPending()`) is identical to what a real tap drives.
  </action>
  <acceptance_criteria>
    - `UITestSeed.seedPendingOpenInsights` returns `false` when the launch argument is absent (inert in production and in every other existing UI test)
    - `drinkpulseApp.init()` sets `AppStorageKeys.pendingOpenInsights` only when `seedPendingOpenInsights` is true
    - The new test asserts on `app.navigationBars["Insights"]`, never on a tab-bar button's mere existence (all tab buttons always exist regardless of selection)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseUITests/WeeklySummaryTapUITests</automated>
  </verify>
  <done>A cold launch with the new launch argument lands directly on the Insights tab, proving RootShellView's tap-consumption wiring from plan 01-03 works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Launch-argument-gated test hook -> production build | `seedPendingOpenInsights` must be provably inert whenever `-dp_uitest_pending_open_insights` is absent from `ProcessInfo.processInfo.arguments` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Elevation of Privilege | `UITestSeed.seedPendingOpenInsights` | low | accept | Identical gating pattern to the already-shipped `forceShowOnboarding` — a hardcoded launch-argument string that App Store builds and normal launches never pass; carries no PII, seeds only a boolean navigation flag |

</threat_model>

<verification>
`xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseUITests/WeeklySummarySettingsUITests -only-testing:drinkpulseUITests/OnboardingWeeklySummaryUITests -only-testing:drinkpulseUITests/WeeklySummaryTapUITests` passes all cases. Full suite (`xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`) stays green — no regression to existing Reminder/Health UI tests from the `resetTransientDefaults()` edit.
</verification>

<success_criteria>
ENGG-01, ENGG-02, and ENGG-07 each have a passing, locale-independent UI test exercising the real screen/flow, matching RESEARCH.md's Phase Requirements -> Test Map with zero Wave-0 gaps remaining.
</success_criteria>

<output>
Create `.planning/phases/01-weekly-summary-notification/01-05-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `drinkpulseUITests/Features/Settings/WeeklySummarySettingsUITests.swift` — new file
- `drinkpulseUITests/Features/Onboarding/OnboardingWeeklySummaryUITests.swift` — new file
- `drinkpulseUITests/Features/Shell/WeeklySummaryTapUITests.swift` — new file
- `drinkpulse/UITestSeed.swift` — new member: `static let seedPendingOpenInsights: Bool`; `resetTransientDefaults()` gains a `weeklySummaryEnabled` removal line
- `drinkpulse/drinkpulseApp.swift` — `init()` gains a `seedPendingOpenInsights`-gated seeding line
