---
phase: 01-weekly-summary-notification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drinkpulse/Domain/WeeklySummaryCalculator.swift
  - drinkpulseTests/Domain/WeeklySummaryCalculatorTests.swift
  - drinkpulse/DesignSystem/AppStorageKeys.swift
  - drinkpulse/Localizable.xcstrings
autonomous: true
requirements: [ENGG-03, ENGG-04, ENGG-05, ENGG-06]

must_haves:
  truths:
    - "WeeklySummaryCalculator.content(currentWeekGrams:priorWeekGrams:hasAnyPriorWeekData:) returns .skip exactly when hasAnyPriorWeekData == false, regardless of grams values (ENGG-06 — first-ever week, per CONTEXT.md discretion: zero ConsumptionEvent rows before the current week start, not toggle-flip-relative)"
    - "WeeklySummaryCalculator.content returns .directionOnly (never .percentage) exactly when priorWeekGrams == 0 AND hasAnyPriorWeekData == true — the zero-grams-but-real-week (ENGG-05) case is never conflated with the no-prior-week-at-all (ENGG-06) case"
    - "The guard against dividing by priorWeekGrams is strict priorWeekGrams > 0 with no epsilon, matching InsightsViewModel.trendFraction's existing guard (line 226) exactly — a logged 0.0%-ABV event does not make priorWeekGrams non-zero"
    - "Exactly +/-5% (abs(fraction) <= 0.05) classifies as .percentage(..., direction: .same) — the boundary is inclusive, not exclusive (ENGG-04)"
    - "currentWeekGrams == 0 with priorWeekGrams > 0 (a fully sober current week following a week with drinks) resolves to .percentage(fraction: -1.0, direction: .down) — NOT .directionOnly, because the zero-grams branch is gated on the PRIOR week, not the current week"
    - statement: "A very large week-over-week percentage (e.g. prior week near zero, current week high) produces a large but non-crashing Int from Int((abs(fraction) * 100).rounded()); OS notification chrome renders the resulting text via its own native line-wrap with no app-side truncation logic added"
      verification: backstop
  artifacts:
    - path: "drinkpulse/Domain/WeeklySummaryCalculator.swift"
      provides: "Pure, Sendable week-over-week content classifier (skip / directionOnly / percentage) — no SwiftUI, SwiftData, or UserNotifications imports"
      exports: ["SignDirection", "WeeklySummaryContent", "WeeklySummaryCalculator"]
    - path: "drinkpulseTests/Domain/WeeklySummaryCalculatorTests.swift"
      provides: "Swift Testing coverage of every calculator branch and boundary"
    - path: "drinkpulse/DesignSystem/AppStorageKeys.swift"
      provides: "weeklySummaryEnabled and pendingOpenInsights key constants"
    - path: "drinkpulse/Localizable.xcstrings"
      provides: "All 13 new localized strings this phase needs (Settings, Onboarding, notification copy)"
  key_links:
    - from: "drinkpulse/Services/WeeklySummaryService.swift (plan 01-02)"
      to: "drinkpulse/Domain/WeeklySummaryCalculator.swift"
      via: "WeeklySummaryCalculator.content(currentWeekGrams:priorWeekGrams:hasAnyPriorWeekData:) call inside scheduleIfEnabled(context:)"
      pattern: "WeeklySummaryCalculator\\.content\\("
  prohibitions:
    - statement: "MUST NOT use shaming, guilt, congratulatory/reward, or 'safe' framing anywhere in the weekly-summary notification body or its Settings/Onboarding copy — stay neutral/factual per CLAUDE.md's risk-language rule (no 'Low/Moderate/High Risk' language either, since this is a trend comparison, not a guideline-threshold judgment)"
      verification: flagged-unverified
    - statement: "MUST NOT include raw gram totals or any other granular health-data value beyond the whole-percent figure in the notification banner body strings — the banner is visible on the Lock Screen; only qualitative direction + rounded percentage may appear"
      verification: flagged-unverified
---

<objective>
Create the pure, unit-testable week-over-week content classifier
(`WeeklySummaryCalculator`) that decides whether the weekly-summary
notification should be skipped, direction-only, or percentage-based — the
domain logic behind ENGG-03/04/05/06 — plus the shared, phase-wide
`AppStorageKeys` constants and the complete set of new `Localizable.xcstrings`
entries every later plan in this phase needs. Consolidating all string-catalog
additions into this one Wave-1 plan means no other plan in the phase edits
`Localizable.xcstrings` again, avoiding parallel-wave file conflicts.

Purpose: give every downstream plan (service, Settings UI, Onboarding UI) a
fixed, already-tested contract to build against, per interface-first ordering.
Output: `Domain/WeeklySummaryCalculator.swift` (+ tests), two new
`AppStorageKeys` constants, and 13 new localized strings.
</objective>

<execution_context>
@/Users/fempter/Developer/drinkpulse/.claude/gsd-core/workflows/execute-plan.md
@/Users/fempter/Developer/drinkpulse/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-weekly-summary-notification/01-CONTEXT.md
@.planning/phases/01-weekly-summary-notification/01-RESEARCH.md
@.planning/phases/01-weekly-summary-notification/01-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create WeeklySummaryCalculator + tests</name>
  <files>drinkpulse/Domain/WeeklySummaryCalculator.swift, drinkpulseTests/Domain/WeeklySummaryCalculatorTests.swift</files>
  <read_first>
    drinkpulse/Domain/ConsumptionEvent.swift (pureAlcoholGrams, lines 70-78 — confirms the physical-density contract this calculator's callers must respect, even though the calculator itself only takes plain Doubles)
    drinkpulse/Features/Insights/InsightsViewModel.swift (lines 216-228 — prevPeriodTotalGrams/trendFraction: the exact "> 0, no epsilon" guard and fraction formula to port)
    drinkpulseTests/Services/ReminderServiceTests.swift (lines 1-60 — Swift Testing @Test/#expect style and file layout convention already used in this target)
  </read_first>
  <behavior>
    - Test 1 (`content_skips_whenNoPriorWeekDataAtAll`): hasAnyPriorWeekData: false, any grams values -> .skip (ENGG-06)
    - Test 2 (`content_directionOnlyUp_whenPriorWeekZero_currentWeekPositive`): priorWeekGrams: 0, currentWeekGrams: 20, hasAnyPriorWeekData: true -> .directionOnly(.up) (ENGG-05)
    - Test 3 (`content_directionOnlySame_whenBothWeeksZero`): priorWeekGrams: 0, currentWeekGrams: 0, hasAnyPriorWeekData: true -> .directionOnly(.same) (two consecutive sober weeks, prior week data exists — never .skip)
    - Test 4 (`content_percentageUp_wellAboveBand`): priorWeekGrams: 100, currentWeekGrams: 120 -> .percentage(fraction: 0.2, direction: .up)
    - Test 5 (`content_percentageDown_wellBelowBand`): priorWeekGrams: 100, currentWeekGrams: 80 -> .percentage(fraction: -0.2, direction: .down)
    - Test 6 (`content_percentageSame_atExactlyPositiveFivePercentBoundary`): priorWeekGrams: 100, currentWeekGrams: 105 -> .percentage(fraction: 0.05, direction: .same) — boundary is inclusive
    - Test 7 (`content_percentageSame_atExactlyNegativeFivePercentBoundary`): priorWeekGrams: 100, currentWeekGrams: 95 -> .percentage(fraction: -0.05, direction: .same)
    - Test 8 (`content_percentageDown_whenCurrentWeekIsZero_priorWeekPositive`): priorWeekGrams: 50, currentWeekGrams: 0 -> .percentage(fraction: -1.0, direction: .down) — NOT .directionOnly, because the zero-guard is on priorWeekGrams, not currentWeekGrams
    - Test 9 (`content_percentageUp_handlesLargeDelta_withoutOverflowOrCrash`): priorWeekGrams: 1, currentWeekGrams: 600 -> .percentage(fraction: 599.0, direction: .up) — must not crash or trap
  </behavior>
  <action>
    Create drinkpulse/Domain/WeeklySummaryCalculator.swift with three declarations, Foundation-only
    (no import SwiftUI, SwiftData, or UserNotifications — this type must stay pure and
    Sendable so it needs no ModelContext or UNUserNotificationCenter to test):
    1. `enum SignDirection: Equatable, Sendable { case up, down, same }`
    2. `enum WeeklySummaryContent: Equatable, Sendable { case skip; case directionOnly(SignDirection); case percentage(fraction: Double, direction: SignDirection) }`
    3. `enum WeeklySummaryCalculator` with a single static function
       `content(currentWeekGrams: Double, priorWeekGrams: Double, hasAnyPriorWeekData: Bool) -> WeeklySummaryContent`
       implementing exactly: guard hasAnyPriorWeekData else return .skip; guard priorWeekGrams > 0
       (strict, no epsilon — port InsightsViewModel line 226's exact guard shape) else return
       currentWeekGrams > 0 ? .directionOnly(.up) : .directionOnly(.same); compute
       fraction = (currentWeekGrams - priorWeekGrams) / priorWeekGrams; if abs(fraction) <= 0.05
       return .percentage(fraction: fraction, direction: .same); otherwise return
       .percentage(fraction: fraction, direction: fraction < 0 ? .down : .up).
    Create drinkpulseTests/Domain/WeeklySummaryCalculatorTests.swift as a `struct` with
    `import Foundation`, `import Testing`, `@testable import drinkpulse`, containing the 9
    `@Test` functions named in the behavior block above, each asserting via `#expect(... == ...)`
    on the exact `WeeklySummaryContent` case and associated values.
  </action>
  <acceptance_criteria>
    - drinkpulse/Domain/WeeklySummaryCalculator.swift contains no `import SwiftUI`, `import SwiftData`, or `import UserNotifications` line
    - `SignDirection`, `WeeklySummaryContent`, and `WeeklySummaryCalculator` all conform to `Sendable`
    - All 9 named test functions exist in WeeklySummaryCalculatorTests.swift and pass
    - `xcodebuild build` for the drinkpulse scheme reports zero warnings for these two new files
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseTests/WeeklySummaryCalculatorTests</automated>
  </verify>
  <done>All 9 tests pass; WeeklySummaryCalculator is a pure Foundation-only, Sendable type with the exact branch behavior specified above.</done>
</task>

<task type="auto">
  <name>Task 2: Add AppStorageKeys constants + all-phase Localizable.xcstrings entries</name>
  <files>drinkpulse/DesignSystem/AppStorageKeys.swift, drinkpulse/Localizable.xcstrings</files>
  <read_first>
    drinkpulse/DesignSystem/AppStorageKeys.swift (current full file — 23 lines, follow the existing `reminder*`/`healthWriteEnabled` naming and comment-block convention)
    drinkpulse/Localizable.xcstrings (JSON string catalog — inspect the existing "reminder.notification.body", "reminder.notification.title", "settings.reminder.*", "onboarding.health.*" entries for the exact `{"extractionState":"manual","localizations":{"en":{"stringUnit":{"state":"translated","value":"..."}}}}` shape to replicate)
    .planning/phases/01-weekly-summary-notification/01-UI-SPEC.md (Copywriting Contract section — exact copy for every new string)
  </read_first>
  <action>
    Per D-08 (Claude drafts the notification body copy, following the existing
    `String(localized:)` pattern used by `ReminderService`'s `reminder.notification.title`/`.body`
    keys, neutral/factual tone, no risk-language violations — CLAUDE.md: never call consumption
    "safe"), use the exact strings below (already drafted and checker-approved in
    01-UI-SPEC.md's Copywriting Contract) rather than inventing new wording.
    In drinkpulse/DesignSystem/AppStorageKeys.swift, add two new static constants inside the
    `AppStorageKeys` enum, preceded by a one-line comment block naming this phase (mirroring the
    existing "Log-reminder local notification (plan-0016)." comment style):
    `static let weeklySummaryEnabled = "dp_weekly_summary_enabled"` and
    `static let pendingOpenInsights = "dp_pending_open_insights"`. No hour/minute keys (fixed 9am,
    no user-facing time picker per ENGG-03/D-07 scope).
    In drinkpulse/Localizable.xcstrings, add 13 new top-level entries to the `strings` object,
    each following the exact existing entry shape (extractionState "manual", one "en" localization,
    state "translated"), inserted in alphabetical key order to match the file's existing sort:
    "onboarding.health.weeklySummary.denied" = "You can turn this on later in Settings.";
    "onboarding.health.weeklySummary.hint" = "Get a weekly note on how your week compares to the last.";
    "onboarding.health.weeklySummary.toggle" = "Weekly check-in";
    "settings.section.weeklySummary" = "Weekly Summary";
    "settings.weeklySummary.denied" = "Notifications are off. Turn them on in Settings to get your weekly check-in.";
    "settings.weeklySummary.hint" = "A weekly note on how this week compares to last.";
    "settings.weeklySummary.toggle" = "Weekly check-in";
    "weeklySummary.notification.body.directionOnlySame" = "No drinks logged again this week.";
    "weeklySummary.notification.body.directionOnlyUp" = "You logged a drink this week — none last week.";
    "weeklySummary.notification.body.down" = "You drank %d%% less than last week.";
    "weeklySummary.notification.body.same" = "About the same as last week.";
    "weeklySummary.notification.body.up" = "You drank %d%% more than last week.";
    "weeklySummary.notification.title" = "DrinkPulse".
    Use `%d` (not `%@`) for the percentage placeholder — it is consumed via
    `String(format:)` with an `Int` argument (matching the existing
    "insights.nav.weeksAgo" = "%d weeks ago" placeholder convention already in this file), and
    `%%` renders a literal percent sign in a printf-style format string.
    Do NOT add a new key for the denied-state "Open Settings" action row copy — reuse the
    existing "settings.reminder.openSettings" key verbatim (identical generic "Open Settings" text).
  </action>
  <acceptance_criteria>
    - `python3 -c "import json; json.load(open('drinkpulse/Localizable.xcstrings'))"` exits 0 (valid JSON)
    - All 13 new keys are present with their exact English values as listed above
    - AppStorageKeys.swift contains exactly two new static let declarations: weeklySummaryEnabled and pendingOpenInsights
    - No new key was added for "Open Settings" — grep confirms only one definition of "settings.reminder.openSettings" in the file
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('drinkpulse/Localizable.xcstrings')); keys=['onboarding.health.weeklySummary.denied','onboarding.health.weeklySummary.hint','onboarding.health.weeklySummary.toggle','settings.section.weeklySummary','settings.weeklySummary.denied','settings.weeklySummary.hint','settings.weeklySummary.toggle','weeklySummary.notification.body.directionOnlySame','weeklySummary.notification.body.directionOnlyUp','weeklySummary.notification.body.down','weeklySummary.notification.body.same','weeklySummary.notification.body.up','weeklySummary.notification.title']; missing=[k for k in keys if k not in d['strings']]; assert not missing, missing; print('OK')"</automated>
  </verify>
  <done>AppStorageKeys has the two new constants; Localizable.xcstrings has all 13 new entries with correct values and is valid JSON; the project still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Device -> Lock Screen notification banner | The weekly-summary notification body renders derived health-trend data (direction/percentage) somewhere any person with physical access to the locked device can read it |
| App code -> OSLog | Service/UI code logs error categories via `Logger`; must never log the computed values this plan's calculator produces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Localizable.xcstrings notification body strings (this plan) | medium | mitigate | Every new "weeklySummary.notification.body.*" string stays qualitative (direction word + rounded whole-percent only) — never a raw gram total; verified by literal string content in Task 2's acceptance criteria |
| T-01-02 | Information Disclosure | WeeklySummaryCalculator's output values, if ever logged upstream | medium | mitigate | WeeklySummaryCalculator itself performs no logging (pure function, no OSLog import) — the discipline of "never log the computed percentage/grams" is enforced in the consuming plan (01-02), not here; this plan's job is to make the values easy to keep out of logs by keeping them in a typed enum rather than loose Doubles passed around |

</threat_model>

<verification>
`xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseTests/WeeklySummaryCalculatorTests` passes with all 9 cases green. `xcodebuild build -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` reports zero new warnings. Localizable.xcstrings remains valid JSON with all 13 new keys present.
</verification>

<success_criteria>
`WeeklySummaryCalculator.content` correctly classifies every ENGG-04/05/06 branch and boundary listed in must_haves.truths; `AppStorageKeys.weeklySummaryEnabled`/`.pendingOpenInsights` exist; every string key later plans reference (`settings.section.weeklySummary`, `settings.weeklySummary.*`, `onboarding.health.weeklySummary.*`, `weeklySummary.notification.*`) already exists in Localizable.xcstrings so no later plan needs to touch that file again.
</success_criteria>

<output>
Create `.planning/phases/01-weekly-summary-notification/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `drinkpulse/Domain/WeeklySummaryCalculator.swift` — new file
  - `enum SignDirection: Equatable, Sendable { case up, down, same }`
  - `enum WeeklySummaryContent: Equatable, Sendable { case skip, case directionOnly(SignDirection), case percentage(fraction: Double, direction: SignDirection) }`
  - `enum WeeklySummaryCalculator { static func content(currentWeekGrams: Double, priorWeekGrams: Double, hasAnyPriorWeekData: Bool) -> WeeklySummaryContent }`
- `drinkpulseTests/Domain/WeeklySummaryCalculatorTests.swift` — new file, 9 `@Test` functions
- `drinkpulse/DesignSystem/AppStorageKeys.swift` — new members: `weeklySummaryEnabled`, `pendingOpenInsights`
- `drinkpulse/Localizable.xcstrings` — 13 new string-catalog keys (listed in Task 2's action)
