---
phase: 02-swift-6-language-mode-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - drinkpulseTests/Features/History/HistoryViewModelTests.swift
  - drinkpulseTests/Performance/ScreenComputePerformanceTests.swift
  - docs/architecture.md
  - docs/DEVLOG.md
  - .claude/context/current-focus.md
autonomous: true
requirements: [SWIFT6-03, SWIFT6-01]

must_haves:
  truths:
    - "The two remaining XCTest performance-test types (HistoryViewModelPerformanceTests in HistoryViewModelTests.swift, and ScreenComputePerformanceTests) each carry an explicit, applied, dated decision comment — kept on XCTest because measure {} has no Swift Testing equivalent (ROADMAP Phase 2 Success Criterion #4 / SWIFT6-03)"
    - "xcodebuild test for the full suite (drinkpulseTests + drinkpulseUITests) is green, and xcrun xccov reports overall coverage >= 90% with Domain 100%, ViewModels >= 90%, Services >= 85%, after the Swift 6 migration (ROADMAP Phase 2 Success Criterion #5)"
    - "docs/architecture.md's Concurrency section, docs/DEVLOG.md, and .claude/context/current-focus.md all reflect the completed Swift 6 flip as current reality, per CLAUDE.md's living-docs update model"
  artifacts:
    - path: "drinkpulseTests/Features/History/HistoryViewModelTests.swift"
      provides: "Explicit, dated XCTest-retention decision comment on HistoryViewModelPerformanceTests"
    - path: "drinkpulseTests/Performance/ScreenComputePerformanceTests.swift"
      provides: "Explicit, dated XCTest-retention decision comment on ScreenComputePerformanceTests"
    - path: "docs/architecture.md"
      provides: "Concurrency section now cites the enforcing build setting (SWIFT_VERSION = 6.0) instead of an aspirational claim"
    - path: "docs/DEVLOG.md"
      provides: "New append-only entry recording the Swift 6 migration and its final coverage number"
    - path: ".claude/context/current-focus.md"
      provides: "Updated current-focus entry pointing at Phase 3 (App Startup Hardening) next"
  key_links:
    - from: "drinkpulseTests/Features/History/HistoryViewModelTests.swift (HistoryViewModelPerformanceTests decision comment)"
      to: "drinkpulseTests/Performance/ScreenComputePerformanceTests.swift (matching decision comment)"
      via: "both cite the same rationale (measure {} has no Swift Testing equivalent) so a future reader finds one consistent, applied policy rather than two independent ad hoc notes"
      pattern: "measure \\{"
  prohibitions:
    - statement: "MUST NOT convert HistoryViewModelPerformanceTests's or ScreenComputePerformanceTests's measure {} blocks to Swift Testing @Test/#expect — doing so would silently drop the existing performance baseline with no replacement (RESEARCH.md Anti-Patterns)"
      verification: flagged-unverified
    - statement: "MUST NOT let overall or per-layer test coverage regress below the CLAUDE.md thresholds (Domain 100%, ViewModels >= 90%, Services >= 85%, overall >= 90%) as a side effect of this migration"
      verification: flagged-unverified
---

<objective>
Close out Phase 2: apply and document the SWIFT6-03 decision on the 2
remaining XCTest performance-test types, run the full build + test +
coverage gate to prove ROADMAP Phase 2 Success Criterion #5 (coverage
holds after the migration), and bring the living docs (architecture.md,
DEVLOG.md, current-focus.md) up to date with the now-real Swift 6
guarantee.

Purpose: SWIFT6-03 is a documented policy decision, not a compiler-driven
fix (RESEARCH.md); this plan makes that decision explicit and applied
rather than leaving it implicit, and closes the phase with the same
build/test/coverage gate CLAUDE.md requires for every non-trivial change.
Output: both XCTest performance-test types carry an applied decision
comment; full suite green with coverage thresholds met; living docs
match reality.
</objective>

<execution_context>
@/Users/fempter/.claude/gsd-core/workflows/execute-plan.md
@/Users/fempter/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md
@.planning/phases/02-swift-6-language-mode-migration/02-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply and document the SWIFT6-03 decision (keep XCTest for measure {})</name>
  <files>drinkpulseTests/Features/History/HistoryViewModelTests.swift, drinkpulseTests/Performance/ScreenComputePerformanceTests.swift</files>
  <read_first>
    .planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md Summary (paragraph 4) and Anti-Patterns ("Converting measure {} performance tests" bullet) and Sources (Apple Developer Forums thread 774088)
    .planning/ROADMAP.md Phase 2 Success Criterion #4 (exact wording for the decision this task applies)
    drinkpulseTests/Features/History/HistoryViewModelTests.swift lines 1-15 (file imports: `import Testing` + `import XCTest` side by side) and lines 240-284 (`// MARK: - Performance tests`, `@MainActor class HistoryViewModelPerformanceTests: XCTestCase`, its 4 `measure { ... }` calls at lines 256/261/266/271)
    drinkpulseTests/Performance/ScreenComputePerformanceTests.swift lines 1-18 (existing class doc comment on `@MainActor final class ScreenComputePerformanceTests: XCTestCase`, its 3 `measure { ... }` calls)
  </read_first>
  <action>
    Add a decision comment to each of the 2 remaining XCTest performance-test types — decision only, no change to any `func test_...()` body, assertion, or `measure { ... }` block. (1) In drinkpulseTests/Features/History/HistoryViewModelTests.swift, directly above `class HistoryViewModelPerformanceTests: XCTestCase` at line 242 (immediately below the existing `// MARK: - Performance tests` comment at line 240), add a doc comment stating: this type is deliberately kept on XCTest, not converted to Swift Testing, because its `measure { }` calls have no Swift Testing equivalent (cite Apple Developer Forums thread 774088, as RESEARCH.md does); this is a reviewed, applied decision per ROADMAP Phase 2 Success Criterion #4 / SWIFT6-03, dated 2026-07-27. (2) In drinkpulseTests/Performance/ScreenComputePerformanceTests.swift, extend the existing class doc comment (lines 4-13) with the same explicit decision sentence — kept on XCTest, `measure { }` has no Swift Testing equivalent, reviewed exception per SWIFT6-03, dated 2026-07-27 — the existing comment explains the class's profiling purpose but not this policy decision. Do not touch `HistoryViewModelTests` (the Swift Testing `struct` earlier in the same file, lines 1-238) — it is out of scope; only the XCTest performance type needs the decision comment.
  </action>
  <acceptance_criteria>
    - `grep -B4 "class HistoryViewModelPerformanceTests" drinkpulseTests/Features/History/HistoryViewModelTests.swift | grep -iE "XCTest"` and `grep -B4 "class HistoryViewModelPerformanceTests" drinkpulseTests/Features/History/HistoryViewModelTests.swift | grep -i "Swift Testing"` both match (decision is stated, not just class purpose)
    - `grep -B10 "final class ScreenComputePerformanceTests" drinkpulseTests/Performance/ScreenComputePerformanceTests.swift | grep -iE "XCTest"` and the same `grep -B10 ... | grep -i "Swift Testing"` both match
    - `grep -n "measure {" drinkpulseTests/Features/History/HistoryViewModelTests.swift | grep -v "///" | wc -l` still equals 4 (no measure block removed or converted)
    - `grep -n "measure {" drinkpulseTests/Performance/ScreenComputePerformanceTests.swift | grep -v "///" | wc -l` still equals 3 (no measure block removed or converted)
    - `xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseTests/HistoryViewModelPerformanceTests -only-testing:drinkpulseTests/ScreenComputePerformanceTests` passes (both types still run and pass under the now-flipped app target)
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:drinkpulseTests/HistoryViewModelPerformanceTests -only-testing:drinkpulseTests/ScreenComputePerformanceTests</automated>
  </verify>
  <done>Both remaining XCTest performance-test types carry an explicit, applied, dated decision comment justifying why they stay on XCTest, without dropping either existing measure {} baseline — SWIFT6-03 satisfied.</done>
  <reversibility rating="reversible">Doc-comment-only edits to 2 test files; trivially revertible via git, zero behavior change.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Full Debug+Release build, full test suite, and coverage gate</name>
  <files>None — verification-only task. If coverage has genuinely regressed below a CLAUDE.md threshold, add the missing test(s) in the file(s) identified by the coverage report before marking this task done.</files>
  <read_first>
    .planning/phases/02-swift-6-language-mode-migration/02-VALIDATION.md (Sampling Rate; Per-Task Verification Map row 02-04-01)
    .planning/phases/02-swift-6-language-mode-migration/02-RESEARCH.md "Verification command sequence" code block (exact commands, including xcrun xccov)
    CLAUDE.md "Testing" section (Coverage targets table: Domain 100%, ViewModels >= 90%, Services >= 85%, overall >= 90%) and "File size enforcement" section (300-line ceiling command)
  </read_first>
  <action>
    Run, in order: (1) `xcodebuild -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug build` and confirm zero error:/warning: lines; (2) the same with `-configuration Release`; (3) `xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -enableCodeCoverage YES -derivedDataPath build/` and confirm `** TEST SUCCEEDED **` covering both drinkpulseTests and drinkpulseUITests; (4) `xcrun xccov view --report --only-targets build/Logs/Test/*.xcresult` and record the overall percentage plus the per-area breakdown (Domain, ViewModels, Services) for this plan's SUMMARY; (5) `find drinkpulse -name "*.swift" -not -path "*/Preview Content/*" | xargs wc -l | awk '$1 > 300 {print}'` and confirm it reports nothing (no file crossed the 300-line ceiling as a side effect of this phase's edits). If overall coverage has dropped below 90%, or any per-layer threshold in CLAUDE.md's Testing section is missed, do not mark this task done — identify the file that lost coverage (the 2 nonisolated additions in DrinkTypePreset.swift are pure annotation changes with no new branches, so no new coverage gap is expected there) and add a test before closing.
  </action>
  <acceptance_criteria>
    - Debug build: `xcodebuild ... -configuration Debug build 2>&1 | grep -E "error:|warning:"` produces zero lines
    - Release build: `xcodebuild ... -configuration Release build 2>&1 | grep -E "error:|warning:"` produces zero lines
    - `xcodebuild test` (full suite, both targets) output contains `** TEST SUCCEEDED **`
    - `xcrun xccov` report shows overall coverage >= 90%, Domain 100%, ViewModels >= 90%, Services >= 85%
    - `find drinkpulse -name "*.swift" -not -path "*/Preview Content/*" | xargs wc -l | awk '$1 > 300 {print}'` returns no output
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -scheme drinkpulse -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -enableCodeCoverage YES -derivedDataPath build/</automated>
  </verify>
  <done>Full Debug and Release builds are clean, the complete unit + UI test suite passes, and coverage meets or exceeds every CLAUDE.md threshold after the Swift 6 migration — ROADMAP Phase 2 Success Criterion #5 satisfied.</done>
  <reversibility rating="reversible">Verification-only; produces no code change unless a genuine coverage gap surfaces, in which case the fix is an additive test.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Living docs wrap-up (architecture.md, DEVLOG.md, current-focus.md)</name>
  <files>docs/architecture.md, docs/DEVLOG.md, .claude/context/current-focus.md</files>
  <read_first>
    docs/architecture.md lines 94-101 (the `## Concurrency` section, specifically the line "Swift 6 strict concurrency is enabled.")
    docs/DEVLOG.md (the two most recent entries at the top of the file, to confirm the file's newest-first ordering and exact heading/prose format before inserting)
    .claude/context/current-focus.md lines 1-17 (the file's newest-first convention and the exact `## Status: ...` heading format used by the most recent entry)
    CLAUDE.md "Documentation update model" (Living documents table) and "End-of-task checklist" items 3, 6, 8
    .planning/ROADMAP.md Phase 2 section (Success Criteria, to cite accurately)
  </read_first>
  <action>
    Update 3 living docs so they describe the now-completed Swift 6 migration as current reality rather than an aspiration, and confirm a 4th needs no change. (1) docs/architecture.md — in the `## Concurrency` section, extend the sentence "Swift 6 strict concurrency is enabled." with one clause stating it is enforced via `SWIFT_VERSION = 6.0` on the app target in project.pbxproj as of this phase (previously only the test targets carried it) — a single-sentence addition, do not rewrite the rest of the section. (2) docs/DEVLOG.md — append a new entry dated 2026-07-27 at the file's existing newest-first position (verify the actual ordering from the two most recent entries read above before choosing top-of-file), in the file's existing heading/prose format, stating: the app target's SWIFT_VERSION flipped from 5.0 to 6.0 (project.pbxproj); the 2 nonisolated fixes in DrinkTypePreset.swift; the 4 @unchecked Sendable sites gained justification comments; the deprecated-API sweep stayed clean (zero hits); the two remaining XCTest performance-test types were deliberately kept as XCTest (documented decision); and the final overall coverage percentage recorded in Task 2. Never edit or delete any existing entry — this file is append-only. (3) .claude/context/current-focus.md — prepend a new `## Status: ...` entry at the top matching the file's existing convention, summarizing the same facts in 3-5 sentences and stating what comes next (Phase 3: App Startup Hardening). (4) Read .claude/context/open-questions.md and confirm nothing this phase resolves or newly surfaces there — do not edit it; state "no change needed" in this plan's SUMMARY instead. Also confirm README.md and .planning/PROJECT.md need no edit (neither describes build-setting internals) and state that in the SUMMARY too.
  </action>
  <acceptance_criteria>
    - `grep -A5 "## Concurrency" docs/architecture.md` contains the substring `SWIFT_VERSION = 6.0` (or `SWIFT_VERSION=6.0`)
    - `docs/DEVLOG.md` has exactly one new top-level entry heading dated 2026-07-27 that did not exist before this task, and the file's line count only grew (no existing entry text was altered or removed)
    - `.claude/context/current-focus.md`'s first `## Status:` heading now references Phase 2 / Swift 6
    - `.claude/context/open-questions.md` is byte-identical to its state before this task (confirmed unmodified)
  </acceptance_criteria>
  <verify>
    <automated>grep -A5 "## Concurrency" docs/architecture.md | grep "SWIFT_VERSION"</automated>
  </verify>
  <done>architecture.md, DEVLOG.md, and current-focus.md all reflect the completed Swift 6 migration as current reality; open-questions.md, README.md, and PROJECT.md confirmed to need no change; no living doc contradicts the shipped state.</done>
  <reversibility rating="reversible">Documentation-only edits across 3 files; git-revertible; does not affect app behavior or build output.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan edits test-file doc comments and project documentation
only. It reads (and, where warranted, writes) build/test output and
markdown files; no new data flow crosses any process, network, storage,
or user-input boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Repudiation | Living documentation (docs/architecture.md's pre-existing "Swift 6 strict concurrency is enabled" claim) | low | mitigate | The claim was aspirational for the app target prior to Phase 2 (only test targets were actually on SWIFT_VERSION = 6.0). Task 3 ties the doc update to the verified build state from Plan 01/Task 2 of this plan, rather than leaving an unverifiable claim in place. |
| T-02-04 | Tampering | Test-suite integrity (silently dropping the measure {} performance baseline while "documenting a decision") | low | mitigate | Task 1's acceptance criteria explicitly count measure {} occurrences before/after (unchanged: 4 and 3) to prove no baseline was silently removed while adding the decision comment. |

No new attack surface is introduced by this phase — no new data flows, no
new external inputs, no new persisted fields, no new network calls.
</threat_model>

<verification>
`-only-testing:drinkpulseTests/HistoryViewModelPerformanceTests
-only-testing:drinkpulseTests/ScreenComputePerformanceTests` passes; the
full `xcodebuild test` run (unit + UI) ends `** TEST SUCCEEDED **` with
coverage at or above every CLAUDE.md threshold; both XCTest performance
types carry a dated decision comment; architecture.md/DEVLOG.md/
current-focus.md all reflect the completed migration.
</verification>

<success_criteria>
SWIFT6-03 is applied and documented (not left implicit); the full suite is
green with coverage intact after the migration (ROADMAP Phase 2 Success
Criterion #5); and every living doc CLAUDE.md requires to track this
change now matches reality — Phase 2 is fully closed out.
</success_criteria>

<output>
Create `.planning/phases/02-swift-6-language-mode-migration/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

No new files and no new public symbols are produced by this plan:

- `drinkpulseTests/Features/History/HistoryViewModelTests.swift` — modified
  (doc comment only, above `HistoryViewModelPerformanceTests`)
- `drinkpulseTests/Performance/ScreenComputePerformanceTests.swift` —
  modified (doc comment extended)
- `docs/architecture.md` — modified (one sentence in the Concurrency
  section)
- `docs/DEVLOG.md` — modified (one new append-only entry; no existing
  entry changed)
- `.claude/context/current-focus.md` — modified (one new entry prepended)
